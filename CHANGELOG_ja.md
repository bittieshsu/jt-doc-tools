[繁體中文](CHANGELOG.md) ｜ [English](CHANGELOG_en.md) ｜ **日本語**

# 変更履歴（日本語）

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

> **収録の範囲。** 本プロジェクトの主要言語は繁体字中国語で、
> **[CHANGELOG.md](CHANGELOG.md) が完全な履歴です**（852 リリース）。
> この日本語のファイルは**直近のリリース**をまとめたもので、何が変わったか、
> アップグレードすべきかを判断できる程度の内容です。それより古いものは
> 中国語のファイルをご覧ください。

---

## [1.16.23] - 2026-09-25

### Windows：OxOffice を入れると OCR の中国語認識が悪くなる（EasyOCR が読み込めない）

* OxOffice 11.0.5 のインストーラーは、システムの Visual C++ ランタイムを同梱の古い版（14.29）に置き換えます。
  EasyOCR は 14.40 以上が必要なため、OCR が常に Tesseract（中国語の精度が大きく劣る）に切り替わり、画面には
  「EasyOCR が失敗し Tesseract を使用」としか出ていませんでした。従来のチェックはレジストリ（新しい版のまま）
  だけを見ていたため「最新」と判定し、修復していませんでした。v1.16.20〜v1.16.22 のインストーラーで OxOffice が
  入ったマシンと、OxOffice 11.0.5 を別途入れたマシンが該当します。
* インストーラーと `jtdt update` は System32 の実際のファイルの版を確認し、古い版に置き換えられていれば自動で
  修復します（再起動不要）。同じ起動中にすでに一度修復している場合は、再起動後に `jtdt update` を再実行するよう案内します。
* 「依存パッケージ」ページの EasyOCR の行にこの状態を表示します。
* `jtdt update` は「EasyOCR 未インストール」と「インストール済みだが読み込めない」を区別して表示します。
* 該当する場合：管理者として `jtdt update` を 1 回実行すれば直ります。

## [1.16.22] - 2026-09-25

### Windows インストーラー：低速回線で OxOffice が入らない／正常に入っても失敗扱いになる

* OxOffice のインストーラーは約 400 MB で GitHub 上にあります。回線によってはダウンロードが遅く、エラーなしに
  不完全なまま終わることがあり、その後のインストールが 1625（「システムポリシーで禁止」）で失敗していました。
  権限の問題に見えますが、実際はファイルの破損です。現在はサイズとデジタル署名を確認し、合わなければ
  再ダウンロード（最大 3 回）し、それでも駄目な場合に LibreOffice を入れます。
* msiexec の 3010（「インストール完了、再起動を推奨」）が失敗扱いになり、LibreOffice も重ねて入っていました。
  現在は成功として扱います（変換に再起動は不要です）。
* OxOffice のインストールログを `%ProgramData%\jt-doc-tools\Logs\oxoffice-msi.log` に書き出します。
* これらは v1.16.22 のインストーラーで有効になります。コマンドライン版（`install.ps1`）も修正しました。

### OCR：2 件が同時に初回認識すると、ダウンロードしたモデルが壊れる

* OCR の初回は認識モデル（約 300 MB）をダウンロードします。その間に 2 件目が始まると（待ちきれずにもう一度押した、
  2 つのファイルを同時に入れた等）、両方が同じ一時ファイルに書き込んで上書きし合い、展開時に `Bad CRC-32` となって
  ダウンロードが無駄になり、その件は Tesseract（中国語の精度が大きく劣る）に切り替わっていました。
  現在はダウンロードは同時に 1 件だけで、ほかの件は完了を待ってそのまま使います。
* モデルのダウンロードに失敗すると、ページごとに再ダウンロードしていました。現在は 10 分間は再試行せず
  （その間は Tesseract を使用）、その後自動的に再試行します。
* API から送った OCR ジョブも、実際に使った認識エンジンと Tesseract に切り替えたかどうかを完了メッセージに書きます
  （以前は Web 画面側だけでした）。
* サービスログ：ジョブが終わるたびに約 50 行の `Registered tool` が追加され、本当のエラーが埋もれていました。
  起動時に 1 回だけ記録するようにしました。

## [1.16.21] - 2026-09-24

### Windows インストーラー：アップグレード時に「LAN からの接続」の設定を引き継ぐ

* v1.16.20 のインストーラーで「LAN からの接続」を既定でオフにした結果、オンにしていたコンピューターで
  インストーラーを再実行してアップグレードすると、このコンピューターからしか接続できない状態に変わって
  いました（部署全体で共有しているコンピューターに、他の人が突然つながらなくなります）。
  アップグレード時は現在のサービス設定を先に読み、オンだった場合はあらかじめチェックします
  （コンポーネント画面に表示され、外すこともできます）。
* アップグレード時に、待ち受けアドレス（特定のネットワークカードのみなど）と独自のポートも保持します。
  これまではポートが 8765 に戻っていました。
* 新規インストールでは引き続き既定でオフです。
* v1.16.20 のインストーラーですでにこのコンピューターのみに変わった場合は、v1.16.21 のインストーラーを
  もう一度実行し、「LAN からの接続」をオンにしてください（データは保持されます）。

## [1.16.20] - 2026-09-24

### Windows：新規インストール後に各ツールを実際に使って見つかった問題を修正

Windows のインストーラーで新規インストールし、各ツールを一通り実行しました。

* **PDF から文書ファイルへの変換が遅く、変換後のプレビューが空白**：プレビュー作成時に組み立てていた Office
  プロファイルのパスが Windows では正しい URL にならず、毎回 180 秒のタイムアウトを待っていました（1 ページの
  PDF で 3 分）。共通の変換処理を使うようにし、数秒で終わるようになりました。
* **`jtdt-reform` エンジンが Windows で Word ファイルを出力できない**：同じ原因で ODT から Word への変換が
  タイムアウトし、代わりに出力した ODT に `.docx` という名前が付き、画面には「完了」と表示されていました
  （Word で開くと破損扱い）。変換は正常になり、万一 ODT に切り替わった場合も拡張子は `.odt` で、その旨を表示します。
* **文字抽出の ODT 出力**が Windows で止まることがありました。こちらも共通の変換処理を使うようにしました。
* **Office のバージョン確認でプロセスが残る**：Windows ではバージョン確認のために起動したプログラムが終了せず、
  確認のたびに一つずつ残っていました。プログラムファイル内のバージョン情報を直接読むようにしました。
* **初回の OCR** では認識モデル（約 300 MB）をダウンロードしますが、画面には「準備中」としか表示されず、
  回線が遅いと固まったように見えていました。ダウンロード中であること、初回のみであることを表示します。
* 「設定 → アプリ」に表示されるバージョンを、実際にインストールされたバージョンにしました。
* インストール時の依存パッケージのダウンロード出力を
  `%ProgramData%\jt-doc-tools\Logs\setup-python-sync.log` に保存し、失敗時の原因を追えるようにしました。
* コマンドラインのインストーラー（`install.ps1`）が OxOffice のインストーラーを見つけられず LibreOffice を
  入れていた問題を修正しました。

### Windows インストーラー（v1.16.20 版）：最後の段階でまれに異常終了／「LAN からの接続」を既定でオフに

* すべてのインストールが終わった後に、4 回に 1 回程度インストーラーが異常終了していました。サービスは動いて
  いますが、「設定 → アプリ」に登録されず、スタートメニューのショートカットもありませんでした。
  インストーラーが使う NSIS の部品が、大量の出力があるとメモリを壊すためです（NSIS 公式の既知の問題で、
  修正版は未リリース）。出力を取り込まない呼び出し方に変更しました。詳細は引き続き
  `%ProgramData%\jt-doc-tools\Logs\installer.log` に記録されます。**この修正と OxOffice 自動インストールの修正はインストーラーに含まれるため、
  v1.16.20 以降のインストーラーで有効です**。古いインストーラーで発生した場合でも、サービスは `http://127.0.0.1:8765/` で利用できます。
* 「LAN からの接続」を**既定でオフ**にしました。オンにすると同じネットワーク上の他のコンピューターからも接続でき、
  1 台で使う場合はサインインがありません。部署全体で共有する場合はインストール時にオンにしてください。
  インストール済みの場合は、インストーラーをもう一度実行してオンにできます（データは保持されます）。

### API マニュアル：そのまま実行すると結果が正しくなかった 3 か所

* ページ番号の総ページ数は `{N}` です。マニュアルに `{total}` とあり、その文字がそのまま各ページに印字されていました。
  現在はどちらも使えます。
* OCR はジョブ ID を返します（バックグラウンド処理）。PDF ではありません。
* PDF から画像への変換は 2 段階です。変換を送信し、返された `upload_id` でダウンロードします。

## [1.16.19] - 2026-09-24

### 会議要約：レイアウトテーマをダウンロードボタンの上に、書き出した表を読みやすく

* 会議要約の結果にある「レイアウトテーマ」はダウンロードボタンの列の最後にあり、ボタンが多いと次の行に押し出されてボタンの下に表示されていました。
  ボタンの上に独立した行として配置し、先にレイアウトを選んでからダウンロードできるようにしました。
* PDF、Word、ODF に書き出したとき、表の見出しの背景色が文字の周りだけでセル全体を塗っておらず、罫線も濃すぎました。
  見出しはセル全体を塗り、罫線はテーマの淡い色に、本文の行は一行おきに淡い背景色を付け、行の高さも詰めました。
  「Markdown からオフィス文書」も同じレイアウトを使うため、同様に改善されます。

## [1.16.18] - 2026-09-24

### 左側メニューのスクロールバーをドラッグできるように

左側メニューがウィンドウより長いとき右端にスクロールバーが表示されますが、見た目だけのものでした。
押してドラッグしても動かず、下の文字の選択が始まり、メニュー全体が反転表示されていました。

* スクロールバーを押したまま上下にドラッグできるようにしました。ドラッグした距離は、メニューが実際にスクロールする距離に換算されます。
* 押せる範囲を見えているバーより少し広げ、押しやすくしました。マウスを乗せたときやドラッグ中はバーが明るくなります。
* ドラッグ中にメニューの文字が選択されなくなりました。

## [1.16.17] - 2026-09-24

### LLM：Ollama 以外のサービスでも使えるように、API キーを暗号化して保存

LLM による付加機能は Ollama を前提に作られていました。ほかの OpenAI 互換サービス（vLLM、LM Studio、クラウドサービス）に
接続するといくつかの箇所で問題が起き、そのうち二つは気付きにくい形でした。

* **フォーム自動入力の LLM による項目ごとの確認は Ollama 独自のエンドポイントだけを呼び、API キーも送っていませんでした。**
  ほかのサービスではどの項目も問い合わせできず、「問い合わせできない」を「問題なし」として扱っていたため、
  すべて正しく入力されたという結果になっていました。Ollama 以外は標準の OpenAI 互換エンドポイントに、設定したキーを付けて送ります。
  回答が一つも得られなかったときはエラーを表示し、一部の項目だけ問い合わせできなかったときは確認できなかった数を示します。
* **すべてのリクエストに Ollama 専用のフィールドを付けていました**（モデルの思考を止める `think`、`reasoning_effort` など）。
  未知のパラメータを受け付けないサービスではすべて拒否されていました。接続前に相手が Ollama かどうかを確認し、
  Ollama にだけこれらのフィールドを送ります。ほかのサービスで思考するモデルを使うと、応答が少し遅くなることがあります。
* ストリーミング応答で `data:` の後に空白がなくても読み取れるようにしました。ストリームの途中で相手がエラーを返したときは、
  空の回答として扱わず、その呼び出しを失敗にして理由をサービスのログに記録します。4xx / 5xx の応答内容もログに記録します。
* **LLM の API キーを暗号化して保存します。** SSO、通知、音声サービスの秘密情報と同じ扱いで、設定ページと API のどちらにも
  キーそのものは表示されず、設定ファイルはサービスアカウントだけが読めます。以前のバージョンで平文のまま保存されたキーは、
  最初に読み込んだときに自動で暗号化されます。設定のバックアップを別のマシンにインポートすると、そのマシンのキーで暗号化し直します。
* 「接続テスト」では、保存済みのキーは保存時のアドレスにだけ送ります。

## [1.16.16] - 2026-09-24

### 会議録音の文字起こし：台湾語の認識モードでも送信できるように

音声サービスの台湾語文字起こしモードは話者分離を行いませんが、これまではすべてのジョブで話者分離を
依頼していました。そのため管理者が認識モードを台湾語に切り替えると、**すべての**ジョブが拒否されていました。

* 送信前に音声サービスが提供する認識モードの一覧を確認し、選んだモードで実行できる処理だけを依頼します。
  台湾語モードでは話者分離を依頼せず、「発言する人数」も送信しません。
* 結果ページに「今回の認識モードは話者分離を行わない」ことを表示し、話者のない文字起こしが分離の失敗に見えないようにしました。
* 設定ページで、話者分離を行わない認識モードを選ぶとその旨を表示します。
* 認識モードの一覧が読めないときは、推測せず設定どおりの処理で送信します。それでも拒否された場合は、
  認識モードが処理の一部に対応していないことを説明し、再送信か別のモードへの切り替えを案内します。
* API の応答に `diarize_skipped` フィールドを追加しました。

## [1.16.15] - 2026-09-24

### 会議録音の文字起こし：言語に「韓国語」を選べるように

音声サービス（JTLW）が韓国語に対応したため、文字起こしツールの言語メニューに「韓国語」を追加しました。
「自動判定」でも韓国語の録音はすでに認識できていましたが、今後は直接指定できます。

* API から呼び出す場合、`language` に `ko` を送れます。`ko-KR` のような地域付きの表記は `ko` にしてから
  送信します（`en-US`・`ja-JP` と同じ）。
* 管理者が処理設定を台湾語用に切り替えている場合、その設定は韓国語に対応していないため、送信時にその旨を
  表示します。「自動判定」か別の言語を選んでください。
* 自動判定は引き続き録音の冒頭約 30 秒の発話だけで判断します。会議全体が韓国語だとわかっている場合は、
  「韓国語」を直接選んでください。

## [1.16.14] - 2026-09-23

### 文書翻訳・PDF 圧縮：完了後のダウンロードボタンを 1 つに

この 2 つのツールでは、完了すると上の進捗バーにダウンロードボタンが表示され、下の結果欄にもう 1 つ
ダウンロードボタンがありました。どちらも同じファイル名の同じファイルで、別のものに見えていました。

* 結果欄のボタンを削除し、ダウンロードは上の「ダウンロード／ワークスペースに保存／新しいファイルを処理」の
  列に統一しました。ほかのツールと同じ位置です。
* 文書翻訳のプレビューの終わりにある「翻訳後のファイルをダウンロード（全 N ページ）」は残します。文書の
  ページ数がプレビューより多いときだけ表示され、「プレビューが最初の数ページだけ」を「最初の数ページしか
  翻訳されていない」と誤解しないためのものです。
* 共通の進捗バーを使うツールが結果欄に独自のダウンロードボタンを置く場合は、進捗バー側を隠すことを
  自動チェックで求めるようにしました。今後追加するツールでボタンが 2 つにならないようにするためです。

## [1.16.13] - 2026-09-23

### 音声サービスの略称を大文字の `JTLW` に統一しました

jt-live-whisper の略称は画面と文書で小文字で表示されており、`JTDT` と並ぶと別の種類のもののように
見えていました。設定ページ、文字起こしページ、エラーメッセージ、紹介サイト、API マニュアル、README、
トラブルシューティングページの**文言**を `JTLW` にしました。

* **URL、モジュール名、設定ファイル名、ページ内アンカーは小文字のまま**です（例：`/admin/jtlw`）。
  変更すると既存のブックマーク、API 呼び出し、設定のインポートが合わなくなるためです。
* 英語と日本語の画面も合わせて変更し、略称が小文字に戻らないよう自動チェックを追加しました
  （識別子は対象外）。

### 紹介サイト：スマートフォンのメニューで GitHub ボタンの上端が切れて見えた

画面が狭くナビゲーションがメニューにまとまると、GitHub の項目は上の角が四角く、上の枠線だけ他の 3 辺と
違っていたため、上端が切り取られたように見えていました。上に区切り線を引こうとしたスタイルと、ボタン
自体の四辺の枠線が重なっていたのが原因です。

* 上の言語メニューと同じ枠にしました：幅・高さ・角丸・枠線をそろえ、文字は上下中央に配置します。
* 3 言語それぞれ、スマートフォン幅でメニューを開いた状態をブラウザで計測し、既存のナビゲーションの
  チェックに追加しました（幅・高さ・角丸が同じで、上下の枠線がそろっていること）。

## [1.16.12] - 2026-09-23

### 更新時に uv の「`UV_NATIVE_TLS` は非推奨」という警告が出なくなりました

企業の TLS 検査環境で uv が OS の信頼ストアを使うよう、新旧 2 つの環境変数（`UV_SYSTEM_CERTS` と
`UV_NATIVE_TLS`）を両方設定し、uv が知らない方を無視すると想定していました。しかし新しい uv は古い方も
認識し、**毎回非推奨の警告を表示**するため、`jtdt update` とインストールのたびに、更新が失敗したように
見えていました。

* uv 自身に新しいフラグに対応しているかを確認し、**認識する方だけを設定**します：新しい uv には
  `UV_SYSTEM_CERTS`、古い uv には `UV_NATIVE_TLS`。確認できないときは古い方を使います（どのバージョンでも
  認識します）。自分で設定した値はそのまま尊重します。
* `jtdt update`、Linux / macOS のインストールスクリプト、Windows インストーラーが実行する
  `setup-python.cmd` の 3 か所を変更しました。Windows の部分は実機の `cmd.exe` で、新しい uv・古い uv・
  ユーザー設定の 3 ケースを確認済みです。
* このバージョンへの更新そのものでは、まだ一度警告が出ることがあります（更新を実行するのは古いコードの
  ため）。次回の更新からは出ません。
* この機能は v1.12.12 で追加されて以来テストがありませんでした。実際の uv で警告が出ないことを確認する
  テストを含めて追加しました。

## [1.16.11] - 2026-09-23

### `jtdt update` が必ず「Health check failed」で終わっていた（サービスは正常）

v1.15.11 以降、**Linux・Windows・macOS のすべてで `jtdt update` の最後の手順が失敗と判定**
されていましたが、サービス自体は正常に起動していました。ヘルスチェックが使うダウンロード用
モジュールが実際には import されておらず（import 行が、ファイル内で生成しているスクリプト文字列の
中に入っていた）、毎回の接続がエラーになり「接続できない」として扱われていました。

* ヘルスチェックが実際にサービスへ問い合わせるようになりました。
* **v1.16.11 への更新そのものでは、まだ一度このメッセージが出ます**。チェックを実行するのは更新前の
  バージョンだからです。ブラウザで開ければ更新は成功しています。次回の更新からは出ません。
* Windows の「プログラムと機能」のバージョンはヘルスチェック通過後に更新されるため、この期間は古い
  番号のままでした。次回の更新が成功したときに自動で直ります。
* 同じ原因で、いくつかの予備処理も動いていませんでした（警告が 1 行出るだけで更新は止まりません）：
  Windows での VC++ ランタイムの自動インストール、繁体字 OCR 言語ファイルの旧ダウンロード経路、
  WinSW の予備ダウンロード。

### ヘルスチェック失敗時に、サービスの実際のログを表示

* **Windows**：以前は `<データフォルダ>\logs\jt-doc-tools.log` を探していましたが、このフォルダは
  存在しないため、常に「no log at …」と表示されていました。サービスのログは
  `C:\ProgramData\jt-doc-tools\Logs\` にあります：`jtdt-svc.err.log`（起動メッセージと例外）、
  `jtdt-svc.out.log`（サービス自身のログ）、`jtdt-svc.wrapper.log`（サービスのラッパー）。
* **macOS**：`~/Library/Logs/jt-doc-tools.log`（サービス自身のログ）だけを読んでいましたが、uvicorn の
  起動メッセージと捕捉されなかった例外のスタックトレースは同じフォルダの **`jt-doc-tools.err`** に
  書かれます。サービスが起動しないときに一番必要なのはそちらです。両方を表示し、`.err` を先にしました。
* **Linux** は `journalctl` で、もともと正しく動いていました。
* `jtdt logs` も同じ一覧を使います。Windows でシステムのコードページ（繁体字中国語なら cp950）で
  書かれたログも正しく読め、大きなログは末尾だけを読みます。
* サービスの応答を待つ時間を約 15 秒から最大 2 分に延ばし、15 秒ごとに「まだ待っている」と表示します。
  更新直後の初回起動は再コンパイルとウイルス対策ソフトのスキャンで時間がかかり、15 秒では足りない
  ことがよくありました（Apple M2 のノート PC で古いバージョンから更新したところ、チェック開始時点で
  まだツールを読み込んでいました）。
* 更新の最後に出ていた `DeprecationWarning`（`locale.getdefaultlocale`）はなくなりました。Windows では
  システムの表示言語で、トラブルシューティングページの中国語版と英語版を選びます。
* トラブルシューティングページのヘルスチェックの項目に、このケースと各プラットフォームのログの場所を
  追加しました。

### 同じ種類の不具合がほかに 4 か所：定義されていない名前を使っていた

全コードを対象にした自動チェックで、同じ種類の誤りが見つかりました：

* **ブラウザで「ログインが必要 / 権限がない / ページが見つからない」のときの案内ページが 500 に
  なっていました**（v1.14.16 以降）。
* 資産バックアップのインポートで zip 爆弾を検出したとき、400 ではなく 500 を返していました（v1.15.13 以降）。
* 統一編号データベースのスケジュールに不正な値を入れたとき、400 ではなく 500 を返していました（v1.14.51 以降）。
* OCR 言語ファイルのバリアント切り替えに失敗したとき、失敗を返さず例外を出していました（v1.7.5 以降）。

`app/` と `tools/` のすべてのファイルで、定義していない名前を使えないようにしました（独自のスコープ
解析で、追加のパッケージは不要です）。

### LLM 無効時：既定はグレー表示、新しいオプションを選んだときだけ非表示

* LLM 設定ページに「無効時は非表示にする」を追加しました。**既定はオフ**で、無効のあいだもツールと
  オプションはグレー表示のまま見え、使えない理由がわかります。チェックすると画面から外れます。
  LLM が有効なときは何も影響しません。
* LLM がないと使えない 3 つのツール（文単位翻訳・文書翻訳・会議要約）は、無効時にサイドバーと
  ホームで理由つきのグレー表示になりました（以前は普通に表示され、開いてから使えないとわかった）。
  オプションをチェックすると、サイドバー・ホーム・検索から外れます。
* フォーム自動入力とテキスト抽出の LLM オプションは無効時に丸ごと消えていましたが、ほかと同じく
  グレー表示になりました。
* 設定ページには無効時に AI 機能が「自動で非表示になる」と書かれていましたが、実際はグレー表示でした。
  説明を実際の動作に合わせ、3 か所で 3 つの数字だったツール数はプログラムから計算するようにしました。
* ホームのツール一覧をサイドバーと同じデータにしました。以前は、サイドバーでグレー表示のツール
  （音声サービス未設定の会議録音の文字起こしなど）がホームでは普通に表示されていました。

### その他

* JavaScript で設定する 3 つのツールチップ（サイドバー検索の件数、権限マトリックスの「組み込み
  アカウントの権限は固定」、ページ別押印エディタのページサムネイル）が、英語と日本語の UI でも
  中国語のままでした。翻訳チェックは `=` の直後の文字列しか見ておらず、条件式を見落としていました。
  値全体を見るようにしました。
* 翻訳キーのチェックは `{{ tr('…') }}` しか見ておらず、`tr('…')|replace(…)` を見落としていたため、
  範囲を広げました。

## [1.16.10] - 2026-09-23

### 会議録音の文字起こし：順番待ちが見えるようになり、待ち時間は上限に含めなくなりました

2026-09-23 の音声サービスの更新以降、GPU の順番待ちの間は前に何件あるかが報告され、認識と補正の進捗も
進むようになりました。

* 順番待ちの間は「順番待ち（前に N 件）」と表示します（件数には他の人の処理も含まれます）。補正中は
  何バッチ完了したかを表示します。
* **順番待ちの時間は待機の上限に含めません**：上限は順番が来てから数えます（15 分と録音の長さの半分の
  うち大きい方）。長い会議の後ろに並んでも誤ってタイムアウトしなくなり、追加の 60 分の猶予は廃止しました。
* 順番待ちそのものには全体の上限（4 時間）があり、GPU サーバーが止まっても枠を占有し続けません。
* **「進捗が止まっている時間」で取り消しはしません**：音声サービスによると、順番が来た後も待機の表示が
  数秒残り、補正はバッチごとにしか更新されず、長い会議は測定していないため、止まっている時間で判断すると
  長い会議を誤って打ち切ってしまいます。
* 更新されていない音声サービス（応答に順番待ちの項目がない）では従来どおりです。
* あわせて修正：サーバーが送る「キューが満杯です。N 秒後に再試行します」「N 分待ちましたが…」などの
  メッセージが、言語ファイルに訳がなく英語・日本語の画面で中国語のまま表示されていました。訳を追加し、
  翻訳チェックの対象にしました。

### 会議録音の文字起こし：「自動判定」は録音の冒頭だけを見ます

音声サービスの測定によると、「自動判定」は**区間ごとに判定するのではなく**、録音の冒頭約 30 秒の発話で
会議全体の言語を決めます。中国語が中心でも冒頭で誰かが英語を話す会議（20 秒で十分です）は全体が英語で
認識され、中国語の文の誤り率は 15% から 88% に上がります。**冒頭の無音、雑音、歌声のない音楽は影響しません**
（発話のない部分は先に取り除かれます）ので、削る必要はありません。言語を指定しても自動判定より悪くなる
ことはありません（判定が正しい場合、結果はまったく同じです）。

* 言語オプションの説明をこの内容に改め、v1.16.9 の「より正確になります」は削除しました。本当の違いは
  「会議全体を誤判定しないこと」です。
* 中国語と英語の文が入れ替わる会議では、多く使われている方の言語を選んでください。もう一方の言語の文は
  ほとんど正しく認識されません。中国語の文の中に英語の用語が混ざる程度なら問題ありません。
* トラブルシューティングに「中国語の会議が全体を通して英語として認識される」を追加し、順番待ちの項目を
  新しい動作に合わせて書き直しました。

### 会議録音の文字起こし：波形にカーソルを置くと、その時点の発言者も表示

カーソル横の時刻ラベルに、その時点の発言者を表示するようにしました（文字起こしと同じ色の点と名前。
名前を変更した場合は新しい名前）。区間と区間の間の空白では時刻だけを表示し、推測で誰かを表示しません。
2 人が重なっている場合は後から話し始めた人を表示します。

### 会議の要約：発言者の名前を変更すると、ほかのブロックも変わる

これまでは文字起こしだけが新しい名前になり、タスクのカードは「担当：S1」のまま、要約と
「誰がどれだけ話したか」も古いコードのままでした。

* カード（担当者と本文）、要約、章、マインドマップ、「誰がどれだけ話したか」がすべて変更に追従し、
  ダウンロードするファイルにも反映されます。
* 自由記述の中で置き換えるのは**コード形式**の名前（`S1`、`SPEAKER_00`）だけです。実名は項目全体が
  その名前と一致する場合だけ置き換え、`S1` の変更で `S12` の一部が置き換わることもありません。
* 「誰がどれだけ話したか」は変更後の文字起こしから再計算します。1 件だけ変更した場合はその発言の
  回数と時間が移り、2 つのコードを同じ名前にすると 1 人にまとめられます。
* 1 件だけの変更では、カードと要約の中のコードは変えません（そこでの `S1` は人物全体を指すため）。

### 会議録音の文字起こしから会議の要約へ：変更した名前を引き継ぐ

文字起こしツールで変更した発言者名が、会議の要約に送ると `S1` に戻っていました。
今は変更した名前（1 件だけの変更を含む）のまま表示し、画面も「分析を開始」まで自動でスクロールします。

### 会議の要約：要約を書いている途中なのに進行状況バーが満杯

4 つの段階がそれぞれ 4 分の 1 を占め、しかも「N 件目を開始」を「N 件目が完了」として数えていたため、
要約を書き始めた時点で 100% になっていました。再確認と「事象と影響」の一巡（呼び出し回数は要点の抽出と同じ）
も進行状況を報告していませんでした。

* 残りのモデル呼び出し回数に応じて配分し、すべての段階が報告するようにしました。
  **完了するまで 100% にはなりません**。
* 英語と日本語の画面で進行状況の文言が中国語のままだった問題も直しました。

### 会議の要約：アップロード後のプレビューが「最初の数件だけ」だと分かるように

これまでは灰色の小さな一行で、文字起こし全体がこれだけしか読まれていないと誤解されやすい表示でした。
今は目立つ説明枠で「全体で M 件、ここに表示しているのは最初の N 件、分析には全体を使う」と示し、
一覧の最後の行でも表示していない件数を知らせます。

### 会議録音の文字起こし：同期 API が音声サービスに拒否されると 500 を返していた

`POST /tools/meeting-transcribe/api/meeting-transcribe` は、音声サービスがリクエストを拒否すると常に
500 を返していましたが、API マニュアルには 400 と書かれていました。500 だと呼び出し側はサービスの故障と
判断して再試行を続けてしまいます。今は原因に応じて返します：送ったパラメータが拒否されたら 400、
音声サービス側の処理失敗は 502、未設定または接続できない場合は 503、時間がかかりすぎた場合は 504。
Web 画面の経路には影響ありません。API マニュアルにも「自動判定」は録音の冒頭しか聞かないことを追記しました。

### スキャン補正：プレビューがときどき表示されない、または古い結果が表示される

同じページのプレビューは常に同じファイルに書き込んでいました。四隅のドラッグや回転では複数のリクエストが
連続して送られ、画面側は古いものを取り消しますが、サーバー側は処理を最後まで続けて後から同じファイルに
書き込むため、最新のプレビューが古いもので上書きされたり（状態行は新しい結果を示したまま）、
書きかけのファイルをブラウザーが読んで右側が空白のままになったりしていました。今はプレビューごとに
別ファイルとし、書き終えてから差し替え、最新の数件だけを残します。

---

## [1.16.9] - 2026-09-23

### 文書翻訳：Excel で保存したスプレッドシートが、翻訳後に Excel で開くと空白になる

症状は、**プレビューは正常なのに、ダウンロードしたファイルを Excel で開くと空白**になること
（または先に「修復しますか」と聞かれ、修復するとデータがすべて消えます）。

原因はファイルに書き戻す段階です。使用している XML ライブラリが出力時に、**登録されていない
名前空間のプレフィックスを付け替え**（`x14ac` が `ns3` に）、**本文で使われていない宣言を
削除**していました（`xr2`、`xr3`）。XML としては正しいままですが、Excel はシートの先頭に
`mc:Ignorable="x14ac xr xr2 xr3"` と書き、これは**プレフィックスの名前で**参照しています。
名前が宣言と合わなくなると、Excel は内容全体を破損と判定します。

* **プレビューでは分かりません**：プレビューは OxOffice / LibreOffice で描画しており、
  この属性を無視するためです。
* **Word と PowerPoint で保存したファイルも同じ問題**：本文にも `mc:Ignorable` があり、
  テキストボックスには `Requires="wps"` があります。
* **これまで見つからなかった理由**：手元のスプレッドシートのサンプルはすべて
  OxOffice / LibreOffice で保存したもので、この属性がありませんでした。新しいテスト素材は
  Excel と Word で保存したファイルの形にし、実サンプルもすべて通しています。

書き戻し時のプレフィックスと宣言は元のファイルのとおりになり、`standalone="yes"` も
残ります。**翻訳済みのファイルは自動では直りません。もう一度翻訳してください。**

一文ずつの翻訳はスプレッドシートを受け付けないため、影響はありません。

### 会議録音の文字起こし：「中国語」を選ぶと送信のたびに失敗する

言語で「中国語」を選ぶと、すべての処理が送信時点で音声サービスに拒否され、
`language` 欄の `invalid_request` と表示されていました。原因は本システム側です。
ドロップダウンが `zh` を送っていましたが、音声サービスが受け付けるのは BCP-47 の
`zh-Hant` です（英語・日本語は `en` / `ja` を送るため問題ありませんでした）。
API マニュアルは以前から `zh-Hant` と記載しており、画面のドロップダウンだけが誤っていました。

* ドロップダウンは `zh-Hant` を送るようにし、サーバー側でも `zh` や `zh-TW` などの一般的な
  書き方を `zh-Hant` に置き換えます。`zh` を送る API 呼び出しも拒否されません。
  **簡体字（`zh-CN`）が繁体字に置き換えられることはありません。**
* 音声サービスが言語設定を受け付けない場合、次にすべきこと（「自動判定」を選んで再送信）を
  表示するようにしました。
* **テストが通っていた理由**：テスト用の偽の音声サービスが言語コードを検査していませんでした。
  現在は本番 API と同じく未知のコードを拒否し、ドロップダウンの各選択肢を実際に一度ずつ送信します。
* 言語ドロップダウンの横に説明を追加しました：会議全体の言語が分かっている場合は、
  選択すると認識がより正確になります。

### 会議録音の文字起こし：録音の最後に長く文字がない場合にお知らせ

音声サービス側からの連絡によると、結果の送信中にサーバーが再起動した場合、半分しか送られて
いない文字起こしが成功として返ることがあります（先方で修正済み、2026-09-23 に適用済み。以降この
お知らせは主に録音の止め忘れを知らせる役割になります。空白の長さは処理ごとに記録し、残すかどうかは後で判断します）。
1 区間もない結果は以前から失敗として扱っています。**後半が欠けたもの**については、
結果画面に「録音の最後の N 分 N 秒に文字がありません」と表示するようにしました。

* **失敗ではなくお知らせのみ**：録音の止め忘れや、拍手・音楽で終わる録音も同じ見た目になり、
  それは完全な文字起こしです。空白の長さを表示するので、録音の最後に何があったかは
  利用者自身が判断できます。
* しきい値は 60 秒：音声サービスが 25 件の録音で測った自然な空白は最長 41.8 秒でした。

### 紹介サイト

* 音声連携の節を「重要な録音がネットワークの外へ出ることはありません」に変更しました（README も同様）。

---

## [1.16.8] - 2026-09-23

### 音声サービス失敗時のメッセージ：2 つは向きが誤り、1 つは次の手が示されていなかった

音声サービス側がこちらのトラブルシューティングページを確認した際に 2 点を指摘し、
**製品の画面に出るエラーメッセージも同じ誤り**をしていました。

* **401 と 403 を区別**。以前は「キーが失効していないか、入力ミスがないか確認」という 1 文に
  まとめていましたが、403 はキーが**正しく**、その操作の権限がないだけなので、その確認では
  原因が見つかりません。403 では相手の管理者に権限の追加を依頼するよう案内し、不足している
  権限はサービスのログに記録します。
* **URL の期限切れは順番待ちが原因ではない**。相手は送信を受け取るとすぐに取得し、その後
  この URL を再び取りに来ることはありません。以前の「順番待ちが長かったため。管理者に有効期間を
  延長してもらってください」は、相手の順番待ちを疑わせ、**存在しない設定**を変えさせるものでした。
* **成功と報告されたのに文字起こしが 1 区間もない**：結果の送信中に相手のサーバーが再起動すると
  起こり得ます（相手側で修正済み、全面展開前）。本システムは以前から失敗として扱い、空の文字起こしを
  渡すことはありません。メッセージで「もう一度送信すればたいてい解決する」と案内するようにしました。

これらのメッセージにはテストがなく、翻訳ガードが見る対応表の外にありました。同じ対応表に
まとめて両方のガードの対象にし、振る舞いのテストを追加しました（両方向の変異で確認済み）。
トラブルシューティングページも 3 言語で更新し、空の文字起こしの項目を追加しました。

---

## [1.16.7] - 2026-09-23

### 文字起こし：順番待ち用に待ち時間の上限を 60 分延長

音声サービスの GPU は 2026-09-23 から**一度に 1 件だけ処理し、残りは順番待ち**になり、
順番待ちの間も認識中も「処理中」のまま進捗がまったく動かないため、本システムでは
「順番待ち」と「停止」を区別できません。前に 3 時間の中国語会議が並んでいると
約 18～30 分待つことになり、従来の下限 15 分を超えて**タイムアウト扱いになり、
本システムが相手にキャンセルを依頼**していました。成功するはずのジョブです。

* 上限を「15 分と録音の長さの半分のうち大きい方」に**順番待ち用の 60 分を加えた時間**に変更。
  本当に止まったジョブを諦めるのは 1 時間遅くなりますが、正常なジョブを止めてしまうよりはましです。
* 音声サービス側で「順番待ち中・前に N 件」の信号が追加されたら、順番待ちを上限に数えない
  方式に切り替え、この猶予は外します。
* **ポーリングのループを実際に動かす**テストを追加（偽の時計と、ずっと「処理中」で進捗が
  動かない偽の相手）。この上限にはこれまでテストが 1 本もありませんでした。
* トラブルシューティングページの説明も更新。

### CI：定期実行のテストが「本番の依存バージョン」ジョブで失敗

「公開ツリーに内部アドレスがあってはならない」ガードはディレクトリ全体を走査しており、
このジョブは `uv sync` で仮想環境をリポジトリ内に作るため、サードパーティのパッケージ
ソースにあるアドレスが本プロジェクトの流出として報告されていました。ローカルでは
（開発ツリーに仮想環境がないため）決して失敗しません。**git で追跡されているファイルだけ**
（実際に公開されるもの）を走査するよう変更し、`.git` がない開発ツリーでのみディレクトリを
走査します。実際のクローンで両方向を確認済みです。

### 入力欄の例示テキストを薄く

ブラウザ既定の例示テキストの色は入力済みの文字とほとんど同じ濃さで、複数行の例
（会議要約の貼り付け欄や会議の背景）がすでに入力された内容のように見えていました。
サイト全体で 1 つの薄いグレーに統一し、実際のブラウザで「ブラウザ既定より薄い」ことを
確認するガードを追加しました（色コードではなく輝度で判定）。

> このガードの初版は空振りでした。管理画面では最初の `input[placeholder]` を拾っており、
> それが**サイドバーの検索欄**（紫地に白、もともと明るい）だったためです。
> 現在は実際の入力欄を指定しています。

---

## [1.16.6] - 2026-09-23

### 「マイジョブ」は 24 時間と表示、実際は 2 時間で結果が削除されていた

完了したジョブの「開く」が **410（ファイル期限切れ）** になり、ジョブ一覧には
「結果ファイルは 24 時間後に自動削除」と表示されていました。本番環境で確認すると、
5 時間以内に完了した会議要約のジョブ記録と「完了」状態は残っているのに、分析結果・
文字起こし・所有者記録はすでに削除されていました。

原因は **2 つの保持期間が一致していない**こと。全ツールのジョブ結果と「開く」で読み込む
データは一時領域（2 時間）に置かれ、ジョブの保持期間は 24 時間です。v1.14.31 で
ジョブ結果のディレクトリをジョブ保持期間に合わせましたが、**そのディレクトリに結果を
書くツールは 1 つもなく**、修正は半分だけでした。影響は会議要約だけでなく
**すべてのバックグラウンドジョブ**に及びます。

* 一時領域の清掃時、**保持期間内（または実行中）のジョブ**に属するファイル
  （結果ファイル、ジョブ番号を名前に含む一時ファイル、所有者記録）はジョブの保持期間に従います。
* ファイル名に埋め込まれたジョブ番号で判別し、**各ツールに登録させない**方式
  （次の新ツールが必ず登録を忘れるため）。
* どのジョブにも属さない古い一時ファイルは従来どおり 2 時間で削除（逆方向の対照テストあり）。
* **清掃の経路は 2 つありました**。保持期間の清掃とは別に、30 分ごとの一時ファイル清掃が
  固定 2 時間で動いており、**管理画面の一時ファイル保持期間の設定を無視していました**。
  修正の初版は前者だけを変えており、デプロイ後に本番環境で確認すると、2 時間を少し過ぎた
  ジョブのファイルが後者に削除されていました。現在は両方が同じ処理を通り、設定も実際に効きます。

> すでに削除された結果は復元できません。修正はアップグレード後から有効です。

### レイアウト：数値欄が行幅いっぱいに伸びる、状態が 2 行に割れる、JTLW の正式名が見つからない

* 小さな数値欄 5 か所（音声サービスのタイムアウト、ワークスペース設定 3 項目、発言者数）が
  行幅いっぱいに伸び、単位が右端に追いやられていました。共通の固定幅スタイルに統一。
* ユーザー一覧の状態表示が、表が窮屈なときに 2 行に割れていました。
  これまでのスクリーンショットは空のユーザー一覧だけだったため見落とされていました。
  この列だけ直すと隣の役割バッジが割れるため、両方とも改行しないようにしました。
* **JTLW の正式名（jt-live-whisper）とプロジェクトページへのリンク**は、設定ページでは
  タイトル行の右上にあって見つけにくく、文字起こしツールには一切ありませんでした。
  両ページとも説明文の後ろに置き、同じコンポーネントを共用します。

> いずれも**実際のブラウザで計測する**ガードを追加しました（欄の幅、行ボックスの数、
> 表示領域）。修正の初版は管理画面でまだ行幅いっぱいと計測されました。実際の原因は
> より詳細度の高い別のルールで、このガードがその場で見つけています。

### 会議系 2 ツールのサーバー側ラベルが公開以来ずっと未翻訳だった

**データの入った**インスタンスで英語のスクリーンショットを撮影中に判明。
文字起こしの書式ドロップダウン、変換ジョブの各ステージ、マインドマップの
ノード種別、音声サービスが返す各失敗理由が、英語・日本語の画面で**すべて
中国語のまま**でした。共通点は「文字がサーバーのデータから来る」ことで、
テンプレート内の `tr('…')` リテラルを走査するガードには見えません。

* 各言語ファイルに 66 項目を追加。
* 5 つの取得元を「プログラムが算出するラベル」のガードに追加し、
  それぞれ変異検証で自分の行だけが赤くなることを確認。
* ジョブの**失敗理由**も `tr()` を通すように変更。進捗メッセージと同じく
  バックグラウンドスレッドで生成される中国語であり、この 1 層がないと
  最も読みたい一文が中国語のままになります。

> **レポートが先頭 4 件しか表示していませんでした。** 実際は 54 件あり、
> 「残り 4 件」が「4 件しか漏れていない」に見えていました。全件表示に変更。

### 「発言者」への統一と、今回こそガードを追加

前日に指摘を受けて手作業で直しましたが、**何も防いでいなかった**ため
3 か所が残っていました。禁止語リストに追加したところ、文書側でさらに
4 か所が見つかりました。

### 貼り付けた文字起こしの判定をファイル名からフラグへ

貼り付け時にブラウザが付けるファイル名は**利用者に見えます**（ジョブ名、
通知）。そのため画面の言語に合わせて翻訳する必要がありますが、サーバーは
そのファイル名を文字列比較して文書タイトルに使うかどうかを決めていました。
翻訳すると比較が成立せず、タイトルが「Pasted transcript 議事録」になり、
**画面上では何も異常に見えません**。

### 紹介サイト：言語メニューと GitHub ボタンの高さが揃っていなかった

それぞれが独自に高さを計算しており、実測で **12.2 px** の差（30.2 と 42.4）、
垂直方向も揃っていませんでした。**padding の数字は調整していません**。
言語を増やしたり文字サイズを変えたりすれば、また崩れるからです。
3 言語版をブラウザで実測するガードを追加しました。

### 人数のヒント：1 件から 20 件へ、ただし表現は強めない

以前は**1 件**の会議の数字を引用していました。音声サービスが公開コーパスの
20 件すべてで測り直した結果、正しい人数を指定した場合と指定しなかった場合に
**有意な差はありませんでした**（13.24% 対 12.46%、対応のあるブートストラップの
95% 信頼区間は 0 をまたぎます）。したがって「迷う場合は 0 のまま」は従来どおり、
**「任せたほうが良い」には書き換えません**。

> その数字より仕組みのほうが確かです。同じ 20 件で、最適なクラスタ数が
> **実際の人数を上回ったことは一度もありませんでした**。6 人がその場にいても、
> 音響的に 6 グループに分けられるとは限りません。

### ドキュメント

README と紹介サイトに音声サービス連携の節（使い方・効果・分担の理由）を
3 言語で追加。ショーケースに「会議要約」のスクリーンショットを追加。
新しい 2 ツールの説明を簡潔にし、欠けていたアイコンを補完。新しい同期 API
も文書化しました。

> API カバレッジのガードは `/api/` パスを**持つ**ツールから表を作っていたため、
> API が 1 つもないツールは見えていませんでした。ツールレジストリと
> 突き合わせる方式に変更しました。

---

## [1.16.5] - 2026-09-22

### プレーヤーの見た目を整えました

再生ボタンを円形の主要ボタンに（隣の副次ボタンと同じ見た目でしたが、
プレーヤーの主役はこちらです）。再生位置を大きく、総再生時間を控えめに。
波形の上でポインターを動かすとガイド線とその位置の時刻が表示され、
**クリックする前にどこへ飛ぶかが分かります**。

---

## [1.16.4] - 2026-09-22

### 「会議要約」の文字起こしでも話者名を変更できます

「会議録音の文字起こし」と同じ操作、**同じ配色**です。同じ会議なのに
ツールによって `S1` が青だったり緑だったりするのは避けるべきです。
名前をクリックすると既定ではその話者の全区間が変わり、
チェックを外すとその 1 区間だけが変わります。

**統計も一緒に移動します**。発言割合のグラフは話者コードをキーにしているため、
文字起こしだけを変更するとグラフは `S1` のまま、本文は実名という状態になります。
**`seq` は一切変更しません** —— 決議とタスクは `seq` で引用しているため、
名前の変更で引用が壊れてはいけません。

---

## [1.16.3] - 2026-09-22

### 文字起こしプレーヤーの 3 点修正

* 再生アイコンが一時停止アイコンに切り替わるようになりました。両方のアイコンを
  描画し、外側の `<span>` で切り替えます。**`<svg>` に `.hidden` は設定できません**
  （`SVGElement` にその属性はありません）。
* **波形が小さすぎました。** 一般的な会議録音のピークは 0.1〜0.3 のため、
  絶対値のまま描くと中央に細い線が出るだけです。最大値で正規化し、
  平方根で圧縮して静かな部分も見えるようにしました。
* **時刻をクリックしたときのスクロール位置がずれていました。** `offsetTop` は
  「最も近い**位置指定された**祖先」からの距離で、文字起こし欄には `position`
  が設定されていないため、パネル上端からの距離を測っていました。
  `getBoundingClientRect()` 2 つの差と `scrollTop` で計算するようにし、
  **CSS に一切依存しなく**なりました。

---

## [1.16.2] - 2026-09-22

### 波形とプレーヤーが一度も表示されなかった — `@router.get` は HEAD を受け付けません

録音がまだ存在するかを HEAD で確認してから表示していましたが、
**FastAPI の `@router.get` は GET だけを登録します**（Starlette の `Route` は
HEAD も追加しますが、FastAPI はしません）。確認は常に **405** を返し、
プレーヤーは一度も表示されませんでした。エラーはどこにも出ません。

両方を修正しました。エンドポイントは HEAD にも応答し（ガードあり）、
画面は確認リクエストで表示可否を決めるのをやめ、`<audio>` 自身の `error`
イベントに任せます。

> **「念のため確認」のリクエストで機能の表示を決めてはいけません。**
> 確認の失敗と「本当に何もない」は画面上で区別がつかず、
> 確認自体が壊れうる経路をもう 1 つ増やします。

---

## [1.16.1] - 2026-09-22

### 聞きながら読めるようになりました

文字起こしカードに波形とプレーヤーを追加しました。波形のどこか（または時刻）を
クリックするとそこから再生し、再生中の区間がハイライトされて自動的に表示され、
**話者ごとに色が変わり**、話者コードをクリックすると名前を変更できます
（既定はその話者の全区間、チェックを外すとその 1 区間のみ）。
**ワークスペースへ保存**も追加しました。保存されるのは `[mm:ss]` と話者名を
含むプレーンテキストです。

波形はブラウザー側で計算するため、サーバーに ffmpeg は不要です。
**60 MB を超える録音はデコードしません** —— 3 時間の会議は数 GB の PCM になり、
タブが落ちます。その場合は**クリック可能なまま**のタイムラインに切り替わります。

> **再生はログイン済みの所有者チェックを通ります。署名付き URL ではありません。**
> 署名付き URL は音声サービスがファイルを取得するためのもので（短時間・ログイン不要）、
> 画面で使い回すと期限切れになるか、恒久的に公開することになります。

### 用語：「語者」を「發言者」に

会議で話す人を指す言い方として、台湾では「語者」も「講者」も使いません。
どちらも 發言者 に統一しました。1 つのものを 2 つの言葉で書くと、
2 つのもののように読めてしまいます。検索キーワードは旧表記も残しています。

---

## [1.16.0] - 2026-09-22

### 音声サービスの接続チェックリストに沿って補完

**エラーコードの対訳表を、2 つの経路のうち片方しか使っていませんでした。**
送信時の拒否は例外として、終了状態の失敗は `errors[]` として届きますが、
対訳表を参照していたのは後者だけでした。先方の v1.7 以降は送信時点で音声を
取得するため、ソース関連のエラーは主に前者を通ります。本番では
`JTLW がこのリクエストを拒否しました：source_not_allowed（項目 source.url）`
と表示されていましたが、対訳表には「管理者に許可リストへの追加を依頼してください」
が既にありました。文言は 1 か所にまとめ、ガードを追加しました。

ほかに追加したもの：ポーリングの実時間上限（**これまで存在しませんでした**）、
`queue_full` 時の `retry_after_ms` に従った再送、校正が失敗したときに
「表示しているのは認識直後の結果です」と明示、`task_not_requested` を
エラーではなく「その層が無い」として扱う、`/result` から校正統計を表示、
`correction_level: punctuation_only` の送信。

### 時刻情報は認識ではなく「受け渡し」で失われていました

「文字起こしに時刻がない」という報告があり、音声サービスは校正済みの層しか
読んでいないのではと推測しました。**そうではありませんでした。** 本番に保存された
258 区間すべてに開始・終了時刻と話者が入っています。時刻が失われていたのは、
「会議要約」へ渡すときにプレーンテキストへ平坦化していた箇所でした。
現在は JSON を渡し、コピーするテキストにも `[mm:ss]` が入ります。

> **先方の説明はもっともらしく聞こえましたが、手元のファイルと合いませんでした。**
> `sum(1 for x in segs if x.get("start_ms"))` の 1 行で「受け取っていない」と
> 「受け取った後で落とした」は区別できます。他人の診断に合わせて自分のコードを
> 変えてはいけません。

---

## [1.15.99] - 2026-09-22

### ボタンが反応しない — 進捗エリアが空の `<div>` でした

`JobProgress` はルート要素の中の `.job-reset` / `.job-bar-inner` / `.job-status`
に接続します。空の `<div>` では `root.querySelector('.job-reset')` が `null` を返し、
`null.addEventListener` で例外が発生して**インラインスクリプトの残りが実行されません**。
そのため「開始」ボタンにクリックハンドラーが付いていませんでした。
画面上は正常に見えます（アップロードは例外より前なので動作し、オプション欄も開きます）。
決め手はサーバーのログでした —— `POST /upload` はあり、`POST /start` は 1 件もなし。

**ブラウザー起動スイープが見逃した理由**は、このツールが音声サービス未設定時に
「先に設定してください」だけを描画し、スイープが使う使い捨てインスタンスは
常に未設定だからです。スイープはその設定を事前に投入するようにし
（投入が効いているかを確かめるガードも追加）、共有コンポーネントなしで
`JobProgress` を生成しているテンプレートを検出する静的ガードも足しました。

---

## [1.15.98] - 2026-09-22

### API キー欄の例示文が、最も誤解を招く場面でだけ表示されていました

`Authorization: Bearer <キー>` ヘッダーはこちらで組み立てるため、欄に必要なのは
キー本体だけです。ところが先方のドキュメントにはヘッダー全体が載っているので、
そこからコピーすると `Bearer ` まで一緒に貼り付けてしまいます。

先方の本番 API で実測しました。

| 送信したヘッダー | 結果 |
|---|---|
| `Bearer jtlw_…`（正しい形） | 200 |
| `Bearer Bearer jtlw_…` | **401「API キーが無効」** |
| `Bearer Authorization: Bearer jtlw_…` | **401**（同上） |

**メッセージが誤った方向を指します**。キーが無効だと言いますが、実際にはキーは
正しく、余分な接頭辞だけが問題でした。保存時に接頭辞を取り除くようにしました。
ただし `bearer` の直後に空白がある場合だけです。そうしないと、たまたまその文字で
始まる本物のキーを削ってしまい、やはり 401 になります。

例示文そのものも直しました。「空欄＝変更しない」は無条件の placeholder でしたが、
placeholder は欄が空のときにしか出ません。つまり**まだキーを保存していないとき**
だけ表示され、そのときは「変更しない」対象が存在しません。キーが保存済みのときに
だけ表示するようにし、未保存のときはキーの形を示したうえで、下に
「キー本体だけを貼る」ことと接頭辞を付けた場合の結果を明記しました。

### 話者数のヒントをさらに絞りました：「識別できるだけ発話した人数」

v1.15.97 で「出席人数」を「発話する人数」に変更しました。音声サービスが
**37 分すべて**の 7 人の中国語会議で再計測しました（以前の数値は 3 分の抜粋
によるもので、先方から訂正がありました）。

| 設定 | 話者の取り違え | 分離できた話者 |
|---|---:|---:|
| 指定しない | 22.90% | **3 / 7** |
| 7 人（正解） | **30.15%** | 7 / 7 |
| 5〜6 人 | **17.8%** | 5〜6 / 7 |

**結論は覆りませんでした**（正しい人数を入れても悪化します）。ただしこの表
だけを見ると誤った判断になります。7 人のうち 3 人は 28.6 秒、56.2 秒、94.8 秒
しか話しておらず、**声紋が足りない人のために無理にクラスタを作ると、
主要な話者のほうが分割されます**。

未入力にも代償があり、しかもそちらのほうが深刻です。指定しない場合、
**7 人中 4 人が出力にまったく現れず**、その人たちの発言はすべて別人の名前で
記録されます。引用の仕組みにとってこれは「数秒足りない」ではなく、
「決議が誤った人に結び付けられる」ということです。

そこでヒントは「**識別できるだけ発話した人数**」を尋ねる形にし、
主催者が判断できる目安（「ひと言ふた言しか話していない人は数えない」）を
添えました。**分からないときは 0 のままにしてください。**

### 用語：「運維」は大陸の言い方で、台湾では「維運」です

`OPS.md` の見出しと README の文書一覧の 2 か所が「日常運維」でした。
`AUTH.md` と 2 つのツールはすでに「維運」だったので、同じ製品の中に両方の
表記が並んでいたことになります。禁止語リストに追加しました。

### 依存パッケージ：anyio を 4.14.2 へ（重大 1 件ほか 2 件）

| アドバイザリ | 深刻度 | 内容 |
|---|---|---|
| GHSA-82r6-8w77-94w6 | **重大** | `TLSStream` がホスト名を IDNA 2003 で符号化するため、証明書の照合を回避されうる |
| GHSA-5p39-cfhj-2xmp | 中 | プロセスプールのワーカーが stderr を読み出さないと無限に停止する |
| GHSA-3w57-8xmc-8v26 | 高 | `run_process` が `extra_groups` を無視し、親のグループを保持しうる |

3 件とも 4.14.2 で修正済みです（こちらは 4.13.0 でした）。anyio は推移的な
依存（`starlette` / `httpx` / `watchfiles`）で、直接 import はしていません。

### 静的解析：ガードの正規表現 3 か所が指数時間でした

`(?:\\.|(?!\1).)*` の 2 つの分岐は**重なっています**。`\a` は `\\.` 一回とも
`.` 二回とも解釈でき、一致しない場合はすべての分割が試されます。

| 連続する `\a` | 修正前 | `[^\\]` に変更後 |
|---:|---:|---:|
| 18 | 128 ms | 0.01 ms |
| 22 | 1,056 ms | 0.01 ms |
| 24 | **5,393 ms** | 0.01 ms |

走査対象は自分たちのソースなので外部からは届きません。**ただし止まったガードは
壊れたガードと同じくらい原因が分かりません。** 3 か所とも修正し、この形が再び
コピーされないようガードを追加しました。

同じ回でもう 2 か所を堅牢化しました。紹介サイトの言語切り替えは同じディレクトリの
`.html` ファイル名だけを受け付けるようにし、名称変更済みツール URL の 308
リダイレクトはパスを再エンコードするようにしました。

---

## [1.15.97] - 2026-09-21

### 「出席人数」を尋ねるのは誤った設問でした

会議録音の文字起こしでは出席人数を尋ねており、説明にも「正確な人数を入力すると
精度が上がる」と書いていました。**これは計測によって否定されました。**

音声サービスが 7 人の中国語会議で計測したところ、7 人のうち 4 人は発話が
10 秒未満でした（最も少ない人は 1.5 秒の 1 文だけで、話者識別にはまったく
足りません）。正しい 7 を指定すると**話者の誤り率はかえって悪化**しました。
7 つに分けることを強制されると、システムは主要な話者の発話を
分割して数を合わせるしかありません。自動判定の 5 人のほうが妥当でした。

そこで項目は「**発言する人数**」を尋ねるように変更し、**分からなければ 0 の
ままに**と明記しました（誤ったヒントより自動判定のほうが概ね良好です）。

> **この 1 行は文字列の検査で固定しました**：元に戻してもテストは赤くならず、
> 結果だけが**系統的に悪化**し、利用者はその理由を知りません。
> `OPS.md` の IIS インストール順序と同じ系統で、手順どおりに進めた人だけが
> 踏みます。
>
> 音声ファイルのアドレスをリクエストのホストから推定しないのと同じ判断でも
> あります：**当然に見える自動化でも、方向を誤れば系統的に有害です。**
> 次の一手として「出席者リストから人数を自動入力する」は自然に見えますが、
> それは結果を悪化させる最適化でした。

## [1.15.96] - 2026-09-21

### 音声サービス設定ページ：まとめて修正

* 「音声ファイル」を指す中国語の表記が台湾の言い方ではありませんでした。
  39 か所を修正し、禁止語リストに追加しました。判定では**正しい長い表記を
  除外する**必要があります。そうしないと直したほうが検査に引っかかります
  （正しい形が誤った形を含む、以前と同じ罠です）。
* **処理プロファイルが自由入力でした**（`meeting.balanced` などのコード）。
  管理者が文字列を覚える必要があり、間違えても送信時まで分かりませんでした。
  **サービス側から取得した一覧**のドロップダウンに変更しました。こちらで
  一覧を持つことはしません（持つとずれます）。取得できない場合は現在の値を
  保持して理由を表示し、**空のドロップダウンにはなりません**
  （空だと「選べるものがない」ように見えてしまいます）。
* **有効化スイッチを独立させました** —— ツール全体がグレー表示になるかを
  決めるもので、その下の入力欄とは種類が違います。
* **正式名称とプロジェクトへのリンク**をタイトルの右に配置しました。
  略称だけではどのサービスを設定しているのか分かりません。
* **暗号化されていない http に警告を出します**：API キーはヘッダーを通り、
  録音ファイルも同じ経路を通ります。ループバックアドレスは対象外です
  （セキュアオリジンの例外であり、警告してもノイズになるだけです）。

* **「公開アドレス」が未入力のときは、現在接続しているアドレスを入力欄に
  補います**（どこから来た値かも明示）。補うのは**入力欄だけ**で、送信時は
  常に保存された値を使います。同じサービスに社内から直接つなぐ経路と
  リバースプロキシ経由の経路があるため、リクエストから推定すると公開名から
  入った利用者の送信が相手の許可リストで弾かれ、「一部の人だけ動く」という
  症状になります。
* 説明パネルにレイアウト用の指定が抜けており、スタイルが当たっていないように
  見えていました。

### 自己署名証明書：**証明書を貼る**のであって、検証を切るのではありません

社内サービスでは自己署名証明書がよく使われます。検証を切るスイッチは残して
あります（既定は有効）が、**正しい対処は相手の証明書を貼ること**です。
この接続にだけ影響し、他には影響しません。

保存時に **SHA-256 フィンガープリント**を計算して表示します。**管理者は相手が
公開している値と照合してから信頼する必要があります** —— 未照合の証明書を
貼るのは、「誰を信頼するか」を貼った人に委ねることになります。証明書でない
ものは保存時に拒否します（そうしないと送信時に ssl の内部エラーで失敗し、
設定ページの貼り間違いだと分かりません）。

> 検証を切る代償は画面に明記してあります：**誰でもサービスになりすませる
> ようになり、送信のたびに API キーを渡すことになります。**

## [1.15.95] - 2026-09-21

### 会議録音の文字起こし：進捗バーがまったく動かない

音声サービスの状態フィールドを 2 か所取り違えていました。

* `progress` は**数値ではなくオブジェクト**です（`percent`、
  `processed_audio_ms`、`total_audio_ms` を含みます）。数値として扱うと型判定が
  成立せず、バーは動きません。
* ステージは `progress.stage` にあり、**トップレベルに `stage` フィールドは
  存在しません**（トップレベルのものはエラーオブジェクト内にのみ現れ、どの
  段階で失敗したかを示します）。値は*ステージ名*（`fetch` / `normalize` /
  `asr` / `diarization` / `correction` / `finalize`）で、こちらが想定していた
  タスク名ではないため、表示は常に既定の「処理中」に落ちていました。

合わさると症状は**「何も起きていない」**です。ジョブは正常に動いているのに、
画面からはそれが分かりません。

> **自前のテストでは捕まえられませんでした**：擬似サーバーが返していたのは
> こちらが*想像した*形（`progress: 1.0` とトップレベルの `stage`）で、製品側と
> 互いに一致するため常にグリーンでした。前バージョンの「自分で組み立てた値を
> 自分で組み立てた期待値で検証する」と同じ系統です。**擬似サーバーは相手の
> ドキュメントの契約どおりに書くべきで、こちらの想像で書いてはいけません。**
> 修正後は擬似サーバーが先に数回「実行中」を返すようにし、これで初めて
> バーが動くかどうかを検証できます。

### 失敗メッセージを利用者が対処できる言葉に

エラーコードの一覧を受け取ったので、コードをそのまま出すのではなく 1 件ずつ
翻訳しました（`audio_too_long` は「録音が長すぎます。相手側の上限は 6 時間
です。先に分割してください」）。

**特に誤解を招くものが 1 つあります**：相手側の「ソースに到達できない」は
**URL の期限切れ**も含みますが、こちらが渡しているのは短期間だけ有効な署名付き
URL です。字義どおり「ファイルが見つかりません」と伝えると、利用者はまだ存在
するファイルを探しに行ってしまうため、署名付き URL に対する 404 は常に
「リンクの有効期限が切れました。待ち行列が長かった可能性が高いです」と
表示します。

## [1.15.94] - 2026-09-21

### 新ツール：会議録音の文字起こし（ツール総数 49 → 50）

音声・動画を入れると、**時刻と話者付き**の文字起こしが出てきます。完了後は
ワンクリックで「会議の要約」へ渡せます。認識・話者分離・句読点の校正は外部の
音声サービス側で実行し（GPU と音声の文脈が必要なため）、**要約・決定事項・
タスク・マインドマップ・翻訳はすべて本システムに残ります**。2 つのツールに
分けたのはそのためです。音声サービスを導入していない環境でも「会議の要約」は
そのまま使えます。

**設定が済むまではグレー表示です。** このツールが依存するのはローカルの
パッケージではなく*外部サービス*なので、機械を見ても使えるかどうかは分からず、
設定だけが判断材料になります。グレー表示のときは理由と設定場所を示し、
ページ自体も同じことを伝えます。見た目は正常なのに押すと失敗するアップロード
欄にしてはいけません。

グレー表示の理由もテンプレートから外しました。従来は理由が 1 つ
（「表示言語が合わない」）だけで、その文がテンプレート 2 か所に直接書かれて
いました。2 つ目の理由を足すと両方が誤った説明をすることになります。
理由はデータ側が持つようになりました。

### このバージョンで踏んだこと

* **`safe_remote_base_url()` は意図的にパスを捨てます** —— それが SSRF の
  バリアとしての動き方です（固定パスは呼び出し側が付ける）。最初の実装は
  管理者が入力したアドレスをそのまま使ったため、リクエストが `/api/v1/jobs`
  ではなく `/jobs` に飛び、404 が返るだけで原因が分かりませんでした。
  **実際に送ってみないと見えません** —— 単体テストは自分で組み立てた文字列を
  検証しているだけなので、常に一致します。固定の接頭辞は自分で付けるように
  しました。
* **管理ページの検査がパスの末尾だけを照合していました** —— `/admin/jtlw` は
  テスト計画に一度も出てきておらず、末尾の `jtlw` がたまたま一致していました。
  数えたところ管理ページ 52 件のうち **4 件**がこの状態で、うち 2 件は完全な
  パスの出現回数が 0 でした。同じ穴は v1.15.30 で API 側だけ塞がれていました。
  判定基準を完全なパスに変更しました。

### 「会議の要約」の立場判定は取りやめ

「この人はこの案に反対した」を誤ると、品質の問題ではなく人を傷つける誤りに
なります。しかも読み手には疑う理由がありません（決定事項のカードと見た目が
同じで、発言番号も付いています）。さらに**測れる基準がありません** ——
同じ発言を読んでも人によって違う立場を付けます。このツールの他の部分は
すべて、機能より先に基準がありました。

## [1.15.93] - 2026-09-21

### 短命の署名付き URL：認証情報を渡さずに、外部サービスに 1 つのファイルを取得させる

外部サービスの中には **URL しか受け付けない**ものがあります。ファイルをこちらから
アップロードするのではなく、こちらが渡したアドレスから相手が自分で取得します。

真っ先に思いつくのは API トークンを発行することですが、**その道は使えません**。
`api_tokens` にはスコープの概念がなく、1 つのトークンで `/api/*` のすべて
（ジョブ・通知・ワークスペース・全ツール）が開きます。ファイル 1 つを取得させる
ためにそれだけの権限を渡すのは、このプロジェクト自身の基準では許容できません。

署名付き URL は逆です。**渡すものが何もありません**。URL そのものが認可であり、
自動的に期限切れになり、失効処理も不要で、漏れた場合の影響範囲は
**そのファイル 1 つ、期限まで**に限られます。

3 つの判断：

* **有効期限は署名の中に入れる** —— ファイル ID だけに署名すると、
  `exp` を書き換えるだけで無期限に延長できてしまいます。
* **検証に失敗したものはすべて 404、403 ではない** —— 403 は「その ID は存在する」
  と教えることになり、ID こそ漏らしたくないものです。ID の形式違い・署名の誤り・
  期限切れ・ファイル不在の 4 つは、外から見て同一に見える必要があります。
* **URL は設定に書いたアドレスから組み立て、リクエストの Host は使わない** ——
  同じサービスには 3 通りの到達経路があります（内部ネットワークからの直接接続、
  リバースプロキシのドメイン、テスト機）。リクエストの Host から組むと、
  公開ドメイン経由で来た利用者のジョブがそのドメインを含むことになり、
  相手側の送信元許可リストに正しく弾かれます。症状は
  「一部の人だけ動かない」です。ガードはその関数のソースに
  `request` / `headers` / `url.hostname` が現れないことを確認します。

> この経路は**どのゲートも変更不要**でした（実測）。bearer のない GET は
> セッション認証にフォールバックし、`/api/` は公開プレフィックスに含まれます。
> CSRF は安全でないメソッドのみを対象とします。**POST はそうではありません** ——
> CSRF は pure-ASGI のミドルウェアでルーティングより前に動くため、
> トークンのない POST は存在しないパスも含めてすべて 403 になります。
> 2 つの経路のゲート状況はまったく異なり、片方の結論をもう片方に持ち込めません。

## [1.15.92] - 2026-09-21

### 会議まとめに 5 つ目の分類「事象と影響」を追加

外部レビューで、まとめがインシデントそのもの（何が起きたか、どこまで影響したか）
に触れていないと指摘されました。**これはモデルの限界ではなく、私たちの設計**です。
まとめは**検証済みの項目だけ**から書かれ、文字起こしを一切見ません。そして
「何が起きたか」は従来の 4 分類のどれでもないため、構造上あらわれ得ませんでした。

### どう追加するかで結果が大きく変わりました

| 方法 | 抽出率（4 分類） | 作り話率（4 分類） | 事象と影響 |
|---|---|---|---|
| ベースライン（4 分類） | 100% | **5%** | — |
| 5 分類を 1 回の呼び出しで | 88〜100% | **16〜19%** | 2 件（重複） |
| 5 分類＋明示的な振り分け規則 | 100% | **20%**（悪化） | — |
| 独立した 1 パス | 100% | 5〜6% | 19 件（半数が進捗報告） |
| **独立パス＋機械的な境界** | **100%** | **6%** | **7 件、すべて妥当** |

**規則を足す → 出力が増える → ノイズが増える。** 項目を 1 つずつ見て理由が
分かりました。モデルは確かに事実に注目し始めたものの、**それを決定事項に
分類していた**のです（「API の rate limit は毎分 60 回」
「保守費は先方が年 32 万円を提示」）。問題は境界の説明不足ではなく、
**1 回の呼び出しで 5 分類を判断すること**そのものでした。

そこで「事象と影響」は**独立した 1 パス**にしました。専用のプロンプトと専用の
再確認を持ち、4 分類側のプロンプトと規則は**一字も変えていません**。
**ゼロ劣化は調整の結果ではなく、構造によって保証されています。**

コストも実測しました。抽出の呼び出し回数が倍になります（160 分の会議で
29 → 61 回、147 → 303 秒）。そのためスイッチを用意し、**トレードオフを画面に
明記**しました —— インシデントや状況共有の会議ではオンのまま、純粋な計画会議では
オフにすると分析時間がおよそ半分になります。

### 境界は語感ではなく機械的に判定できる必要があります

最初の版は「個人の進捗報告は含めない」と書きました。**モデルには区別できません**。
「設定ファイルは送付済みです」は確かに既に起きたことだからです。
2 つの機械的な規則に変更しました。

* 「完了した／送付した／確認した」および「詰まっている／待ち／まだ」は**書かない**。
* 各項目は**何に影響したか**か**具体的な数値**のいずれかを述べられること。
  どちらも述べられないなら書かない。

> **⚠ 穴を塞ぐ過程で別の穴を開けました**：規則を書き直した際、
> 「権限が下りず詰まっている」という例を段落ごと置き換えてしまい、
> そのクラスがまとめて再出現しました（4 件、重複あり）。
> **1 つの穴を塞ぐために別の穴を開けないこと** ——
> 戻した規則は置き換えではなく**併存**させています。

> **最初のガードには歯がありませんでした**：`analyse` には分類ごとのループが
> 2 箇所あり、私は `MAIN_KINDS` がどこかに現れることしか確認していませんでした。
> 片方だけ `KINDS` に戻しても緑のままです。判定基準は
> 「**どのループも `KINDS` を使ってはならない**」に変更しました。

## [1.15.91] - 2026-09-20

### 修正：マインドマップのノード文字が途中で切れ、分析が内容を失ったように見えていた

ノードのラベルは `text[:60]` —— **60 文字で、しるしは一切なし**でした。画面には
「…環境変数（Environment Variable）に L」「…全部 de」と表示される一方、
**カードと文字起こしには最初から全文がありました**。

利用者には「表示のために短くした」のか「分析が後ろを失った」のかが区別できず、
この 2 つは深刻度がまったく違います。

修正は 3 点です。**省略記号**を付けて短縮を見えるようにすること、
**ラテン文字の識別子を途中で切らない**こと（`LOG4J_FORMAT…`、`deployment.yaml`、
`X-Forwarded-Proto` はそれぞれ 1 語として扱う）、そして
**ツールチップに全文を出す**こと（従来は同じ切れた文字列を表示していました）。

### 60 文字という上限は、それに届かないコーパスから決めていた

過去の実行から得た **868 件**の項目：中央値 **18** 文字、**最長 56** ——
**上限に一度も達していません**。つまりこの切り詰めは、私が取ったどの計測にも
現れませんでした。

実際の委員会の文字起こしで実行（54 件）：**中央値 39、p90 = 101、最長 379**。

| 上限 | 全文表示 |
|---:|---:|
| 60（従来） | **77%** —— およそ 4 件に 1 件が切れる |
| 90 | 87% |
| **120（現在）** | **94%** |
| 160 | 98% |

> **コーパスは大きくても形が違えば届きません。** 評価用コーパスは合成した短い
> 会議で、実際の項目は英語の用語や括弧書きを伴います。
> 「合成サンプルでは届かない種類」と同じ教訓です。

> **ガードは、守る対象のコードと定義を共有してはいけません**：最初の版は
> 「識別子を途中で切ったか」の確認に製品側の `_is_token_char` を使っており、
> その定義を空にする変異を**まったく検出できませんでした** ——
> 判定基準が変異と一緒に動き、常に自己整合してしまうためです。
> テストは自前の定義を持つようにしました。
>
> 同じテストの最初の版は `原文.startswith(残した部分)` も使っており、これも
> **歯が立ちません**：`LOG4J` は `LOG4J_FORMAT…` の接頭辞なので、
> **まさに壊れた切り方が正しいと判定されます**。

## [1.15.90] - 2026-09-20

### 修正：会議まとめの「発言時間」は半分が推定値なのに、画面がそう言っていなかった

字幕ファイル（.vtt / .srt）と JSON は**各キューが自分の終了時刻**を持っています
—— それは実測値です。プレーンテキストの文字起こしには各人が話し「始めた」時刻
しかないため、終了時刻は**次のセグメントの開始**から借りており、算出される
発言時間には**間の沈黙も含まれます**。

画面上は両者がまったく同じに見えます。推定のほうは、列見出しを
「発言時間（推定）」に変え、表の下にその値の算出方法を 1 行添えました。

> **判定はサーバー側が行います**（どのパーサーが動いたか）。フロントエンドが
> セグメントから推測してはいけません ——「各終了時刻が次の開始と一致する」は
> 本物の字幕ファイルでも成り立ちうるため、推測した基準は一部のファイルで
> 嘘をつきます。
>
> ガードはまず**前提が成り立つことを証明します**（プレーンテキストが実際に
> 次の開始を借りていること）。パーサーが補完をやめた場合、上記の期待値は嘘に
> なりますが、それでもテストは緑のままになってしまいます。

### 空のカードが会議について結論を出さないように

「この会議にはこの種の内容がありませんでした」を「**分析では**この種の内容が
**文字起こしの中に見つかりませんでした**」に変更しました。実測した抽出率は
94〜100% であって 100% の保証ではありません。前者は会議について結論を出しており、
後者は私たちが実際に知っていることだけを述べています。

## [1.15.89] - 2026-09-20

### 修正：提出前チェックの行が「マイジョブ」で何も押せなかった

あの行には出口が 2 つしかありません。**ダウンロード**（`result_path`）と
**開く**（`view_url`）です。提出前チェックが作るのはファイルではなく
**案件の下に置かれるレポート**なので `result_path` がないのは当然ですが、
`view_url` もありませんでした。そのため行には「完了」と出るのに何も押せず、
利用者は自分でツールに戻って案件を探す必要がありました。

案件ページは元から案件 ID でアドレス付けされ、進捗も自前でポーリングし、
アクセス確認も持っています。したがってそこを指すだけで十分で、
**`?job=` による復元は不要**でした。

**バックグラウンドジョブを投げる 29 ツールすべてを調査しました**。実際に
「開く」を提供しているのは 3 つだけ（逐文翻訳・文書翻訳・会議まとめ）で、
いずれも正しく復元します。先に修正した「開くと空白ページになる」不具合は
他のツールには存在しません。出口が 2 つともないのはこのツールだけでした。

> **⚠ 最初の調査は誤りでした。** `"view_url" in ソース` で探したところ、
> **`preview_url` が `view_url` を含む**ため、プレビュー画像を表示するだけの
> 7 ツールが「開くあり」と判定され、うち 3 つを一時は不具合と勘違いしました。
> またも部分文字列一致です（このプロジェクトは前方一致・部分一致で何度も
> 痛い目に遭っています）。ガードは AST ベースに変更し、
> **`preview_url` を `view_url` と取り違えないことだけを確認するテスト**も
> 追加しました。

> **「走査が本当に何かに届いているか」の確認にも助けられました**：最初の AST
> 照合は `_jm.job_manager.submit`（`Attribute`）しか認識せず、多くのツールは
> `from … import job_manager` して直接呼ぶ（`Name`）ため、
> **29 ツール中 3 つにしか届いていませんでした**。この確認がなければ、
> ガードは静かに 3 ツールだけを調べて緑のままだったはずです。

> **判定基準は「マイジョブ」が実際に受け取るデータに置く必要があります**：
> `view_url` は `submit()` の**後**に設定され、一覧との間にデータベース書き込みが
> 挟まります。そこで静的なガードに加えて、実際にジョブを投入し、データベースに
> 記録されるのを待ち、一覧側から読めることを確認するテストも用意しました。

## [1.15.88] - 2026-09-20

### マイワークスペースがプレーンテキスト（.txt / .md）に対応

会議まとめの文字起こしや、リスト処理の出力といったプレーンテキストは、
これまでワークスペースに保存できませんでした。

**判定基準はファイル名ではなく中身です。** ワークスペースが受け付ける他の
形式にはいずれも明確な手がかりがあります（PDF / PNG はマジックバイト、
オフィス文書は zip を開いて内部構造を確認）。プレーンテキストにはどちらも
ありません。拡張子で受け付ければ、`.txt` に改名するだけで何でも通ってしまい、
型チェックはまさにそれを防ぐために存在しています。判定基準は
「ファイル全体が UTF-8 として解釈でき、タブ・CR・LF 以外の制御文字を含まない」
とし、ファイル名は **.txt と .md のどちらかを選ぶためだけ**に使います。
`.txt` に改名した PNG は、やはり PNG として検出されます。

**先頭数 KB のサンプリングではなく、ファイル全体を確認します** ——
先頭 8 KB がきれいで後半がバイナリというファイルは簡単に作れ、
サンプリングではそのまま通ってしまいます。

> プレーンテキストには描画できる「1 ページ目」がないため、サムネイルは空の
> プレースホルダーになります。ただしそれを明示する必要があります。
> 「PDF として扱う」経路に落ちると存在しない `file.pdf` を探しに行き、
> エラーが「ファイルが存在しません」となって、利用者のファイルが消えたように
> 見えてしまいます。

### 修正：同じ一覧がまた二重に書かれていた —— アップロードの accept 属性

`my_workspace.html` のファイル選択欄は 2 箇所で
`application/pdf,image/png,.docx,…` を直書きしていましたが、サーバー側の一覧は
v1.14.6 で表計算とプレゼンテーションが追加されていました。症状は
**サーバーなら受け付けるファイルをファイル選択欄が除外してしまう**ことです。
利用者には「このファイルは送れない」としか見えず、手がかりになるエラーも出ません。

一覧はサーバー側で算出してテンプレートに渡すようにしました
（`data-ws-exts` と同じやり方です）。ページ上の「PDF / PNG」という文言も、
今後ずれないよう書き換えました。

## [1.15.87] - 2026-09-20

### 修正：変換時のマクロ無効化設定が一度も効いていなかった

変換が扱うのは利用者がアップロードした信頼できないファイルなので、毎回使い捨ての
プロファイルに `DisableMacrosExecution` を書き込んでいました。実測したところ、
どの変換関数にも `--safe-mode` が付いており、このフラグは起動時に
**ユーザープロファイルをリセットします** —— 実行が終わった時点でその設定は
消えていました。

古いコメントには「`--safe-mode` はマクロとは無関係」とありました。この文自体は
正しく、見落としていたのは**こちらが書き込んだ設定とは大いに関係がある**という点です。
まるごと消し去ります。

> 判定基準は「実行後に設定が残っているか」であって「書き出したか」ではありません。
> ファイルは毎回書き出されていたので、書き込みだけを確認していては永久に見つかりません。

深刻度を正確に書きます。LibreOffice の出荷時のマクロセキュリティは「高」で、
ヘッドレス変換はマクロを自動実行しません。したがってこれは
**効いていなかった多層防御の一層**であり、悪用可能な脆弱性ではありません。

`--safe-mode` が防いでいた 2 点は既に別の手段で担保されています。ユーザー設定
（呼び出しごとに新しい使い捨てプロファイル）と、クラッシュ復旧ダイアログ
（`--norestore`）です。外したうえで実ファイル 3 件（docx / xlsx）を比較し、
ページ数・用紙サイズ・文字数・ページごとの描画ピクセルハッシュがすべて一致しました。

### 修正：書き出した PDF が A4 ではなく Letter だった

台湾で印刷するのは A4 です。Letter は 18mm 短く 6mm 広いため、A4 前提で組んだ
内容がずれます。影響を受けるのは**元ファイルが用紙サイズを持たない**変換すべて
です（Markdown を文書ファイルに、会議まとめの書き出し、プレーンテキスト、CSV）。

* **CSS の `@page { size: A4 }` は** LibreOffice の HTML 取り込みでは**無視されます**。
* **`LANG` や `LC_PAPER` も効きません** —— `LANG=zh_TW.UTF-8` は長さの単位
  （in → cm）が変わるだけで用紙は Letter のまま。そもそも多くのサーバーには
  zh_TW ロケールが生成されていません。

有効なのは使い捨てプロファイルに `ooSetupSystemLocale` を書くことです。
**ただし無条件に適用してはいけません**。これはシステムロケールでもあるため、
CJK のフォントフォールバックが変わります。実際の取引先フォーム（docx）では、
先頭の空白が別のフォントで幅を測られ、ラベル全体が**約 10pt 左にずれました**
（インク量も字形も完全に同一で、変わったのは空白の幅だけです）。
フォーム自動入力のツール群は座標のずれに極めて敏感で、しかもその元ファイルは
最初から用紙サイズを持っているので、この設定は不要です。

したがって判断基準は**元ファイルが用紙サイズを宣言しているか**です。宣言していない
もの（HTML・プレーンテキスト・CSV）だけこちらで決め、宣言しているものはそれに従います
（Letter を宣言した .odt は Letter のまま、実際の docx はページごとの
ピクセルハッシュが変更前と完全に同一でした）。

## [1.15.86] - 2026-09-20

### 修正：台湾の言い回しではない語を置き換え

`估計` は大陸寄りの表現です。台湾では `預估`（予想読了時間）、
`推估`（他の数値から算出したもの）、`約`（約 800 MB）を使います。
禁止語リストに追加し、対象は**利用者に見える文字**に限定しました
（コメント内の `背景估計` は技術用語なので対象外）。

追加したとたんに 2 箇所見つかりました。文字数カウントの読了時間のラベル
（**すぐ隣の欄は正しい表記だった**ので、同じ画面に 2 通りが並んでいました）と、
README のメモリ測定についての一文です。ジョブ一覧の `估 800 MB` も
`約 800 MB` にしました —— **英語訳は元々 "about"** で、中国語側だけが例外でした。

> 禁止語を追加するときは、**既にコードにある同義の表記も同じ回で一括して見る**こと。
> さもないと画面の半分が新しく半分が古いままになり、何もエラーになりません。

## [1.15.85] - 2026-09-20

### 修正：.odt / .docx の書き出しで見出しが白文字になり見えない

ビジネスレポートのテーマは見出しを「白文字＋濃色の帯」で描きますが、
Office エンジンの HTML 取り込みは**文字色を保持しつつ段落の背景色を落とす**ため、
白地に白文字となり行ごと消えていました。PDF では発生しません（背景を描けるため）。

**「Markdown を文書ファイルに」も同じ不具合**でしたが報告がありませんでした。
両ツールで同じ上書きを共用し、文書形式では濃色文字＋下罫線に、
PDF では従来の帯のままにしました。

> 原則：**可読性を背景色に依存させない**。背景は変換で最初に失われるもので、
> 失われたときの症状は「色がおかしい」ではなく「何も見えない」です。

### 会議の要約：貼り付けた文字起こしの表題を会議の背景から取得

貼り付け時のファイル名はこちらで付けた「貼り付けた文字起こし.txt」のため、
書き出した文書の表題が内部的な表現のままになっていました。

優先順位を「**会議の背景に明記された主題 → ファイル名 → 単に「会議記録」**」に
変更しました。ダウンロード時のファイル名も同じ情報源です。

**要約から主題を推測はしません**：要約は一続きの文であり、先頭を切り出すと
文の途中で切れ、実行のたびに表現も変わります。背景欄が「会議主題：…」を
書く場所です。

### その他

- 会議の要約の書き出し欄から「Markdown を文書ファイルに」への引き渡しボタンを
  削除しました（結果欄に Word と ODF があります）
- LLM 設定の会議の要約の説明を、gemma4:26b 以上の推奨と、
  ビデオメモリが限られる場合の qwen3.8:27b に更新しました

## [1.15.84] - 2026-09-19

### 会議の要約：発言の分布の帯をクリックすると、その時刻の発言へ移動します

従来は行全体で 1 つの段落番号（その人の最初の発言）を共有していたため、
どの帯を押しても同じ場所に移動していました。読み手が見ているのは特定の時刻の
帯です。各帯が自分の段落番号を持つようにしました。空白部分をクリックした
場合は従来どおり行の最初へ移動します。カーソルを合わせるとその帯の時刻と
段落番号が表示されます。

### 会議の要約：議題のタイムラインで最長の行の数値が折り返さないように

バーがテキストと幅を奪い合っていたため、最長の行ではバーが場所を占め、
割合と時間が 2 行に折り返していました。バーを固定幅のトラック内に描くように
し、テキストの場所が常に確保されるようにしました。各行の比率も同じトラックを
基準に測られ、ラベルの長さに左右されません。

## [1.15.83] - 2026-09-19

### 会議の要約：Word と ODF への書き出しとレイアウトテーマの選択に対応

PDF に加えて **Word（.docx）** と **ODF（.odt）** で書き出せるようになりました。
そのまま編集したり、社内テンプレートに流し込んだりできます。

ダウンロード欄に**レイアウトテーマ**の選択を追加しました（クリーン／GitHub 風／
学術論文／書籍／ビジネスレポート／ミニマルの 6 種類。既定はビジネスレポート）。

3 つの形式も 6 つのテーマも「Markdown を文書ファイルに」の仕組みをそのまま
使っています。レンダラーもテーマ定義も共用です。ここで別のレイアウト処理を
書けば既存のものより劣りますし、あちらにテーマが増えてもここには反映されません。

## [1.15.82] - 2026-09-19

### 修正：書き出したグラフが画面に追従していなかった

前版で話者の棒グラフを表に統合し「発言の分布」に置き換えましたが、
**書き出しはサーバー側で描画した別のグラフを使っています**。そのため画面は
分布、PDF は旧来の棒グラフのままで、話者不明も `unknown` 表示でした。
**同じものを二箇所に書けば必ずずれます**。今回は「画面」と「書き出し」の
間でずれました。

サーバー側も「誰がいつ話したか」を描くようにし（1 人 1 行、話している箇所に
帯）、書き出しの表の並び順と列名も画面に合わせました。話者不明も符号では
表示しません。解析結果のみで発言データがない場合（公開 API）は従来の棒グラフに
フォールバックします。

### 会議の要約：引用は段落番号のみ表示

カードが狭く、1 項目に 3〜4 個の引用が付くこともあります。「第 10 段」の
3 文字のうち 2 文字はすべてのチップで同じ装飾です。完全な表記はツールチップと
スクリーンリーダー用ラベルに残しています。

## [1.15.81] - 2026-09-19

### 会議の要約：議題のタイムラインで、長さのバーを見出しに寄せました

見出しとその下のバーは一体のものですが、間が空いていて 2 行に見えていました。
間隔の原因は 2 箇所 —— 行の行高（本文用の 1.5 で、見出しの下に 3〜4 ピクセル
余白が残る）とバー自身の上マージンで、**片方だけ直しても足りません**。

## [1.15.80] - 2026-09-19

### 会議の要約：書き出した PDF でグラフが版面からはみ出さないように

画像が元のピクセル寸法で配置されており、議論の構造は幅 980px のため
**右側が切れていました**。`max-width: 100%` は効きません
（Office エンジンの HTML 取り込みはこの CSS を無視します。実測でも 980px の
まま）。HTML の `width` 属性を使うようにしました。

**幅の制限だけでは不十分**です。ページ幅に縮めても議論の構造は 1 ページより
高く、エンジンは画像を分割しません —— 配置して、はみ出した分は切り捨てます
（実測で画像の下端がページ下端より 57pt 下）。背の高いグラフは 1 ページに
収まるよう分割するようにしました。

### 会議の要約：グラフの見出しが二重に出ないように

グラフ自体が表題を描いているのに、周囲の Markdown が同じ文言の見出しを
追加していました。

## [1.15.79] - 2026-09-19

### 修正：会議の要約の PDF 書き出しは一度も成功していなかった

「画面はきれいなのに、書き出した PDF がひどい」との報告。出力はグラフ 3 ページ
のみで**本文が一文字もなく**、ページサイズも 980×2640 ——
これはグラフの原寸であり、紙のサイズではありません。

変換ヘルパーは第 2 引数が**出力先ファイル**で、戻り値は `None` です。
旧コードはディレクトリを渡し、戻り値を生成ファイルとして扱っていたため、
その判定は**一度も成立せず**、毎回グラフのみの版にフォールバックしていました。
`None` を返すのは例外ではないため `except` も発火せず、
**ログに一行も残らず、ダウンロードは常に 200** でした。

また Markdown をそのまま Office エンジンに渡していましたが、
**Markdown は読めません**。先に HTML へ変換するようにし
（「Markdown を文書ファイルに」と同じレンダラーを共用）、
表題・要約・決議・タスク・リスク・議題・話者表を備えた文書を出力します。

**フォールバック時は必ず警告を残す**ようにしました。グラフのみの PDF と
完全な PDF は、利用者から見ればどちらも「ファイルがダウンロードされた」です。

### 会議の要約：文書の表題に拡張子が付かないように

### 議題のタイムライン：「章」を「議題」に変更

「章」は動画や書籍から借りた言い方で、会議について誰もそうは言いません。
カードを「議題のタイムライン」、その下のグラフを「議題ごとの所要時間」に
変更しました。

そのグラフの見出しは**一度も翻訳されていませんでした**。
`tr(条件 ? 'A' : 'B')` と書かれており、キーが実行時に決まるため抽出ツールからは
見えません。見出しを HTML へ移し、分岐ごとに翻訳するようにしました
（セクションのアイコンもそこに置けます）。

## [1.15.78] - 2026-09-19

### 修正：「マイジョブ」から会議の要約を開いても中身が空だった

URL にジョブ ID を付けて戻ると、ジョブは既に完了しているため進捗バーは
「完了」と表示されるのに、**その下が空**で、結果が消えたように見えていました。

結果の読み込みにはアップロード ID が必要ですが、これはアップロード時にしか
存在せず、ページを開き直すと空のため、その処理が**黙って何もしていません**
でした。ジョブ自身がその ID を保持しているので、先に取得してから結果を
読むようにしました。

### 会議の要約：発言の分布を表に統合

従来は左にグラフ、右に表を並べており、行ごとに揃える必要がありました
（順序・行高・開始位置のすべて）。行高はフォントとブラウザに依存するため、
計測して揃えても数ピクセルずれ、その「わずかなずれ」こそが揃っていない
状態そのものでした。

統合により構造で揃うようになりました。分布の列は百分率で配置するため、
列幅の変化に自動で追随します。行をクリックすると、その人が最初に発言した
箇所へ移動します。

## [1.15.77] - 2026-09-19

### 会議の要約：話者のグラフを「誰がいつ話したか」に変更

従来は「誰が最も話したか」の棒グラフでしたが、それは**隣の表に既に
書いてあります**。同じ数値を別の形で描き直しているだけでした。

1 人 1 行のタイムラインに変更しました。話している箇所に色の帯が入ります。
表では分からない 3 点が一目で分かります。**誰が主導したか**（その行の帯の量。
割合も読み取れます）、**リズム**（全体に散らばっているか、一箇所に集中して
いるか）、**どの区間に登場しなかったか**（空白部分）。

タイムスタンプがなくても描けます。横軸が文字起こしの順序になるだけで、
会議の進行であることは変わりません。どちらを使っているかはグラフ上に
表示されます。

### 会議の要約：グラフ右側の数値を桁揃えに

割合・時間・回数が 1 つの右寄せ文字列だったため、最初の列が最後の列の幅に
押されていました（`9.0%` と `13.8%` は 1 文字違うため列全体がずれる）。
それぞれを独立した右寄せの列にし、等幅数字にしました。

### 会議の要約：背景欄にスタイルを適用

幅・高さ・文字サイズしか指定がなく、枠線・角丸・余白がないため
ブラウザ既定の入力欄のように見えていました。

### 会議の要約：話者不明を `unknown` と表示しないように

`unknown` は解析時に入れる符号であり名前ではありません。データ上はそのまま
（統計・引用・書き出しがこれを参照）とし、画面では「話者不明」と表示します。

## [1.15.76] - 2026-09-19

### 会議の要約：解析後の統計 4 項目を等幅で横いっぱいに

幅が内容に合わせて変わるため 4 つの大きさが揃わず、左に寄って下の全幅の
項目と揃っていませんでした。等幅のグリッドで横いっぱいに並べ、
幅が足りなければ折り返すようにしました。

### 会議の要約：レイアウトの注記が二重括弧にならないように

レイアウト名自体に括弧が含まれるため、外側を括弧で囲むと二重になっていました。
「自動判定：…」と表示するようにしました。

## [1.15.75] - 2026-09-19

### 会議の要約：話者の表に罫線を追加

1 行に 5 つの数値があるにもかかわらず、ごく薄い下罫線のみで縦線がなく、
横方向に目で追えませんでした。横罫・縦罫・交互の背景色を追加し、
カーソルを合わせた行を強調するようにしました。

### 会議の要約：表の「割合」を「文字数の割合」に変更

隣のグラフは**発言時間**に基づくのに対し、表の割合は一貫して**文字数**に
基づいていました。同じ人物の 23.1% と 21.8% が並ぶと、どちらかが誤りの
ように見えます。見出しでどちらかが分かるようにしました。

## [1.15.74] - 2026-09-19

### 修正：会議の要約のダウンロードがサイト全体を止めていた

PDF／グラフ PNG／ZIP は**ボタンを押してから生成**します（グラフのラスタライズ、
圧縮）。その CPU 処理が Web サーバーのイベントループ上で直接動いていたため、
誰かがダウンロードしている間は**他の人はトップページすら開けません**でした。

ワーカースレッドに移しました。既存の「エンドポイントはイベントループを塞がない」
という静的チェックはこの形を検出できないため（重い処理が呼び出し先の関数の中に
ある）、このエンドポイントはそのテストの明示リストに登録しました。

### 会議の要約：ダウンロードボタンに「生成中…」を表示

以前は単なるリンクだったため、ファイルが届くまで画面は何も変わらず、
反応がないように見えて何度も押されていました。押した時点で「生成中…」と
スピナーに切り替わり、完了までの連打を防ぎます。

ファイルは自前で取得するようになったため、サーバー側のエラーはメッセージとして
表示されます（以前はエラーページがファイルとして保存されていました）。

### 会議の要約：章のタイムラインの軸線が途切れていた

軸線は行ごとに描画され、行の**コンテンツボックス**までしか伸びないため、
行の上下パディング分が行間で空いていました。はみ出し量をパディングと同じ変数に
結び付けたので、片方を変えてももう片方が壊れません。

## [1.15.73] - 2026-09-19

### 会議の要約：アップロードカードの順序を実際の流れに合わせました

「会議の背景」が送信ボタンの**下**にあったため、押してから気付く順序に
なっていました。「文字起こしを貼り付け → 背景 → ボタン」の順に改め、
ボタンを最後に置きました。

ボタン名も「このテキストを使う」から「文字起こしを解析」に変更しました。
実際に行うのは段落と話者への解析であり（押すと段落数・話者数と冒頭数行が
表示されます）、ラベルはそれを表すべきだからです。

## [1.15.72] - 2026-09-19

### 会議の要約：「会議の背景」が独立したブロックに見えていた

この領域はもともと「1. 文字起こしのアップロード」カードの中にありましたが、
枠線と背景色を持っていたため、**カードの中のもう一枚のカード**のように
見えていました。枠を外し、区切り線のみにしました。「文字起こしを直接貼り付け」
と同じ視覚的な重みになり、同じカードの一節であることが分かります。

## [1.15.71] - 2026-09-19

### 会議の要約：「会議の背景」を記入できるようになりました

アップロード欄の下に任意の入力欄を追加しました（既定では折りたたみ）。
**文字起こし以外の情報** —— 主題、日時と場所、出席者と役職、専門用語の説明、
その他伝えたいことを記入できます。

記入すると分析の精度が上がります。誰が管理職で誰が担当かが分かれば
「○○さんにお願い」から担当者を判別でき、システムの略称や符丁が分かれば
文面が崩れません。

**背景が項目の出所になることはありません。** そこに書かれた事柄は会議では
起きていないため、そこから決議が生まれてしまうと「すべての項目が文字起こしに
辿れる」という本ツールの保証が —— しかも気付かれない形で —— 崩れます。
三重の防御を入れています。

1. プロンプトで明示し、**利用者のテキストの前後を当方のルールで挟みます**。
   記入内容が指示のように読める可能性があるため、最後に述べるのは当方です。
2. **引用検証**：項目は文字起こしの段落を指していなければならず、背景は
   比較対象に含まれません。
3. **背景と非常に似ており、かつ文字起こしより明らかに似ているものは破棄します。**

3 番目はテストを書く過程で追加しました。2 番目だけでは不十分だったためです。
背景に書かれる内容（議事次第、議題一覧）は会議で扱う事柄そのものなので、
議事次第から写した決議でも文字起こしとの重なりが閾値を超えてしまいます。

> 背景と文字起こしの言い回しが大きく異なり、モデルが両者を混ぜて作った文は
> この仕組みでは捕捉できません。背景は依然として注意の要る入力です。

公開 API でも受け付けます（`context`、最大 4000 文字）。

## [1.15.70] - 2026-09-19

### 会議の要約：グラフとレイアウトの見直し

- **章をタイムライン表示に**：左に軸線、章ごとに点を置き、見出しの横に
  長さのバーを描きます。従来の「時刻 ｜ 見出し」の 2 列表は右側が大きく
  空いていました。同じデータで「どこが一番長かったか」も分かります。
- **バーにカーソルを合わせると対応する凡例が強調され、他は淡く**なります。
- **話者グラフと表を左右に並べ**、幅が足りなければ縦に折り返します。
- **アップロード後の統計を色付きのブロック**にしました（段落／話者／文字数／全長）。
- 凡例の文字を 1 段階大きくしました。

### 修正：グラフの文字が大きすぎたり小さすぎたりする

同日に両方向の報告をいただきましたが、**原因は同じ**です。グラフを描いた幅と
画面上で実際に占める幅が一致せず、CSS がグラフ全体を（文字ごと）拡大縮小して
いました。

大きすぎたのは、議論の構造の領域が**幅を測る時点でまだ非表示**だったため
（0 と測られ既定値に落ち、1.7 倍に引き伸ばされた）、小さすぎたのはその逆です。

グラフは実ピクセルで描画し、**描画後にもう一度測って**実際の幅と 2% 以上
ずれていれば実寸で描き直すようにしました。測り損なう経路は多いため
（領域の展開中、フォント読み込み中、スクロールバーの数ピクセル）、
individually 塞ぐより描いた後に鏡を見るほうが確実です。

### 修正：議論の構造の右側の項目にカーソルを合わせても反応しない

左の章は強調されるのに、右の項目は変化しませんでした。それらの枠は背景が白で、
効果が不透明度の変更だったため、**白い画面上では違いが分かりません**でした。
明度の変更に改め、白い枠でも色付きのバーでも分かるようにしました。

### 修正：進捗バーに余分な「.json をダウンロード」ボタン

会議の要約の結果欄には既に一連のダウンロードボタン（PDF／Markdown／グラフ
PNG／ZIP／JSON／引き渡し）があるのに、共通の進捗バーがもう一つ
「.json をダウンロード」を表示しており、別物のように見えていました。
ツール側で共通進捗バーのそのボタンを非表示にできるようにしました。

また、進捗バーのボタンと下のカードの間に余白を入れました
（バックグラウンド処理を持つ全ツールに適用されます）。

## [1.15.69] - 2026-09-19

### 修正：「停止」を押しても止まったように見えない

停止を押すと時間は止まりますが、**進捗バーはその位置のまま、状態表示は
「抽出中 4/5」のまま、「このページを閉じても大丈夫です」も出たまま**で、
処理が固まっているのと見分けが付きませんでした。

停止処理は「ポーリングの停止」と「サーバーへの通知」だけを行っており、
**「停止しました」と描画するコードはポーリングの経路にあった**ため
（サーバーが取消済みと返したときに実行される）、ポーリングを止めた時点で
決して実行されなくなっていました。

押した時点で状態を「停止しました」にし、バーをグレーにし、
「このページを閉じても大丈夫です」を取り下げるようにしました。
**経過時間は残します** —— どれだけ実行してから止めたかは有用な情報です。
共通の進捗バーなので、バックグラウンド処理を持つすべてのツールに反映されます。

### 修正：会議の要約の貼り付け欄で、ヒント文に `&#10;` がそのまま表示される

「文字起こしを直接貼り付け」欄のヒント文に、改行ではなく `&#10;` という
文字列がそのまま表示されていました。

ヒント文は翻訳関数を通っており、**翻訳結果は自動エスケープされます** ——
`&` が `&amp;` に変換されるため、ブラウザにはその 6 文字が表示されます。
三つの言語カタログが同じ書き方をそのまま写していたため、三言語とも同じ
症状でした。

## [1.15.68] - 2026-09-19

### 会議の要約：グラフをブラウザ側で描画し、クリックできるようにしました

三つのグラフ（議論の構造、章ごとの割合、話者ごとの割合）はこれまでサーバーで
生成した画像だったため、**中の要素をクリックできませんでした**。
このツールの要は「どの項目も文字起こしに辿れること」ですので、
ブラウザ側で描画するように変更しました。

- **グラフのどこかをクリックすると、文字起こしの該当箇所へ移動して強調表示**
  されます（引用ボタンと同じ仕組みです）
- コンテナの幅に合わせて描画し、ウィンドウのサイズ変更で描き直します。
  横に広い画面でも右側が大きく空きません
- 書き出す PNG / PDF と Markdown に埋め込まれる画像は引き続きサーバー生成です

### 会議の要約：議論の構造の右半分が空白にならないようにしました

決議とタスクは会議の後半に集中するため、十の章のうち三つにしか項目が
付かないことが多く、残り七行の右半分が空白になり、グラフが壊れているように
見えていました。それらの章は**実際に成果物がなかった**ので、隠したり
ノードを作り出したりせず、全幅の帯として描き右側に「決議・タスクなし」と
記すようにしました。

### 修正：タイムスタンプがあるのに「ない」と表示される

プレーンテキストの文字起こしでは時刻は行頭の `16:19` のような表記で、
**すべての行にあるわけではありません**（冒頭の見出し、「（短い沈黙）」の
ような行、最後の段落には付きません）。章の時間を「最初の段落の開始」と
「最後の段落の終了」で固定的に求めていたため、章の先頭か末尾がそうした行に
当たると章全体が時刻なしとなり、**一つでも時刻のない章があるとグラフ全体が
段落数にフォールバック**していました。

実際に時刻が付いている段落の中から最早・最遅を取るように変更しました。

### 会議の要約：文字起こしを全展開、カード見出しを見やすく

文字起こしは独自のスクロール領域に閉じ込められていたため、引用から移動しても
小さな枠の中でのスクロールになり、ページのスクロールでは後半が見えません
でした。全展開に変更しました。四つのカード（決議・タスク・リスク・未決事項）
の見出しは大きくし、背景色と左の色帯を付け、件数をバッジ表示にしました。

### Windows バッチファイルの改行コードにガードを追加

`setup-python.cmd`（Windows のインストール時に Python 環境を用意する
バッチファイル）は **CRLF 改行でなければなりません**。LF 改行の `.cmd` は
cmd.exe がトークンごとにエラーを出すため、git が入っていない環境で
アーカイブから導入した場合にインストール全体が失敗します。
v1.12.8 / v1.12.10 で三重の対策（実行前の正規化、ファイル自体の CRLF 化、
`.gitattributes` でのルール宣言）を入れましたが、**テストは一つも
ありませんでした**。

`tests/test_script_line_endings.py` を追加し、`.cmd` / `.bat` の全行が CRLF で
終わること、`.sh` に CR が一つも無いこと、`.gitattributes` の三つのルールが
残っていること（clone した側はこれに依存しています）を確認します。

clone した人にはこの種の問題は見えません。チェックアウト時に CRLF へ
変換されるため、開発機では常に正しく見えてしまいます。

### その他

- 配置用アーカイブを許可リスト方式に変更し、作成後に中身を検証するようにしました
- 内部ツールとガードテストの整理

## [1.15.67] - 2026-09-18

### 修正：テキスト抽出の 4 つのダウンロードボタンがすべて失敗

アップロードしたファイル名の拡張子の前に空白があると（`議事録 .pdf` —— Web
からファイル名をコピーするとよくあります）、TXT / Markdown / Word / ODT の
**4 つともダウンロードできず**、「ワークスペースに保存」も一緒に壊れていました。
ブラウザーはエラーメッセージをファイルとして保存し、保存ダイアログには
`txt.json` と表示されます。

原因は、**書き込みは元のファイル名、読み出しは前後の空白を取り除いた名前**で
行っていたため、組み立てたパスが一致しなかったことです。メッセージは「期限切れ」
と表示され、ファイルが削除されたように見えて、名前の不一致だとは分かりません。

ダウンロードには ASCII のみの予備名も付けるようにしました。UTF-8 の名前だけを
送ると、一部のブラウザーは URL の末尾をファイル名として使うためです。

### テキスト抽出：プレビューにコピーボタン

コピーされるのはプレビューの内容（先頭 5000 文字）です。ボタンが何文字コピーしたか
を示し、全文は「TXT をダウンロード」から取得できることも伝えます。

### 会議の要約：このリリースで大きく変わりました

**文字起こしの書式を自動判定し、手動でも指定できます。** よくある 8 種類に対応：
話者見出しが独立した行にあり本文が次の行に来る形、1 行 1 発言、時刻が先頭や
括弧内にある形、箇条書き、半角コロンなど。判定した書式を画面に表示し、
間違っているときは自分で選び直せます。

**文字起こしを直接貼り付けられます。** いったんファイルに保存する必要はありません。

**非常に長い発言は自動的に分割します。** 委員会の記録では一人が数分続けて話す
ことが多く、以前はそれが 2 万文字の 1 発言になっていました。すべての引用が
「1 番目の発言」を指し、クリックすると大量の文字が表示される —— それでは出典に
なりません。現在は 1 発言あたり最大 400 文字です
（実測：26,907 文字の記録が 4 件から 78 件になりました）。

**長い会議でも失敗しなくなりました。** モデルに送る内容が発言の長さに比例して
増え、モデルが読める範囲を超えると**黙って切り詰められて**いました（成功したように
見えて、実際には一部しか読まれていない）。送信内容に上限を設け、再確認も分割して
実行するようにしたので、1 つ失敗してもその分だけで済みます。

**マインドマップ・発言割合・章の割合はサーバー側で描画するようになりました。**
画面・Markdown・PDF が同じ画像を使います。マインドマップは**生成ではなく組み立て**
です。各ノードは引用の検証を通った項目から作られるため、図のどの枠も文字起こしに
たどれます。

**タイムスタンプがなくても「誰がどれだけ話したか」が見られます**（発言回数と文字数）。
以前はこのセクション全体が消えていました。章立ても同様です。

**新しい書き出し**：PDF（議事録全体）、図表の PNG、Markdown と画像の ZIP。
ダウンロードする Markdown には画像が埋め込まれるため、
「Markdown をオフィス文書へ」に渡しても画像が残ります。

**修正**：「Markdown をオフィス文書へ送る」ボタンが反応しませんでした。
各セクションとカードにアイコンを追加し、書き出しボタンと下の内容の間隔を空けました。

### ホーム画面にツール数を表示

「PDF / Office 文書の統合処理プラットフォーム **49** ツール」——
この数字は計算されるので、ツールを追加すれば自動的に変わります。

## [1.15.66] - 2026-09-18

### 修正：「API トークンの強制チェック」を有効にすると web 画面が止まる（issue #52）

強制チェックを有効にすると、web 画面が自分で行う `/api/` のリクエストが
すべて 401 で拒否されていました。進捗の取得、ジョブの取り消し、通知、
受信箱、ワークスペース一覧などです。**ページ自体は開き、ジョブも実際には
バックグラウンドで完了している**ため、「進捗が動かない」「押しても反応がない」
ように見え、設定が原因だとは分かりませんでした。

判定が**ハードコードされたパス一覧**（管理画面と 2 つのプレビュー）しか
認識しておらず、それ以外は外部からの API 呼び出し扱いになっていました。
現在は、そのリクエストがログイン後の**セッション**を持っているかで判断します。
持っていれば通常の権限チェックへ、持たないもの（スクリプトや `curl`）だけを
拒否します。維持すべき一覧がなくなったので、今後追加する端点が漏れることも
ありません。

設定の説明も事実に合わせました：**認証がオフのあいだ、この設定は効きません。**
そもそも身元を必要としない状態では web 画面とスクリプトを区別できないため、
どちらも通します。外部からの呼び出しを実際に制限したい場合は、先に認証を
有効にしてください。以前の説明は「すべて 401 で拒否します」と書かれており、
その状態では成り立っていませんでした。

## [1.15.65] - 2026-09-18

### 新しいツール：会議の要約

会議の文字起こしから、**要約・決定事項・タスク・リスク・未解決の論点・章立て**
を作ります。

**どの項目も元の発言と話者にたどれます** —— クリックすると文字起こしの
その行に移動します。これは飾りではありません。議事録は「何が決まったか」の
根拠として使われるもので、**出典のない決定事項は、その項目がないよりも悪い**
からです。引用元の発言に見つからなかった項目は残さず、
結果ページに「何件を除いたか」を表示します。

対応する文字起こし：字幕（`.vtt` / `.srt`）、文字起こしの JSON、
テキスト（`.txt` / `.md`）、Word と ODF（`.docx` / `.odt`）。
テキストの場合は 1 行に 1 発言、先頭に「名前：」を付けられ、
タイムスタンプも読み取れます。

**アップロード後、分析を始める前に解析結果を確認できます**（発言数・話者・
最初の数件）。話者の判定を間違えていたら、時間を使う前に気づけます。

タイムスタンプがある場合は**発言割合**も計算します。時間から直接求めており
（重なった発言は一度だけ）、推定値ではありません。
**タイムスタンプがない場合、このグラフと章立てのタイムラインは表示されません。**
理由も画面に表示します —— 推測した数値は使えないからです。

その他：分析は途中で停止できます。タブを閉じても処理は完了し、「マイジョブ」
から開き直せます。ダウンロードした Markdown はそのまま
「Markdown をオフィス文書へ」に渡してレイアウトできます。
管理画面で LLM を有効にしておく必要があります。

ツール総数 48 → 49。

## [1.15.64] - 2026-09-18

### `/readyz` を追加 —— 「動いている」と「欠けていない」は別の質問

ツールの読み込みに失敗すると（多くは依存パッケージの不足）、ログに 1 行残るだけで
そのツールは静かに消えていました。サイドバーから 1 つ減り、URL を開くと 404、
それでも `/healthz` は `{"ok":true}` を返します。管理者が気づける場所が
どこにもありませんでした。

`GET /readyz` を追加しました（`/healthz` と同じくログイン不要）：

- ツールが欠けている → **200 ＋ `degraded: true`**。残りのツールは通常どおり使えます
- データディレクトリに書き込めない、またはデータベースを開けない → **503**。
  それが本当に動作できない状態です
- モジュール名・例外メッセージ・ファイルパスは返しません。この端点は公開だからです

**どのツールが、なぜ読み込めなかったか**は管理画面の**システム状態**ページの
最上部に表示されます（管理者のみ）。失敗がないときは一切表示されません。
パッケージを追加インストールした後は、サービスを再起動すると読み込まれます。

`/healthz` は従来どおりで、ツールの読み込み状況を**意図的に見ません**。
サービスマネージャーはこれを見て再起動を判断するため、ツールが 1 つ欠けたことで
再起動を繰り返すほうが問題そのものより有害です。同じ理由で、ツールが欠けたときに
`/readyz` は 503 を返しません。本製品は単一の web プロセスで動作するため、
唯一のインスタンスを不健全と判定すると、利用者にはサイト全体のエラーページが
表示されてしまいます。

監視の設定は `OPS.md` に記載しています。これは外部のソースコード監査からの指摘です。

## [1.15.63] - 2026-09-18

### Markdown から文書ファイルへ：バックグラウンド処理になり、途中で停止できます

これまでは同期リクエストでした。「変換中…」を眺めるしかなく、止める手段もなく、
タブを閉じると処理が無駄になりました。

進捗バーと**停止**ボタンが付き、ページを閉じても処理は完了し、「マイジョブ」の
**開く**から復帰できます。停止は変換プロセスをツリーごと終了させます
（v1.15.62 で追加した仕組み）。進捗バーを隠すだけではありません。

完了したジョブは「マイジョブ」からもダウンロードでき、ファイル名には入力した
タイトルが使われます。

## [1.15.62] - 2026-09-18

### v1.15.61 の不具合を修正：コードブロックがページ外にはみ出していました

前のリリースでは、コードと枠の間に余白を作るためコードブロックを 1 セルの表で
囲んでいました。その結果、**長いコマンドが折り返されなくなり、ブロック全体が
ページの右端を越えて内容が切れてしまいました**。

内容が切れることは、文字が枠に接することよりはるかに深刻なので、この方法は
取りやめました。余白を得る方法を 3 通り実測しましたが（`padding`、外側の
コンテナ、1 セルの表）、変換エンジンはいずれも反映しません。現在はコードブロックを
**枠なしの、右にインデントした色帯**にしています。枠がなければ文字が接する対象も
なく、インデントによってコードと本文がはっきり分かれます。

表の枠線は影響を受けず、前のリリースで追加したまま見えています。

### 停止ボタンを押すと、実際に処理が止まります

「マイジョブ」やツール画面の停止ボタンは、状態を「停止」に変えるだけでした。
変換処理は外部プログラムの待機に数分を費やすため、こちら側のコードにある
キャンセル確認点には到達しません。画面では停止と表示されながら、
**サーバーはその変換を最後まで実行し**、CPU とメモリを使い続けていました。

現在は、そのジョブが起動した外部プログラムをツリーごと停止します。外部プログラムを
呼び出すすべてのツール（文字認識、各種変換）が対象です。

停止する前にプロセスの同一性を確認します。長時間動作するサービスではプロセス番号が
再利用されるため、確認しないと無関係なプロセスを止めてしまう可能性があります。

## [1.15.61] - 2026-09-18

### Markdown から文書ファイルへ：表に枠線が付き、コードが枠に貼り付かなくなりました

変換した表に枠線がなく列幅も不揃いで、コードブロックの文字が枠にぴったり
くっついていました。

変換エンジンが対応している CSS はごく一部です。`border: 1px solid …` は
**まったく描画されず**、コードブロックの `padding` はインデントとして扱われます
（枠と文字が一緒に右へ動くだけで、間に余白はできません）。HTML の表現属性に
切り替えたところ安定しました。表の枠線は実測 0.75pt（はっきり見えます）、
コードブロックは文字と枠の間に約 6pt の余白ができます。

同じ文書でページ数も 27 から 26 に減り、属性による枠線と CSS による極細線が
重なって干渉することもなくなりました。

## [1.15.60] - 2026-09-18

### Markdown から文書ファイルへ：ほぼ空白のページが出なくなりました

変換した PDF に、1～2 行しかないページが現れ、表が 1 行ずつ別ページに分割されて
いました。原因は、HTML を**文書レイアウトではなくウェブ表示レイアウト**で変換して
いたことです。同じファイルで実測すると、36 ページ中 3 ページがほぼ空白でしたが、
文書レイアウトでは 25 ページで空白ページはなく、1 ページあたりの文字数の中央値は
687 から 1029 になりました。表の列幅も改善しています。

他の 2 つの出力（.docx / .odt）は以前から文書レイアウトを使っており、
PDF の経路だけが漏れていました。

### Markdown から文書ファイルへ：出力形式を選べます

これまでは毎回 PDF と DOCX と ODT の 3 種類すべてを変換していました。形式ごとに
変換エンジンを一度実行し、エンジンは直列化され、1 回あたり 120 秒の上限があります。
そのためホストが混雑していると、数十 KB の Markdown でも 2 分以上かかって失敗する
ことがありました（画面には「10-30 秒」と表示されていました）。

出力形式のチェックボックスを追加し、既定では PDF のみを変換します。ページプレビューは
PDF から生成されるため、PDF を選ばないとプレビューがないことも画面に明記しました。
公開 API は形式を指定しない場合これまでどおり 3 種類すべてを生成するため、既存の
呼び出しには影響しません。

### Markdown から文書ファイルへ：コードブロックの構文強調

コードブロックは言語指定に応じて色分けされ、インラインコードの色も保持されます。
**極簡黒白**テーマは意図的に色を付けません。完全に中立な見た目がそのテーマの目的だからです。

### 変換がタイムアウトしても実際には停止していませんでした

タイムアウト時に停止していたのは外側の起動プログラムだけで、実際にファイルを解析して
いるプロセスは生き残り、CPU を使い続け、自力で終了しませんでした。変換プロセスは
（ウェブ画面の応答性を保つため）意図的に優先度を下げているので、残ったプロセスは
その後のすべての変換を遅くし、タイムアウトしやすくしていました。現在はプロセスツリー
全体を停止します。

7 つの変換経路すべて（文書から PDF、文書から画像、Markdown から文書など）が対象です。

### タイムアウトのメッセージが誤った方向を指さなくなりました

Markdown の変換がタイムアウトしたとき、メッセージは「ファイルが破損している可能性が
あるので相手に PDF を依頼してください」と表示していました。しかしその経路の中間ファイルは
本ツール自身が生成したもので、アップロードされた内容とは関係ありません。現在は、
タイムアウトは通常ホストの混雑か文書が大きいことが原因だと説明します。

## [1.15.59] - 2026-09-17

### `jtdt-reform` エンジンもページ単位で進捗を報告するようになりました

`pdf-to-office` の `pdf2docx-refine` と `jtdt-layout` は以前からページ単位で
報告していましたが、**`jtdt-reform` だけは開始と終了の 2 回だけ**でした。
大きなファイルでは数分かかり、その間画面がまったく動きません。
「何も起きていないように見える」のは最も診断しにくい症状で、
利用者は固まったと考えます。

解析と文書生成の両方の段階でページごとに報告するようにしました。
20 ページのファイルで実測したところ 41 回、進捗 0.07 → 0.95 でした。

> **進捗は付随物であって成果物ではありません**：報告用の関数自体が例外を
> 投げても、変換は成功しなければなりません。わざと例外を投げる門番を
> 用意しています。
>
> **書かれている状態も確認が必要です。** 私たち自身のメモには「3 つの
> エンジンすべてが対応済み」と書いてありましたが、コードを読むと 2 つ
> だけでした。書かれたものは、次の人が信じる事実になります。

## [1.15.58] - 2026-09-16

### ⚠⚠ 文書の差分比較：1 ページ挿入すると、それ以降が全ページ「別物」扱いに

ページの対応付けは**添字任せ**（旧版の N ページ目と新版の N ページ目）でした。
20 ページの文書の 3 ページ目に 1 ページ挿入して計測したところ、
**21 ページ中 2 ページしか対応が付かず、残り 19 ページが「ページ丸ごと削除＋
ページ丸ごと追加」**になりました。利用者には「全部変わった」と見えますが、
実際には 1 ページ増えただけです。

行の差分と**同じ仕組みを 1 段上**で使うようにしました。変更のないページを
アンカーとして 1 対 1 で対応させ、変更されたページはアンカーの間で位置的に
対応します。同じ文書で、いま差分と報告されるのは挿入された 1 ページだけです。

> **あいまい類似度は要りません** —— 変更のないページは一字一句同じなので、
> アンカーとして十分です。類似度を入れると調整すべきしきい値が増えるだけです。

> 挿入後は両者のページ番号がずれるため、見出しにその旨を出し
> （「旧版 4 ページ ↔ 新版 5 ページ」）、ページ表示は行番号ではなく
> **実際のページ番号**で画像を取得します。

### 項目名を「覚えさせる」とき、その名前が他に何に対応しているかを表示します

フォーム自動入力の「覚える」は**サイト全体で共有**する項目名の対応表に書き
込みます。これまでは「覚えました」としか出ず、何に影響したのか分かりません
でした。

> **「衝突を検出したら拒否する」には変えていません。** 索引の作り方を読んだ
> ところ、前提が間違っていました。**1 つのラベルが複数の項目に対応するのは
> 意図的**です。台湾のフォームでは 1 つの枠が 2 つのことを兼ねることが多く、
> 2 つの項目がそれぞれ自分の選択肢をチェックします。拒否にすると、その種の
> フォームのチェックが黙って壊れ、**しかもテストは赤くなりません**。
> ですから、ブロックではなく結果を伝える形にしました。

## [1.15.57] - 2026-09-16

### 日本語文書の匿名化に運転免許証番号と健康保険証を追加

日本語文書への対応（マイナンバーと法人番号＋検査用数字、電話、郵便番号、
住所、氏名）はすでにありました。今回は当初の計画で残っていた 2 種類を
追加します。

**どちらも「ラベル＋値」の形でのみ検出し、マスクするのは値の部分だけです。**

* **運転免許証番号は 12 桁で、マイナンバーと同じ長さ**です。しかも検証できる
  検査用数字の算式は公開されていません。ラベルが無ければ 12 桁の数字列は
  どちらか一方と推測するしかなく、**どちらに倒しても誤判定になり、画面には
  「処理しました」と出てしまいます**。
* **健康保険証の形式は保険者ごとに異なり**、全国共通の桁数も検査用数字も
  ありません。こちらはさらに、後ろに「番号」が続くことを条件にしています。
  そうしないと通常の文書にある「記号：A」まで拾ってしまいます。

> 値の中のハイフンは「各種ダッシュ」の文字クラスを使う必要があります ——
> PyMuPDF が取り出すのは通常のハイフンではなく非改行ハイフン `U+2011` です。
> テスト素材は手で打った文字ではなく `\xa0` と `\u2011` を直接書いています。
> そうしないとテストは通るのに実ファイルでは何も拾えません。

> 誤検知用の素材も引き続き合否の一部です：型番・注文番号・ISBN だらけの
> 日本語文書で、機微カテゴリのヒットは **0** でなければなりません。
> 「検出できること」だけを見ていては、何でも拾うまで緩めた式でも通ります。

### 隠しコンテンツ検査を別プロセスで解析するようにしました（外部監査 F04）

監査の指摘は「PyMuPDF は上流がマルチスレッドを想定していない」でした。
**字面どおりにロックを足すのは問題の取り違え**です。私たちは `Document`
オブジェクトを共有していませんし（ジョブごとに開いて閉じます）、報告自体も
クラッシュを再現できていません。さらにツールごとに散らばったロックは、
すべての入口が守っている保証も、**解析器のクラッシュを封じ込める力**も
ありません。

やる価値があるのは**被害範囲**です。MuPDF の C 層で segfault が起きると
**サービスのプロセスごと**落ち、実行中のジョブも閲覧中の利用者も巻き添えに
なります。分離しておけば、失敗するのはその 1 リクエストだけです。

**まずは隠しコンテンツ検査だけ。** 判断基準は「入力がもっとも信用できず、
倒れたときの影響がもっとも広い」ことで、あのツールはまさに「このファイルは
怪しいので見てほしい」ためのものです。他のツールはこれまでどおりで、
本番でしばらく動かしてから広げます。

> **`multiprocessing` ではなく `subprocess`**：spawn では子プロセスが
> **親の `__main__` を再 import** します。サービスは `python -m app.main` で
> 起動しているので、分離呼び出しのたびにサービス全体を作り直すことになります。

> **⚠ 計画時に測ったコストは間違っていました。** 「spawn ＋ PyMuPDF の
> import ＋ ファイルを開く」で 385 ms と測り、**自分たちの import 連鎖を
> 数えていませんでした**。実装後の最初の計測は **1.6 秒**。`app.tools.…` を
> import するとツールパッケージの `__init__.py` が走り、Web フレームワークと
> 設定の連鎖まで引き込むためです（その 1 行だけで 1,375 ms）。
> 検査本体を **PyMuPDF しか import しない**モジュールに切り出し、
> **ファイルパスで読み込む**ようにしたところ、固定費は **約 0.5 秒**に
> なりました。そのモジュールの import 一覧は門番が見張ります。

> **分離は機能ではなく防護です**：子プロセスが起動できないときは同一プロセスで
> 処理を完了させます。壊れたファイルの例外型もプロセス境界を越えて保持します
> —— 失うと 400「ファイルが壊れています」が 500「サーバーが壊れています」に
> 変わり、利用者は延々と再試行してしまいます。

> **検証は両方向**：同じクラッシュするコードが、分離しなければ親プロセスを
> 道連れにし、分離すれば道連れにしないことを確認します。「分離したら生き残った」
> だけでは何も証明できません（そもそもクラッシュしていないかもしれません）。
> さらに**実際にリクエストを送って**エンドポイントが分離を通っているかを確認
> します。内部関数だけを試すと、エンドポイントを元に戻しても緑のままです
> （監査の F06 が記録したのと同じ穴です）。

## [1.15.56] - 2026-09-16

### 文書の差分比較に「ページ表示」を追加 —— 差分を元のページ上に直接囲みます

これまではテキスト表示だけで、**何が**変わったかは分かっても、**ページの
どこで**変わったかは分かりませんでした。見出しに切り替えを追加しました。

* **テキスト表示** —— 従来どおり、一行も変えていません。
* **ページ表示** —— 左右に元のページ画像を並べ、差分をその場で囲みます。
  赤＝削除、緑＝追加、黄＝変更。

両方の表示は同じ比較結果を共有するので、切り替えても再アップロードも
再比較も起きません。オフィス文書も対象です（もともと PDF に変換してから
比較しているため、ページ表示は**変換後**のレイアウトになります）。

> **比較そのものは一文字も変えていません。** 行はこれまでどおり
> `get_text("text")` から取り、座標だけを `rawdict` から別に取得して
> 「空でない行の N 番目」で突き合わせます。実在サンプル 30 件で
> **101 / 101 ページ**が一致しました。一致しないページは**囲みを一切
> 描きません** —— 囲みが無いのは機能が一つ欠けるだけですが、
> 違う場所に囲みを描くのは嘘になるからです。

> 変更行の中の**文字範囲はもともと計算していて、計算したあと捨てていました**。
> 残すだけで「この行の何文字目が変わったか」を示せます。中国語・日本語は
> 文字単位でなければいけません：`get_text("words")` は空白の無い行を
> 「1 つの単語」として返すため、1 文字の変更で行全体が囲まれてしまいます。

### 回転したページで、危うく囲みが全部ずれるところでした

文字の座標とページ画像は同じ座標系ではありません。同じページを各回転角で
描画し、囲みの中のインク被覆率を測った結果：

| 回転 | 座標そのまま | `page.rotation_matrix` を掛ける |
|---:|---:|---:|
| 0 | 22.7% | 22.7% |
| 90 | **0.0%** | 22.7% |
| 180 | **0.0%** | 22.6% |
| 270 | 4.6% | 22.6% |

掛けないと囲みは真っ白な場所に落ちますが、ページ自体は正常に見えるので
誰も気づきません。そのため合否の基準は「囲みが描かれたか」ではなく
**囲みの中のインク被覆率**にしました。4 つの回転角すべてを検査し、
変異検証済みです（その 1 行を外すと 90 / 180 / 270 が赤くなります）。

### テキスト層が無いときは、そう言います（「差分なし」に見せません）

スキャン、アウトライン化された文字、文字対応表が壊れた PDF では座標が
取れません。ページ表示はそのページにその旨を表示し、OCR を案内します。
「囲みが無い」と「このページは変わっていない」は見分けがつかないため、
この一文は省けません。

### 同じ設定を 8 か所に写し、8 個目で 1 本まるごと壊れていました

ヘッドレスブラウザのテストは「ブラウザを探す」「ブラウザが読めるディレクトリ
を選ぶ」を各自で持っていました。新しく書いた 8 個目はファイルの中身を読まず
パス文字列だけを見ていたため、Ubuntu の `/usr/bin/chromium-browser` が
snap 版を起動する**シェルスクリプト**だと判定できませんでした。素材は
ブラウザから読めない場所に置かれ、アップロードは「成功」し、ファイル名も
表示され、送信時だけ失敗 —— 結果としてテストは丸ごと **skip** され、
pytest の出力上は何の問題も無いように見えていました。

`tools/browser_probe.py` に一本化し、8 本すべてがそれを使うようにしたうえで、
9 個目を作らせない門番を追加しました。

## [1.15.55] - 2026-09-16

### ツール名の変更：「文書の傾き補正」→ **スキャン補正**

旧名には 2 つの問題がありました。**「ページの向き」と紛らわしい**こと
（どちらも「ページをまっすぐにする」ように聞こえますが、あちらはページ全体を
90 / 180 度回すだけです）、そして**トリミング・傾き補正・地色のならし**という
3 つの処理のうち 1 つしか名前に表れていないことです。

新しい名前は「スキャン結合」と同じ系統になり、同類の機能だと一目でわかります。
英語は `Scan cleanup`。

> **ツール id と API パス `doc-straighten` は一文字も変えていません** ——
> id を変えると組み込みロール、データベース移行、既存インストールの権限まで
> 動かすことになりますが、ここで変えるのは表示名だけです。
> 旧名は検索キーワードに残してあるので、旧名で探しても見つかります。

### 2 つの差分ツールの「全文をコピー」ボタンが未翻訳だった

`旧版の全文をコピー` / `新版の全文をコピー` と完了メッセージが、英語・日本語 UI
でも中国語のままでした。Jinja のアイコン呼び出しで始まるテンプレートリテラルに
書かれていたため、文全体を辞書で引くことができず、誰も包んでいませんでした。
**比較が終わって結果が出てからしか現れない**ので、ページ単位の走査では
見つかりませんでした。

### Office エンジンが無いとき、文書の差分比較が 500 を返していた（503 に修正）

500 は「サーバーが壊れた」という意味です —— 利用者は再試行を繰り返し、監視側は
誤検知だらけになります。soffice が無いのは**配備**の問題であり、何を入れれば
よいかを伝えるべきです。`OfficeUnavailableError` と全体ハンドラーは以前から
ありましたが、このツールが `except Exception` で握りつぶして 500 に包んでいたため、
**ハンドラーまで届いていませんでした**。

> 門番はソースに「503」と書いてあるかを調べるのではなく、soffice を見つからない
> 状態にして実際に Office ファイルを送り、ステータスコードを見ます。
> 変異検証済み：修正を戻すと 500 になり、門番が赤くなります。

## [1.15.54] - 2026-09-16

### アンインストール後にスタートメニューのフォルダーが消えずに残ることがありました

フォルダー名はパスでもあり、レジストリは**最後にインストールしたときに作った
もの**しか覚えていません。そのため「言語 A で入れ、次に言語 B で入れ、それから
アンインストール」すると A のフォルダーが孤児になり、アンインストール後も残った
まま、以後どこからも削除されません。

**テスト機で実際に起きました**（2026-09-16）。アンインストール後も
`Jason Tools Document Toolbox` が残り、中の 2 つのショートカットは既に削除済みの
インストール先を指していました。

両側で対応しました：

* **アンインストール**：レジストリのものを消したあと、**各言語のフォルダー名**
  （および v1.15.30 以前の固定の中国語名）も順に試します。いずれも
  「`$SMPROGRAMS\` の下にあること」という同じ確認を通します —— 自分たちのもの
  でなければ触りません。
* **インストール**：前回記録したフォルダーを読み戻し、今回と異なれば削除します。
  そうしないと別言語で入れ直したときに 2 つ並び、どちらが生きているのか分かりません。

実機で確認：修正前 2 個 → 修正版をインストール → アンインストール → **0 個**。
4 つの SQLite ファイルのサイズは変わらず（利用者データは保持）。

> ガードは**宣言済みの言語一覧**をたどります。宣言した言語には必ず
> `SM_FOLDER_xx` があり、かつ削除リストに現れる必要があります ——
> 4 つ目の言語を足すとまずここが赤くなります。5 方向で変異検証済み。

### 三項演算子の**片方だけ**が包まれていた —— 英語 / 日本語 UI で 16 か所が中国語のまま

JS の文字列を翻訳対象にするとき、**三項演算子は全体を包まない**方針です
（包むと検索キーが実行時に決まり、いつまでも一致しないうえに無言で失敗する
ため）。正しい書き方は**分岐ごとに包む**こと。ところが自動で包むツールは
三項演算子を丸ごと飛ばすので、「**片方だけ包まれている**」状態を誰も見て
いませんでした。

調べたところ **16 か所**あり、いずれも英語 / 日本語 UI で中国語が出ます。
管理画面の保存結果（失敗側だけ翻訳済み）、`pdf-fill` / `doc-deident` の
右クリックメニューの見出し、文字数カウントで 1 時間を超えたときの表示、
フォント管理の「失敗 N 件」、文字認識で 3 秒以上待ったときの順番待ち表示。

**ページ単位の走査では見つかりません** —— クリック・切り替え・数値が
しきい値を超えたときにしか出ない文字だからです。新しい門番
`tests/test_ternary_branches_go_through_tr.py` を追加し、4 方向で
変異検証しました。

> **走査器で引用符を正規表現で対応づけてはいけません。** 最初の版は
> `'[^']*'` を使っていたため、引用符付き文字列とテンプレートリテラルが
> 混在する行で無関係な引用符どうしを組にし、間を丸ごと飲み込んで
> 誤検知と見逃しを同時に起こしました。1 文字ずつ走る方式（および
> `${…}` はコードなので分けて見る）に変えたところ、本当の見逃しが
> さらに 3 件見つかり、誤検知は消えました。

### 会社情報の項目名が英語 / 日本語 UI で中国語のままだった

`pdf-fill` ページの会社情報カードには **50 あまりの項目名**があり、訳語が
一度も用意されていませんでした。区分の見出しはさらに惜しく、**訳語は
すでに辞書にあったのに、表示側が `tr()` を呼び忘れていた**だけです。

表示側で訳すようにしました。**利用者が変更した項目名はそのまま**です
（見つからなければ原文を返すため）。管理ページの**編集できる**項目名の
入力欄はあえて手を付けていません —— 訳してしまうと保存した瞬間に利用者の
項目名が英語に書き換わるからです。

### デモデータ生成側が項目名をもう一部持っていて、ずれていた

公開するスクリーンショットを作る `tools/seed_demo_data.py` が、独自の
項目名対応表を持っていました。出荷側と **6 項目**ずれており、うち 2 つは
中国大陸の言い回し（台湾では使わない語）になっていました。私たち自身の
禁止語リストに載っている語ですが、用語の門番がそのフォルダを走査して
いなかったのです。単一の出どころに統一し、門番の範囲も広げました。

## [1.15.53] - 2026-09-16

### データのあるインスタンスで走査し、データと画面文言を切り分けました

これまでのページ単位の走査はすべて**空の**インスタンスで行っていたため、
「データがあって初めて現れる表」（TEST_PLAN §0.6 の 3 つ目）は一度も見られて
いませんでした。今回はデモデータを入れてから走査しています（英語 71 件、
日本語 9 件）。

**1 件ずつ確認した結果、訳し漏れは 1 件もありません** —— すべて利用者自身の
データでした：会社情報カードの項目名と値、グループ名と説明、印影・署名・
透かしの名前、会社設定ページの各セル。

これらには `data-i18n="skip"` を付けました。**フォーム自動入力の会社情報
カードが特に重要**です。そこの項目名は管理者が変更でき、しかも台湾の取引先
書式に印字された中国語の項目名と照合するための基準でもあります ——
訳してしまうと照合が静かに失敗します。

> **「たくさん出た」＝「訳し漏れが多い」ではありません。** その区別がないと
> 次回から誰もこの報告を読まなくなり、本物の漏れもノイズごと無視されます。

## [1.15.52] - 2026-09-16

### JS から設定する表示用属性も `tr()` を通します

`title` / `placeholder` / `aria-label` / `alt` の出どころは 2 つあります。
テンプレートの `title="…"`（描画時に翻訳されるので問題なし）と、
**実行時に JS が設定する** `el.title = '…'` です。後者はテンプレート走査から
見えず、テキストノードでもないため、ページ単位の走査でもマウスを乗せない限り
見えません。

残っていたのは 4 か所、すべて変数を埋め込んだテンプレートリテラル
（`` el.alt = `第 ${n} 頁` ``）でした —— 前バージョンのダイアログと同じ形です。
ガードは `tests/test_js_set_attributes_go_through_tr.py`。

> 下限は**実測**（実際 10 か所、その半分）です。きりのいい数字にすると、
> 常に成立するか、編集のたびに落ちるかのどちらかになります。

## [1.15.51] - 2026-09-16

### 送信後の結果表示が、英語・日本語の画面でも中国語のままでした

TEST_PLAN §0.6 には「ページ単位の走査では**送信後の結果表示は見えない**」と
明記してありますが、そこを走査した人は今までいませんでした。今回は
**スクリーンショット取得ツール**のついでに走査します。これは実際にファイルを
アップロードして送信し、結果が出るのを待ってから撮影するので、
**その画面こそが抜けていた部分**です。

初回で **11 個のツール**に残りが見つかりました（透かしのプレビュー状態表示、
PDF エディターの読み込みメッセージ、OCR のアップロードメッセージ、
一文ずつ翻訳の読み込みメッセージ、匿名化の集計チップ、PDF から文書ファイルの
変換前後の見出しとページ番号、ページごとのダウンロードの説明…）。

原因はすべて同じ形でした：**その文は変数を埋め込んで作られている**
（`` `第 ${n} 頁` ``）ため、埋め込み後の文全体は辞書に一致しません。
そもそも `tr()` で包まれていないものもありました。

> **`tr()` に退路を追加**：見つからないときは連続する数字を `{0}` `{1}`… に
> 置き換えてもう一度引き、見つかれば数字を戻します。これで**サーバーが作る
> ジョブのメッセージ**（`完成（3 份）` はバックグラウンドスレッドで作られ、
> リクエストがないため利用者の言語が分かりません）も翻訳できます。
> 2 回目も外れたらそのまま返すので、最悪でも今までと同じです。
>
> **その境界もテストにしました**：変数がすべて数字の文だけが対象です。
> `已上傳 a.pdf（6379.1 KB）` は最初の変数がファイル名なので、
> その文を作る場所で包む必要があります。

> **匿名化の検出種別名は half-fixed でした**：v1.15.47 で直したのは結果行の
> ラベルで、**集計チップは漏れていました**。同じ系統は一度に洗うこと —— また。

### 翻訳してはいけないドロップダウンがあります

個人情報の用途限定印で選んだ「用途」の文字列は、**そのまま印影に刷り込まれ
ます**。訳してしまうと、画面で選んだものと印字されるものが食い違い、
しかも見た目は完全に正常です。

このリストには `data-i18n="skip"` を付け、**走査側も `<option>` の skip を
見るように**しました。今までは見ていなかったため、これらは永久に誤検出として
報告に残り続けます —— 誤検出だらけの報告は誰も読みません。

> **`<select>` に付けるだけでは足りませんでした**：サイト内のドロップダウンは
> 自作ウィジェットで、**実際に見えているリストは元の `<select>` の外側に作られた
> 別のノード**です。`closest()` では印が見つからず、隠れたネイティブの
> `<option>` しか除外できていませんでした。ウィジェット側で `data-i18n` を
> 自分が作る外枠へ引き継ぐようにしました。

権限マトリクスのアカウント名・グループ名もデータとして扱います（利用者の
データであり、中国語のままが正しい）。一方、ツール名・ロール名・「グループ」の
3 か所は**本来訳すべきなのに包まれていなかった**ので併せて対応しました。

### ダイアログ：静的走査で 15 か所の `tr()` 漏れ

TEST_PLAN §0.6 はダイアログを「手作業でしか確認できない」枠に置いていますが、
**静的走査はすべての分岐を見られます**（エラー時しか通らない箇所も含めて）。
形は 3 種類 —— 変数を埋め込んだテンプレートリテラル、包み忘れた素の文字列、
そして最も厄介な**三項演算子の片側だけ包まれている**もの（対応済みに見えます）。

判定は「第 1 引数から `tr('…')` を取り除いて中国語が残らないこと」です。
「`tr(` で始まるか」だけを見ると `cond ? tr(a) : tr(b)` や
`err.message || tr(c)` という正しい書き方まで違反にしてしまいます。

> **自分のコメントが自分の走査を欺きました**（また）：修正の隣に書いた説明が
> 誤った書き方を例として引用しており、ガードがそれを検出しました。
> 先にコメントを除くこと。テンプレートでは `<script>` ブロックだけを取ること
> （HTML 全体を JS 用のコメント除去にかけると、本文をコードとして扱ってしまいます）。

## [1.15.50] - 2026-09-16

### メモリの受け入れ判定：送り出した分を先に計上する（監査 F06）

ディスパッチのループは 1 回で複数のジョブを送り出せますが、それぞれが
**その時点**の空きメモリを読みます。直前のジョブは**まだメモリを確保して
いない**ため、2 件目は古い数値を見て両方とも通ってしまいます。既定の同時
実行数 2 なら 1 件分（800 MB）、管理者が 4〜6 に上げれば 2.4〜4 GB のずれ
—— ちょうど OOM になる量です。

送り出したジョブは見積り分を予約として保持し、**落ち着き時間（30 秒）を
過ぎると計上しません**。その頃には実際の使用量が空きメモリに反映されて
おり、両方数えると二重計上になるためです。

> **「実行中が 0 件のときは判定しない」はそのまま**です。これは意図した
> 妥協（メモリ不足が続くとキューが永久に解けなくなる）で、コードにも
> そう書いてあります。今回は足りなかった半分だけを補いました。

### 複数 worker で起動したときに知らせます（監査 F11）

`OPS.md` には以前から「複数 worker にしないこと」と書いてありましたが、
**何も止めていませんでした**。症状（同じジョブが何度も送り出される、
実行中のジョブが突然「中断」になる）は設定の問題にはまったく見えません。

起動時に検出したら ERROR を 1 行記録し、**どう判断したか**と**何が起きるか**
を書きます。記録するだけで止めません —— 起動を拒否するほうが問題より
たちが悪いためです。

> 判定は uvicorn のソースを読んで確かめたものです（worker は
> `multiprocessing.get_context("spawn")` で起動するので、worker 側の
> `parent_process()` は `None` になりません）。`WEB_CONCURRENCY` も見ます。

### その他

* ソースに無効なエスケープシーケンスを残さないようにしました。Python 3.12
  は警告だけですが、**3.14 からは `SyntaxError`** になり、テスト全体が収集
  段階で止まります。実際の 2 か所はどちらも説明文の中でした。
* インストーラーの 2 つのガードで「どの言語の訳文に漢字があってよいか」の
  判定を 1 つに共通化しました。
* ページ単位の i18n スキャンに `--reveal` を追加しました。走査の前に
  **DOM にはあるが表示されていない**パネルやセクションを展開し、
  「開かないと見えない」種類を拾います。実測では英語・日本語とも 82 ページで
  追加の検出は **0 件**でした。**実行時に生成されるノード**
  （クリックして初めて現れるダイアログ）は依然として範囲外です。
* 1 つのラベルが複数の正規キーに対応するのは**意図した仕様**です
  （台湾の書式では 1 つの欄が 2 つのことを兼ねることがよくあります）。
  ガードを追加しました —— 危うく「不具合」として直すところでした。

## [1.15.49] - 2026-09-15

### 修正後の画像の四辺に赤い枠が出る（お客様からの報告）

四隅の検出が机まで拾ったように見えますが、**背景ではなく、こちらが塗った色**
でした。回転で新しく出た縁を埋めるのに

    cv2.warpAffine(..., borderValue=255)

を使っていました。OpenCV の `borderValue` は 4 つの数を取る `Scalar` で、
`255` だけ書くと `(255, 0, 0, 0)` に展開されます。1 チャンネルなら白（正しい）、
**3 チャンネルなら純粋な赤**です。v1.15.47 で処理をグレースケールからカラーへ
移したときに、この 1 行の意味が静かに変わっていました。

報告された名刺での実測：**純赤のピクセル 5,628 → 0**、外周 2 px の平均彩度
**43.8 → 2.8**（紙そのものは 4.2）。四隅の検出自体は正確でした
（インク被覆率 1.000、純度 1.000）。

同じ流れで、より古い順序の誤りも直しました：**地色の平坦化は回転の前**に
行います。回転は四隅を埋めますが、その余白は撮影された内容ではないのに
背景推定が「ここは元から明るい」と解釈し、ゲイン場が下がって全体が暗くなり
ます：同じ写真で**暗いピクセルが 6.9% → 64.7%**。以前の余白は赤
（輝度 54）で暗部としてマスクされていたため、誤った順序が表に出ませんでした。

### 四隅のドラッグ：1 つ目を動かすと画像が跳ねる

ステータス行は「修正前」の画像のすぐ上にあり、文字が長いと 2 行に折り返し
ます。再計算のたびに短い「プレビュー生成中…」へ戻していたため、
**角を 1 つ動かすたびに画像が 40〜50 px 上へ跳び**、残り 3 つの角が 19% 下に
落ちていました。

その四角形は妥当でないと判定され（内角の差 40.4°、上限 40°）**まるごと破棄**
されるのに、画面には「自分でドラッグした四隅を使用」と出たままでした。
ステータス行は**縮まない**ようにし、高さは決め打ちせずこれまでで最も高かった
値を覚えます（中国語・英語・日本語で長さが違い、ウィンドウ幅も変わるため）。

### インストーラー：日本語対応と文字化けの解消

**文字化け**（お客様からの報告、Win11 25H2）：`winget` は UTF-8 で出力し、
NSIS の `nsExec::ExecToLog` は**システムの ANSI コードページ**で解釈します
（繁体字中国語は CP950、**日本語は CP932**、英語は CP1252）。どれでも化けます。
その出力はもともと読む価値がないため `installer.log` へ回し、画面には自前の
1 行だけを残します。

**日本語**：言語テーブルは繁体字中国語と英語しか宣言しておらず、NSIS は
見つからないとき**最初に宣言されたもの**を使うため、日本語 Windows では
中国語のインストーラーが表示されていました。日本語ではコンポーネント一覧、
完了ページ、失敗メッセージ、そして**アンインストールの 3 つの確認**まで
対応しました（この経路は言語ダイアログの前に戻るため、最も漏れやすい場所です）。

### その他

* **文書の傾き補正**ツールの中国語名を 文件擺正 に変更しました。URL と API の
  パス（`doc-straighten`）は変わりません。
* 変更履歴に発言をそのまま引用しないようにしました。症状は残し、引用符と
  「誰が言ったか」だけを外しています。

### 匿名化が**日本語の文書**に対応しました

文書の言語に「日本語」が加わり、日本語専用の 6 種類を検出します：
**マイナンバー（個人番号）**、**法人番号**、**電話番号**、**郵便番号**、
**住所**、**氏名**。日本語のインターフェースでは、文書の言語も既定で日本語です。

* **検査用数字があるものは必ず検証します。** マイナンバーも法人番号も
  持っています —— 検証しなければ 12 桁 / 13 桁の数字がすべて該当し、
  型番や注文番号が大量に誤検出されます。しかも画面には「処理しました」と出ます。
* **誤検出用の資料が受け入れ条件の一部です。** 型番・注文番号・ISBN・
  バージョン番号ばかりの日本語文書で、機微な分類の検出は **0 件**でなければ
  なりません。「検出できること」だけを確かめるなら、何にでも一致する正規表現でも
  合格してしまいます。
* **置換値は日本語で、しかも検査用数字を通りません。** 検査を通る偽の番号は
  実在の誰かのものかもしれません（SSN に未割当の `9xx` を使い、偽の IBAN が
  わざと mod-97 を通らないのと同じ理由です）。
* **台湾向けと英語向けは無退行**（変更の前後で実際の資料を走査して比較）。

> **⚠ PDF から取り出される区切り文字は、入力した文字とは限りません。**
> 同じファイルに対して PyMuPDF が返したのは、ノーブレークスペース `\xa0` と
> **ノーブレークハイフン `\u2011`**（`-` ではありません）でした ——
> `[ \-]` で照合すると 7 種類のうち 3 種類しか取れず、
> **それでも画面には「処理しました」と出ます**。本プロジェクトは空白側の
> 同じ教訓をすでに記録しており、これはそのハイフン版です。
>
> 区切りを緩めたところ、今度は誤検出が 1 件出ました
> （`社内コード：03-1234-5678-X-99` が電話番号と判定）——
> 「前後にハイフンが来てはならない」側まで一緒に外すことはできません。

### ⚠ 十数個の走査ツールの `</script>` 正規表現がファイルを丸ごと見落とし得ました

`</script  >` は妥当な HTML です。`</script>` を直書きした正規表現はこれを
「まだ閉じていない」と扱い、**残りのファイルをスクリプトの中身として飲み込みます**
—— その走査ツールはそこから先を黙って検査しなくなります。CodeQL の
`py/bad-tag-filter`（High 11 件）が指摘していたのはこれです。

`tools/source_text` に 1 つだけ実装を置き（`</tag\b[^>]*>`）、14 か所すべてを
それに合わせました。

### PDF エディタ：切断やタブを閉じても作業が残ります

編集の状態はこれまでそのタブの中にしかありませんでした。サーバーにあるのは
**すでに焼き込まれた PDF** であって編集状態ではなく、しかも 2 時間後には
一時ファイルの掃除で消えます。取り戻せてもテキストボックスは動かせません。

編集内容はブラウザーに保持され、**同じファイル**を開き直すと
「前回の続きから編集しますか」と尋ねます。

> **別のパソコンには付いてきません** —— このツールが作る他のものはサーバーに
> あるので、その旨を画面に明記しています。
>
> **空の編集状態が本物の下書きを上書きしてはいけません。** ファイルを開き直すと
> まだ空のキャンバスのスナップショットが取られ、その 1.5 秒後に前回の下書きを
> 消していました ——**利用者が「続きから」を押す前に**です
> （実測：保存時は 1 個、読み戻すと 0 個）。これは実際のブラウザーで動かして
> 初めて見えます。静的な検査はコードがあることしか見ません。

### PDF → 文書ファイル：ページごとの進捗

`Converter.convert()` は最初から最後まで一気に走る 1 回の呼び出しで、
このツールは数分かかることも珍しくありません —— バーはまったく動きませんでした。
その呼び出しが内部で行っている 4 つの手順に分け、ページ単位の粒度は
pdf2docx **自身**のログ行から取ります（内部構造には触れません）。
4 ページのファイルで 10 回報告するようになりました。

### 紹介サイトのスクリーンショットが空の状態ではなくなりました

「ユーザー管理」と権限マトリクスは「ユーザーもグループもまだありません」でした。
デモデータにアカウント 6 件、グループ 3 件、そして**同じアカウント名が local と
ldap の両方に存在する状態**（あのスクリーンショットの主題そのもの）を
加えました。すべて架空の値です。

---

## [1.15.48] - 2026-09-14

### サイト左上のタイトルが英語・日本語で折り返していました

**実測**：折り返さない場合に必要な幅は中国語 166 px、英語 225 px、
**日本語 285 px**。ナビゲーションを加えると英語は **1186**、日本語は **1188** で、
コンテナは **1180** —— わずか数ピクセル足りていませんでした。

ブランド表示を 2 行の固定レイアウトにしました。1 行目は常に `Jason Tools`
（どの言語でも同じ）、説明は 2 行目に置きます。3 言語とも必要幅は
919 / 1052 / 1002 px となり、100 px 以上の余裕ができました。

> **文字を小さくしたのでも、折り畳む幅を上げたのでもありません** —— どちらも
> 問題を次の言語へ先送りするだけです。長さが変わる部分を、長さが影響しない
> 場所へ移しました。

---

## [1.15.47] - 2026-09-14

### ⚠ 紹介サイトのスクリーンショットには、そもそも一度も書類が入っていませんでした

入れ忘れていたわけではありません：

* この環境の `/usr/bin/chromium-browser` は **snap 版**で、**`/opt` を読めません**
  —— サンプルファイルはまさにそこに置いてありました。
* それでも `DOM.setFileInputFiles` は「成功」し、画面にはファイル名も出ます。
  **実際に送信する段になって初めて XHR が `network error` で失敗し、
  サーバー側にはリクエストが 1 件も届いていません。**

つまりこの一連のアップロードは公開当初から一度も成功していませんでした
（英語版が「Please upload a PDF first」のダイアログで止まっていたのも同じ原因です）。
ブラウザーが読める場所へサンプルを置き直したことで、すべてのページが直りました。

> **この種類に共通する教訓**：「うまくいったように見える」（ファイル名が出ている）
> ことと「うまくいった」ことは別です。判定は
> **サーバーがそのリクエストを受け取ったか**、そして
> **スクリーンショットにダイアログが被っていないか**（出力するようにしました）に
> 変えました。

### スクリーンショット：すべて架空のデモデータで、実際にツールを動かす

`tools/seed_demo_data.py` がデモ用の会社（範例科技股份有限公司、統一番号
12345675、02-1234-5678 など —— **ひとつも実在の値ではありません**）、デモ用の
印章、そして合成した取引先情報シートを作ります。フォーム自動入力の画面は
**18 項目が実際に入力された**状態です —— もう実データを塗りつぶす必要はありません。

各ページには「どのサンプルを使うか、ボタンを押すか、どこまでスクロールするか」の
手順を持たせ、固定秒数で待つのではなく**結果が実際に描画されるまで待ちます**。

### 台湾専用のツールは英語版・日本語版のサイトに載せません

それらのツールは英語・日本語のインターフェースでは元々グレーアウトされています。
スクリーンショットだけ載っていると使えるように見えてしまいます。どれを外すかは
レジストリの `ToolMetadata.locales` から決めます（手書きの一覧は持ちません）。
残った図版は番号を振り直します —— 01 / 03 / 04 と飛んでいる方が見苦しいからです。

### サイトの言語切り替えをドロップダウンにしました

ナビゲーションに `繁體中文English日本語` が続けて並び、バー全体が 2 行に
折り返していました。言語は増える一方なので、幅が言語数に依存しないドロップダウンが
適切な形です。

ナビゲーションはあわせて 3 点変更しました：

* **項目を折り返さない** —— 折り返すと語の途中で切れていました。
* **日本語の項目を短く**（`インストール` → `導入` など）。
* **ハンバーガーに畳む幅は実測**（`chromium --headless` で言語ごとに計測）：
  必要な幅は中国語 805 px / 日本語 875 px / 英語 933 px。英語は 1060 px で
  収まらず 1100 px なら収まるので **1080 px** にしました。従来の 980 px は
  中国語の長さだけを見て決めた数値でした。

### 文書のまっすぐ補正：「白黒にする」を選んでいないのに白黒になる

撮影されたのはカラーの名刺でした。処理全体がグレースケールの複製の上で走っていた
ため（`crop_page(gray)` / `warp_quad(gray, …)`）、色は一度も残っていませんでした
—— 画面のチェックボックスには「白黒にする（既定はオフ）」と書いてあるのに、です。
**インターフェースが、実装していないことを約束していました。**

**グレースケールで測り、カラーに対して処理する**方式に変更しました。あわせて
RGB / BGR の取り違えも見つかりました（PyMuPDF が返すのは RGB、`imencode` が
求めるのは BGR）—— 直さなければ赤と青が入れ替わっていました。

> 判定は**彩度**であって、チャンネル数ではありません。3 チャンネルでも中身が
> 同じなら見た目は白黒のままです。

### 文書のまっすぐ補正：出力サイズを指定できます（ピクセル / mm）

自動（補正後のサイズに合わせる）/ A4 / A4 横 / A3 / Letter / カスタム（幅 × 高さ
＋ 単位）。プレビューのあと、**実際に出力されるサイズが先に入力されます**。
一度でも手を加えたら、そのあとは自動で上書きしません。

> **比率が合わないときは、比率を保ったまま周囲を白で埋めます —— 切り取ることも、
> 引き伸ばすこともしません。** このツールは最初から「内容を切るのは許されない、
> 机が少し余分に写るのは見苦しいだけ」という方針です。余白は黒ではなく白です
> （黒だと印刷でインクを大量に使います）。不正なサイズは「未指定」として扱い、
> 500 は返しません。

### 文書のまっすぐ補正：不要になった再描画はすぐ中止します

これまでは「遅れて返ってきた応答を無視する」だけで、リクエスト自体は最後まで
走っていました —— **サーバーは誰も必要としないページを描画していました**。
`AbortController` で実際に中止します。

> 新しい処理に置き換えられたことは**失敗ではありません**。「プレビューに失敗
> しました」と出すと壊れたように見えますが、実際には次の 1 枚が進行中です。

### ⚠ 匿名化：日本語インターフェースが台湾として扱われていました

v1.15.46 で直したのは文書の匿名化の方で、**テキストの匿名化には古い規則
（「`en` でなければ `zh-Hant`」）の写しが残っていました** —— 日本語の
スクリーンショットで `4111 1111 1111 1111` が台湾の固定電話として検出されて
いるのが見つかりました。両方が `patterns.default_doc_lang` を共用するようにし、
本当に委譲しているかを AST で検査します。

> 「**同じ種類はまとめて洗い出す**」—— 同じ教訓を 1 日に 2 回繰り返しました。

同じ回でもう 2 点：

* **検出結果の分類名が翻訳を通っていませんでした** —— 英語でも日本語でも
  あの行は中国語のままで、2 つのツールの両方が該当していました。
* **住所のマスクが台湾の形に固定されていました** ——
  `1842 Maple Street, Springfield, IL 62704` が `OO市OO區OO路OOO號` になり、
  元の文書に台湾の住所が書いてあったかのように読めてしまいます。マスクの目的は
  *形を保ったまま中身を見せない*ことなので、別の国の形に置き換えては前半が
  成り立ちません。中国語の住所は従来どおり、それ以外は 1 文字ずつ伏せ、
  区切り文字と長さだけを残します。

### その他

* 「PDF → 文書ファイル」から **Beta** の表記を外しました。
* **アップグレードの案内に「`-C` は大文字」と明記しました**（2026-09-14
  お客様からの報告）：小文字で `git -c /opt/jt-doc-tools/ …` と打つと
  `fatal: not a git repository (or any of the parent directories): .git`
  と返ります。小文字の `-c` は*設定*オプションなので、git はそのディレクトリへ
  移動しないためです。**このメッセージは「インストール先が git 管理ではない」
  ように読めるため**、お客様はまったく別の方向を調べることになりました。
  README・`OPS.md`・サイトの 3 か所に注記を入れ、コピーして使うコード
  ブロックに小文字の `git -c <パス>` が現れないことを検査します。

---

## [1.15.46] - 2026-09-14

### 日本語インターフェースを追加しました

インターフェース、紹介サイト、API ガイド、トラブルシューティングのページ、README を
日本語で提供します。サイドバーの「インターフェース言語」から切り替えられます。

* インターフェースの言語カタログ **4,891 項目**。
* 紹介サイト / API ガイド / トラブルシューティングの各ページと README の日本語版
  （`index-ja.html` / `api-ja.html` / `troubleshooting-ja.html` / `README_ja.md`）。
* 言語の切り替えは**「切り替え」ではなく一覧**になりました —— 2 言語のときは
  1 つのボタンで足りましたが、3 言語では日本語のページから英語のページへの
  出口がなくなります。各ページが**自分以外のすべての言語**へリンクします。

> **生成スクリプトと各種チェックは、言語の一覧を
> `app/core/ui_locale.SUPPORTED` から読むようになりました。** これまでは 8 か所が
> それぞれ `en` を直書きしていました —— そのままでは 3 つ目の言語が増えたときに
> **黙って英語だけを検査し続けます**（pytest の出力では「1 つも走査していない」と
> 「走査してすべてきれいだった」は同じに見えます）。

**日本語を追加して初めて見つかった問題が 3 つあります**（いずれも
「英語を追加したときには起きなかった」種類のものです）：

* **用語チェックが日本語を全面的に誤検出していました。** 日本語の
  「保存」「字体」は正しい日本語ですが、中国語の禁止用語リスト
  （中国語では「儲存」「字型」を使います）に載っています。日本語は
  ほぼすべての行に漢字があるため、除外しないと誤検出だらけになり、
  **誤検出が増えるとこのチェック自体が雑音として無視されます**。
  除外の判定は**ファイル名**に置きました（`README_ja.md` /
  `index-ja.html`）。言語版は中国語版と同じ階層にあり、ディレクトリでは
  区別できないためです。
* **カタログのチェックにある「訳文に漢字があってはならない」は日本語には
  成り立ちません。** 日本語の訳文は必ず漢字を含みます。代わりに
  「現代日本語では使わない中国語の語」を検出する方式にしました
  （`這` / `嗎` / `什麼` / `沒有` など）。**`的` は入れてはいけません** ——
  「一般的」「自動的」は正しい日本語です（最初の版では入れてしまい、
  その場で 3 件の誤検出になりました）。
* **インターフェースに日本語を足しても、匿名化が日本語文書に対応した
  わけではありません。** 文書の言語の既定値は「`en` で始まらなければ
  台湾」だったため、日本語インターフェースの利用者に台湾向けの
  パターンが適用されていました。台湾の固定電話 / 住所 / 統一番号の
  パターンを他言語の文書に当てると「検出できない」のではなく**誤検出**し、
  画面には「処理しました」と表示されます。専用のパターンがない言語では、
  言語に依存しない組に切り替えます。

### 修正：ドロップダウンの文字が翻訳を通っていませんでした

日本語のインターフェースを実際のブラウザーで 1 ページずつ走査して 5 か所
見つかりました。**そのすべてが英語のインターフェースでも中国語のまま**で、
翻訳対訳辞書の 2 つのドロップダウンは v1.15.19 の公開時からずっとそうでした：

| 場所 | 内容 |
|---|---|
| 翻訳対訳辞書 | 言語のドロップダウン 12 項目 |
| システム状態 | データベース一覧の名称（監査記録 / 統一番号データベース…）|
| 文書の匿名化 **と** テキストの匿名化 | 文書の言語のドロップダウン |
| 文書のまっすぐ補正 | 解像度の説明 |
| ログイン画面 | 認証元（ローカルアカウント）|

> **共通点は「文字がサーバーから来たデータである」こと** —— テンプレート内の
> `tr('…')` を文字列として探すチェックには一切見えません。新しい
> `test_option_labels_go_through_tr` は `<option>` の中の式が `tr()` を
> 通っているかを直接見ます。本当にデータであるもの（ユーザー名、ツールの id、
> 言語の自称）は**理由を書いたうえで**除外します。
>
> **同じ種類はまとめて洗い出すこと**：匿名化の 2 つのツールは同じ形の
> テンプレートをそれぞれ持っていて、最初は片方しか直していませんでした。

### 修正：紹介サイトの 1 文の語順が誤っていました（英語版も）

「Office エンジンが必要なツール」の段落は `<b>` で区切られ、区切りごとに
翻訳されるため、2 つの動詞が入れ替わっていました。
**各区切りの訳文は、それ自身の位置で読んで通る必要があります。**

日本語には組版の規則を 1 つ追加しました：インライン要素の**両側が日本語**の
ときは、その間の空白を取り除きます（中国語の原文では、タグの中がラテン文字
であるために空白が入っていることが多いためです）。取り除かないと
「不要 です」のようになります。

### 修正：言語切り替えの cookie に HTTPS サイトで `Secure` が付いていませんでした（ZAP が検出）

`/ui-locale` は `request.url.scheme == "https"` を見ていましたが、本プロジェクトは
uvicorn の `proxy_headers` を無効にしているため、**リバースプロキシの背後では
その値は常に http** です。そのため HTTPS のサイトでも `jtdt_locale` に
`Secure` が付いていませんでした。全体で共用している `is_https_request()` を
使うようにしました（SSO のトランザクション cookie も同様にしました。
そちらは IdP に送る redirect_uri を見ていましたが、`Secure` が対象とするのは
**ブラウザーとの区間**です）。

> **その関数の説明には「すべての cookie の `secure` はここを通すこと」と
> 以前から書いてありました —— ただし検査がありませんでした。**
> 本プロジェクトの失敗はほぼこの形で再発します。新しい
> `tests/test_cookie_secure_flag.py` は AST を辿って `set_cookie` /
> `delete_cookie` のすべてを検査します（**削除にもフラグが必要です** ——
> `Max-Age=0` は作成時のフラグを引き継ぎません）。加えて、唯一の除外である
> ローカル変数が本当にその関数から計算されているかも検査します。それがないと、
> `is_https = True` と直書きしても緑のままです。
>
> **この経路はそれまで一度も走査されていませんでした**：日本語を追加したことで
> 言語切り替えがリンクの集まりになり、ZAP のクローラーが初めて `/ui-locale` へ
> POST しました。**「走査して何も出なかった」と「問題がない」は別の主張です**
> —— 走査が証明するのは「到達した範囲はきれいだった」ことだけです。

### その他

* スクリーンショットの取得ツールと 1 ページずつの走査ツールは `--locale` を
  受け取るようになり、日本語版のサイトは**日本語インターフェース**の
  スクリーンショット（`screenshots/ja/`）を使います。
* `tests/test_i18n_catalog.py` は JavaScript から `tr()` のキーを集める前に
  **コメントを取り除く**ようになりました —— 規則を説明するコメントに書いた
  例が本物のキーとして数えられていました。本プロジェクトで繰り返している
  「走査ツールが、自分の探している名前そのものに騙される」で、今回は
  こちらが捕まりました。

---

## [1.15.45] - 2026-09-14

### 文書のまっすぐ補正：複数ページ / 複数ファイルのすべてが見えるようになりました

これまではページ番号の**数値入力欄**しかありませんでした：番号を打ち込む必要があり、
どのページがあるのかも、どれを調整したのかも見えず、複数のファイルを 1 つに結合すると
どのページがどのファイル由来かもわかりませんでした。

ページごとのサムネイル一覧を追加しました。クリックで切り替えてそのページを再計算し、
現在のページを強調表示し、「〇° 回転 / 四隅を手動指定」を表示します。複数ファイルのときは
各元ファイルの先頭ページに区切りとファイル名を表示します。

### 文書のまっすぐ補正：「自動で検出」に戻しても戻っていませんでした

「四隅を自分で合わせる」に切り替えて調整したあと元に戻しても、補正後の画像と
四角形の範囲が手動のままでした。原因は 2 つ重なっていました：モードの切り替えが
オーバーレイの表示だけを切り替えて**プレビューを再実行していなかった**こと、
そして保存された手動の座標がそのまま使われ続けていたことです。

> **モードはスイッチであって削除キーではありません。** 自動モードではその座標を
> 送信しませんが（プレビューと送信の**両方**で除外する必要があります。片方だけだと
> 画面には「自動で検出」と表示されているのに納品されるファイルは手動のもの、
> という状態になります）、座標自体は**保持します** —— 手動に戻したときに
> 利用者の作業が失われないようにするためです。

### 文書のまっすぐ補正：回転したあとに四隅を合わせると結果が壊れていました

利用者が合わせる四隅は**回転後**の画像上の点ですが、呼び出し側は**回転前**の
幅と高さで 0〜1 をピクセルに換算し、さらにコアがもう一度回転させていました。
90° での実測：出力 **834×358**（正しくは 471×629）、暗いピクセルの割合 **41.2%**
（枠に入っていたのはほとんど机で、正しい値は 1.9%）。

> **修正は 2 つの換算を直すことではなく、座標系を 1 つに減らすことです。**
> `quad` は常に「**回転後の正規化された 0〜1**」となり、自動検出もコア内
> （回転後）へ移しました。途中に換算は存在しません。
>
> 同じ原因の、誰も報告していなかったもう 1 つの分岐：**ブラウザーへ返す自動検出の
> 四隅も回転前の幅と高さで正規化されていました** —— 回転後に手動へ切り替えると
> ハンドルがまったく無関係な位置に現れますが、画面上はハンドルが 4 つあり、
> 完全に正常に見えます。

---

## [1.15.44] - 2026-09-14

### 文書のまっすぐ補正：四隅が**一度もつながっていませんでした**

輪郭を太くしても直りませんでした。原因は太さとは無関係です：

* **`SVGElement` には `hidden` の IDL 属性がありません**（仕様では
  `HTMLElement` に定義されています）。`svg.hidden = false` は誰も読まない
  プロパティを設定するだけで、**`hidden` 属性自体は変わりません**。
* そして `platform.css` には `[hidden] { display: none !important; }` があります ——
  名前空間のない*作成者*スタイルシートです。ブラウザー自身の `html.css` は
  HTML に名前空間が限定されていますが、こちらは限定されていないため、
  **SVG も同様に隠されます**。

つまりこの四角形は最初のバージョンから `display:none` のままで、しかも
**JavaScript のエラーはどこにも出ません**：隣の 4 つのドラッグハンドルは
`<div>` なので正常に動き、画面には点だけが見えて線は決して現れないため、
「壊れている」ではなく「まだ作られていない」ように見えていました。
どちらでも動作する `toggleAttribute` に変更しました。

### 文書のまっすぐ補正：「クリーンアップ」を追加（既定で有効）

スマートフォンで撮影したときに最も多い問題が、半分に影がかかることです。
**判定基準は文字認識率であり、見た目が整っているかどうかではありません：**

| 状況 | 未処理 | クリーンアップ後 |
|---|---:|---:|
| 片側の強い影 | **0.472** | **0.982** |
| 四隅の減光 | 0.884 | 0.986 |
| 斜め方向の影 | 0.967 | 0.986 |

> **二値化はクリーンアップではありません。** 同じ素材で局所的な二値化を行うと
> 認識率は **0.108** まで下がりました —— しかも*見た目*が最もきれいなものが
> 最悪の結果でした。二値化はファイルを小さくするための独立した既定オフの
> 選択肢のままです。CLAHE も一貫して悪化しました。

> **`divide(画像, 局所最大値)` は使えません**：認識率は同等に良いものの、
> 広い暗い領域を真っ白に飛ばしてしまいます —— 濃いグレーの写真ブロックが
> 平均 66.3 から **254.9** になり、つまり消滅しました。代わりに、緩やかに
> 変化する上下限付きのゲイン場を推定し、信頼できない（広く暗い）領域は
> マスクして**境界から** `inpaint` で埋めます。

### PDF エディタ：ファイルに電子署名があるときに警告します

編集して新しいファイルとして保存すると**署名は必ず無効になります** ——
署名はファイル全体を対象とするため、どんな変更でも（保存し直しただけでも）
ビューアで「署名が無効／文書が変更されています」と表示されます。

### 割印：印章の周囲の透明な余白で印が小さくなっていました

「印章の幅 40 mm」と指定しても、*画像全体*を 40 mm に縮小していました ——
写真から切り出した印章には透明な余白が残っていることが多く、実際のインクは
**26.8 mm**（余白 25% の場合）にしかならず、**同じ設定でも元画像が違えば
違う大きさ**になっていました。

### 割印：プレビューをクリックすると原寸で表示できます

> **14 個目の実装は書きませんでした。** 調べたところ**13 個の別々の
> lightbox の実装**があり、それぞれキーボード操作や閉じ方が微妙に違いました。
> 共通の `static/js/lightbox.js` を 1 つ用意し、新しい実装が増えないように
> するチェックを追加しました。

---

## それより前

それより前のリリースは [CHANGELOG.md](CHANGELOG.md)（完全な履歴）または
[CHANGELOG_en.md](CHANGELOG_en.md)（英語の要約）をご覧ください。
