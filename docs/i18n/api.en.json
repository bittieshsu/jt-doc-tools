{
  "+ 對應 HTTP 4xx/5xx": "plus the matching HTTP 4xx/5xx",
  "/ LDAP / AD) 完全獨立。": "/ LDAP / AD).",
  "0 起算頁碼；超過該 PDF 頁數者自動跳過": "0-based page number; anything beyond the document is skipped",
  "0~1 的比例": "a 0~1 ratio",
  "1. 認證": "1. Authentication",
  "10. Job 模式 API": "10. Job-mode API",
  "11. 管理端 API": "11. Administration API",
  "12. 整合範例": "12. Integration examples",
  "13. CLI 管理 token": "13. Managing tokens from the CLI",
  "14. 速率限制 / 大檔上限": "14. Rate limits and upload size",
  "14b. 使用者工作區（session 認證，非 Bearer API）": "14b. User workspace (session authentication, not a Bearer API)",
  "14c. 使用者通知與收件匣（session 認證，非 Bearer API）": "14c. User notifications and inbox (session auth, not the Bearer API)",
  "15. 變更歷史": "15. Change history",
  "2. 通用約定": "2. Common conventions",
  "3. 文件轉換 API": "3. Document conversion API",
  "4. PDF 編修 API": "4. PDF editing API",
  "5. PDF 擷取與分析 API": "5. PDF extraction and analysis API",
  "6. 表單與簽章 API": "6. Forms and signing API",
  "7. 安全與隱私 API": "7. Security and privacy API",
  "8. 文字與比對 API": "8. Text and comparison API",
  "9. 商務查詢 API": "9. Business lookup API",
  "<strong><code>X-Straighten-Worst-Residual</code> 是驗收指標</strong>：轉錯方向時「角度」看起來有變化， 只有殘留角會現形。正常應該在 0.2° 以內。": "<strong><code>X-Straighten-Worst-Residual</code> is the acceptance figure</strong>: if the rotation went the wrong way the reported angle still looks like something happened; only the residual reveals it. It should stay within 0.2 degrees.",
  "<strong><code>context</code> 只拿來讀懂逐字稿，不會變成項目的來源。</strong> 知道「某某是營運副總、會議主席」對判斷誰在交辦、誰是負責人很有幫助， 但背景裡寫的事情<strong>沒有在會議上發生過</strong> —— 決議 / 待辦 / 風險 / 未決問題 一律只從逐字稿產生。除了提示裡明講之外，還有兩道機制擋著： 引用驗證（項目必須指得回逐字稿的段落，背景不在被比對的範圍裡）， 以及「跟背景很像、而且明顯比跟逐字稿更像」就丟掉。 被丟掉的會計進 <code>dropped_count</code>。": "<strong><code>context</code> is used only to make sense of the transcript; it never becomes a source of items.</strong> Knowing that someone is the VP of Operations and chairs the meeting helps decide who is delegating and who is the owner, but what you write in the background <strong>did not happen in the meeting</strong> — decisions, actions, risks and open questions are always drawn from the transcript. Besides saying so in the prompt, two mechanisms enforce it: citation verification (an item must point back to transcript segments, and the background is not part of what is compared), and dropping anything that closely matches the background and matches it noticeably better than it matches the transcript. Dropped items are counted in <code>dropped_count</code>.",
  "<strong>Jason Cheng</strong><br>\n        <a href=\"https://[網址]\" target=\"_blank\" rel=\"noopener\">Jason Tools</a>": "<strong>Jason Cheng</strong><br>\n        <a href=\"https://[網址]\" target=\"_blank\" rel=\"noopener\">Jason Tools</a>",
  "<strong>先問可用的目標格式再送轉換。</strong> <code>target</code> 的值會因安裝而異（沒裝 Impress 模組就不會有簡報那一組），不要把 id 寫死在自己的程式裡：": "<strong>Ask which target formats are available before submitting a conversion.</strong> The values of <code>target</code> differ from one installation to another (without the Impress module there is no presentation group), so do not hard-code the ids in your own program:",
  "<strong>原文一送出就查得到</strong>（右側還沒翻好的項目沒有 <code>translated</code> 欄位），": "<strong>The source text is available as soon as it is submitted</strong> (items not yet translated simply have no <code>translated</code> field),",
  "<strong>取得轉換前後對照縮圖</strong>：job 完成後，<code>GET /api/jobs/{job_id}</code> 的 <code>meta.preview</code> 內含 <code>page_indices</code>（要預覽的 0-based 頁碼清單，≤ 6 頁全取 / &gt; 6 頁取前 2 + 中 2 + 後 2）、 <code>orig_pages</code>、<code>result_pages</code>、<code>orig_chars</code>、<code>result_chars</code>。逐頁縮圖用：": "<strong>Getting before/after thumbnails</strong>: once the job finishes, <code>meta.preview</code> in <code>GET /api/jobs/{job_id}</code> contains <code>page_indices</code> (the 0-based pages to preview: all of them for ≤ 6 pages, otherwise the first 2, middle 2 and last 2), <code>orig_pages</code>, <code>result_pages</code>, <code>orig_chars</code> and <code>result_chars</code>. For the thumbnails themselves:",
  "<strong>回應型別依 <code>include_images</code> 而不同</strong>：<code>false</code> 回 <code>text/markdown</code>，<code>true</code> 回 <code>application/zip</code>。": "<strong>The response type depends on <code>include_images</code></strong>: <code>false</code> returns <code>text/markdown</code>, <code>true</code> returns <code>application/zip</code>",
  "<strong>多層 nginx 情境（自架 LLM proxy + jt-doc-tools 兩台 nginx）每一層都要設</strong>，一層用預設整鏈就斷": "<strong>With several layers of nginx (a self-hosted LLM proxy plus jt-doc-tools) every layer needs it</strong>; one layer left at the default breaks the whole chain",
  "<strong>時間戳記是選用的</strong>：純文字逐字稿沒有時間時，摘要 / 決議 / 待辦照常有， 但 <code>speaker_stats</code> 會是空的、<code>charts</code> 不會包含 <code>speaker_share</code> 與 <code>timeline</code> —— 那兩個要靠時間算，猜出來的數字不能用。": "<strong>Timestamps are optional</strong>: for a plain-text transcript with no times, the summary, decisions and action items still work, but <code>speaker_stats</code> comes back empty and <code>charts</code> will not contain <code>speaker_share</code> or <code>timeline</code>; both are computed from the times, and a guessed number would be worse than none.",
  "<strong>會議背景</strong>（選填，上限 4000 字）：主題、與會者與職稱、專有名詞說明，或你自己要交代的話": "<strong>Meeting background</strong> (optional, up to 4000 characters): the topic, attendees and their roles, terminology, or anything else you want to tell the model",
  "<strong>每一份都偵測不到內容區塊時回 <code>422</code></strong>（例如整張全白、或掃描時蓋子沒關的純黑）。 那不是請求格式的問題，而是「送進來的圖裡沒有東西可以拼」—— 訊息會說清楚是哪一種。": "<strong>Returns <code>422</code> when none of the files has a detectable content block</strong> (an all-white page, say, or an all-black one from scanning with the lid open). That is not a problem with the request format: there is nothing in the images to assemble, and the message says which case it is.",
  "<strong>每頁獨立位置</strong>（選用）。JSON 陣列，每個物件自帶頁碼與座標 → 不同頁可放不同位置、<strong>同一頁可放多個</strong>。提供時會忽略 <code>x_mm</code> / <code>y_mm</code> / <code>width_mm</code> / <code>height_mm</code> / <code>rotation_deg</code> / <code>page_mode</code> / <code>pages_json</code>；<strong>未提供則行為與舊版完全相同</strong>": "<strong>Per-page positions</strong> (optional). A JSON array in which each object carries its own page number and coordinates → different pages can use different positions, and <strong>one page can hold several</strong>. When present it overrides <code>x_mm</code> / <code>y_mm</code> / <code>width_mm</code> / <code>height_mm</code> / <code>rotation_deg</code> / <code>page_mode</code> / <code>pages_json</code>; <strong>when absent the behaviour is exactly as before</strong>",
  "<strong>每頁獨立位置（placements）</strong>：適合多頁合約 / 續保單這種「每頁簽名位置不同、同一頁要簽好幾處」的情境。": "<strong>Per-page positions (placements)</strong>: suited to multi-page contracts and renewals, where each page signs in a different spot and one page may need several signatures.",
  "<strong>角度是整個章先轉好才切片</strong> —— 反過來（先切再各自旋轉）接縫會對不起來。<strong>同一組內的片位置與角度完全一致</strong>，否則拼不回去。": "<strong>The whole stamp is rotated before it is sliced</strong>; the other way round (slice first, rotate each piece) leaves seams that do not line up. <strong>Within one group the position and angle of every slice are identical</strong>, otherwise the pieces do not fit back together.",
  "<strong>認不出來的檔案不會讓整批失敗</strong> —— 成功的進 <code>entries</code>，失敗的逐檔列在 <code>failed</code> （HTTP 仍是 200）。要判斷有沒有漏，看 <code>failed</code> 是不是空的，不要只看 HTTP 狀態。 超過 200 個檔案回 <code>400</code>；完全沒帶 <code>files</code> 回 <code>422</code>。": "<strong>A file that cannot be recognised does not fail the whole batch</strong>; successes go into <code>entries</code> and each failure is listed in <code>failed</code> (the HTTP status is still 200). To tell whether anything was missed, check whether <code>failed</code> is empty rather than going by the HTTP status. More than 200 files returns <code>400</code>; no <code>files</code> at all returns <code>422</code>.",
  "<strong>這支是同步的</strong> —— 一場兩小時的會議要跑幾分鐘（約 30~60 次模型請求）， 呼叫端的逾時要放寬。要背景處理請走網頁那條路 （<code>POST /upload</code> → <code>POST /start</code> → 拿作業編號輪詢 <code>/api/jobs/{id}</code>）。": "<strong>This endpoint is synchronous</strong>; a two-hour meeting takes several minutes (roughly 30-60 model requests), so raise the caller's timeout. For background processing use the web path instead (<code>POST /upload</code> to <code>POST /start</code>, then poll the job id with <code>/api/jobs/{id}</code>).",
  "API 介面遵循 SemVer：minor 版本（如 1.4.x → 1.5.x）保證<strong>後相容</strong>；major 版本（1.x → 2.x）才會 breaking。新加 endpoint 不算 breaking。": "The API follows SemVer: a minor release (1.4.x → 1.5.x) is guaranteed <strong>backwards compatible</strong>; only a major release (1.x → 2.x) may break. Adding an endpoint is not a breaking change.",
  "API 使用手冊": "API manual",
  "API 使用手冊 — Jason Tools 文件工具箱": "API manual; Jason Tools Document Toolbox",
  "API 手冊": "API manual",
  "Body（JSON）：": "Body (JSON):",
  "Body（JSON）：<code>{&quot;vat&quot;: &quot;12345678&quot;}</code>": "Body (JSON): <code>{&quot;vat&quot;: &quot;12345678&quot;}</code>",
  "Body（JSON）：<code>{&quot;vats&quot;: [&quot;12345678&quot;, &quot;23456789&quot;]}</code>": "Body (JSON): <code>{&quot;vats&quot;: [&quot;12345678&quot;, &quot;23456789&quot;]}</code>",
  "CLI 管理，與 web 認證 (": "Managed from the CLI, entirely separate from web authentication (",
  "Form（multipart）：": "Form (multipart):",
  "GitLab CI / GitHub Actions：把 Word 文件自動轉 PDF": "GitLab CI / GitHub Actions: convert Word documents to PDF automatically",
  "Impress 模組": "Impress module",
  "JSON body。管理頁的「測試連線」按鈕會把<strong>還沒存檔</strong>的那組設定帶進來； 命令列只想確認「現在這組通不通」時整個 body 都可以省略。": "A JSON body. The admin page’s \"Test connection\" button sends the settings that are <strong>not saved yet</strong>; from the command line, when you only want to know whether the current settings work, the whole body can be omitted.",
  "JSON（除非明確回 PDF / PNG / ZIP 二進位資料）": "JSON (unless it explicitly returns PDF / PNG / ZIP binary data)",
  "Jason Tools 文件工具箱": "Jason Tools Document Toolbox",
  "Jason Tools 文件工具箱 API 使用手冊：所有工具的 REST API 端點、認證方式、請求／回應格式與整合範例（cURL / Python / Node.js / CI）。": "Jason Tools Document Toolbox API manual: REST endpoints for every tool, authentication, request and response formats, and integration examples (cURL / Python / Node.js / CI).",
  "Jason Tools 文件工具箱對外提供 RESTful API，所有工具皆有對應 endpoint，可整合到自動化流程、自家系統或排程工作。": "Jason Tools Document Toolbox exposes a RESTful API. Every tool has an endpoint, ready to plug into automation, your own systems or scheduled jobs.",
  "LLM 校驗（pdf-fill）": "LLM verification (pdf-fill)",
  "Markdown 檔。與 <code>text</code> 擇一": "A Markdown file. Use this or <code>text</code>",
  "Markdown 轉辦公文件": "Markdown to office document",
  "Node.js：逐句翻譯": "Node.js: sentence translation",
  "OCR 語言包 —— 切換啟用的語言": "OCR language packs: switch the active language",
  "OCR 語言包 —— 切換辨識品質": "OCR language packs: switch recognition quality",
  "OCR 語言包 —— 切換預設引擎": "OCR language packs: switch the default engine",
  "PDF / Office 整合式文件處理平台：不上雲，資料留在自己手中。": "An all-in-one PDF and Office document platform: no cloud, your data stays with you.",
  "PDF / Word 文件": "PDF / Word document",
  "PDF 中繼資料": "PDF metadata",
  "PDF 分割": "Split PDF",
  "PDF 加密": "Encrypt PDF",
  "PDF 加頁碼": "Add page numbers",
  "PDF 合併": "Merge PDFs",
  "PDF 圖片擷取": "Extract PDF images",
  "PDF 壓縮": "Compress PDF",
  "PDF 多頁合一（N-up）": "PDF N-up",
  "PDF 字數統計": "PDF word count",
  "PDF 或文書檔": "PDF or word processing file",
  "PDF 或文書檔，<strong>至少 2 頁</strong>": "A PDF or office document, <strong>at least 2 pages</strong>",
  "PDF 文字擷取": "Extract PDF text",
  "PDF 旋轉": "Rotate PDF",
  "PDF 書籤與目錄": "PDF bookmarks and contents",
  "PDF 浮水印": "PDF watermark",
  "PDF 用印 / 簽名": "Stamp / sign a PDF",
  "PDF 編輯器": "PDF editor",
  "PDF 表單填寫": "Fill a PDF form",
  "PDF 解密": "Decrypt PDF",
  "PDF 註解平面化": "Flatten PDF annotations",
  "PDF 註解整理": "PDF annotation report",
  "PDF 註解清除": "Remove PDF annotations",
  "PDF 轉 Markdown": "PDF to Markdown",
  "PDF 轉 Office": "PDF to Office",
  "PDF 轉圖片": "PDF to images",
  "PDF 轉簡報": "PDF to slides",
  "PDF 附件擷取": "Extract PDF attachments",
  "PDF 隱藏內容掃描": "PDF hidden content scan",
  "PDF 頁面加框": "PDF page borders",
  "PDF 頁面尺寸統一": "Unify PDF page size",
  "PDF 頁面處理": "PDF page operations",
  "PDF 騎縫章": "PDF seam stamp",
  "PDF、圖片（含手機拍的 HEIC）或文書檔": "PDF, an image (including HEIC from a phone) or an office document",
  "PDF、辦公文件（.doc / .docx / .odt / .xls / .xlsx / .ods / .ppt / .pptx / .odp）或純文字（.txt / .md / .csv / .log / .json / .xml / .html）": "A PDF, an office document (.doc / .docx / .odt / .xls / .xlsx / .ods / .ppt / .pptx / .odp) or plain text (.txt / .md / .csv / .log / .json / .xml / .html)",
  "PDF（已填好欄位，準備校驗）": "PDF (fields already filled, ready for verification)",
  "POST / PUT 參數：<code>name</code>、<code>tax_id</code>、<code>address</code>、<code>aliases</code>、<code>type</code>、<code>note</code>。": "POST / PUT parameters: <code>name</code>, <code>tax_id</code>, <code>address</code>, <code>aliases</code>, <code>type</code>, <code>note</code>.",
  "Python 客戶端：批次清掉 PDF 註解": "Python client: clear PDF annotations in bulk",
  "Shell：監看 job 完成後下載": "Shell: watch for a job to finish and download it",
  "Token 管理": "Token management",
  "Token 透過 admin /": "Tokens are managed from admin /",
  "Word 2007 寫法": "Word 2007 format",
  "Word 2010–365 寫法": "Word 2010; 365 format",
  "file（可多個）": "file (repeatable)",
  "job 完成後 <code>GET /api/jobs/{job_id}</code> 的 <code>meta.stats</code> 內含 <code>pages</code>（投影片張數）、 <code>images</code>、<code>objects</code>（物件總數）。": "Once the job finishes, <code>meta.stats</code> in <code>GET /api/jobs/{job_id}</code> contains <code>pages</code> (the number of slides), <code>images</code> and <code>objects</code> (the total object count).",
  "mode=ranges 時的範圍，例 <code>1-3,5,7-9</code>；mode=every 時填數字 N": "The range when mode=ranges, e.g. <code>1-3,5,7-9</code>; a number N when mode=every",
  "overlay 物件模型 JSON 字串": "Overlay object model as a JSON string",
  "placement 欄位": "placement fields",
  "side 模式貼哪一邊：<code>right</code>（預設）/ <code>left</code>": "Which edge to place it on in side mode: <code>right</code> (default) or <code>left</code>",
  "—— 每一條抽出來的內容 都要在它宣稱的段落裡找得到，找不到就不留。這個數字偏高時代表模型在編， 換一個模型會比調參數有效。": "Everything extracted has to be findable in the segment it claims; if it is not, it is not kept. A high number here means the model is inventing, and changing the model helps more than tuning parameters.",
  "、IP 用": ", IP addresses use",
  "、MAC 用": ", MAC addresses use",
  "。其他值回 400": ". Any other value returns 400",
  "「我的工作區」是綁定登入 session 的網頁功能（cookie 認證），<strong>不屬於對外 Bearer API</strong>，因為每個檔案以登入者帳號隔離。需 admin 在「工作區設定」啟用；停用時下列端點一律回 404。": "\"My workspace\" is a web feature tied to the signed-in session (cookie authentication) and is <strong>not part of the public Bearer API</strong>, because each file is isolated by account. An administrator must enable it in \"workspace settings\"; while it is disabled every endpoint below returns 404.",
  "「我的工作區」頁面": "The “My workspace” page",
  "一個印章切成數片蓋在連續頁面上 —— 任何一頁被抽換或掉頁，那一片就對不起來。收 PDF 與文書檔。": "Split one stamp across consecutive pages; if any page is swapped or lost, that slice no longer lines up. Accepts PDF and office files.",
  "一個章跨幾頁，預設 <code>2</code>；<code>0</code> = 整份文件一個章": "How many pages one stamp spans, default <code>2</code>; <code>0</code> = one stamp for the whole document",
  "一或多個 PDF / 文書檔。多檔時依順序串接": "One or more PDF / office files; several are joined in order",
  "一次查多筆統編。": "Look up several tax IDs at once.",
  "上一頁": "Previous",
  "上下位置（mm），<code>0</code> = 垂直置中": "Vertical position (mm), <code>0</code> = centred",
  "上傳檔案：<code>multipart/form-data</code>；JSON：<code>application/json</code>": "File uploads: <code>multipart/form-data</code>; JSON: <code>application/json</code>",
  "上限 30 秒（管理頁不能卡住）": "Capped at 30 seconds, so the admin page cannot hang",
  "上面那個端點是<strong>同步</strong>的：整份翻完才回應，句數一多就會撞到反向代理的逾時。 幾百句以上請改用背景作業 —— 送出後立刻拿到作業編號，翻譯在伺服器繼續跑， <strong>呼叫端可以離線</strong>，之後再回來查進度與結果。": "The endpoint above is <strong>synchronous</strong>: it responds only when the whole document is done, so a large number of sentences will hit the reverse proxy's timeout. For a few hundred sentences or more, use the background job instead: you get a job id straight away, translation carries on the server, <strong>the caller can go offline</strong>, and progress and results can be fetched later.",
  "上面那次比對回的識別碼": "the id returned by the comparison above",
  "下一頁": "Next",
  "下載 <code>GET /api/jobs/{job_id}/download</code> 取結果（單檔 PDF / 多檔 ZIP）": "Download the result with <code>GET /api/jobs/{job_id}/download</code> (a single PDF, or a ZIP for several files)",
  "下載 job 結果": "Download a job result",
  "下載 job 結果為 PNG": "Download a job result as PNG",
  "下面這些是<strong>管理頁面自己呼叫的端點</strong>，不是穩定的對外介面：它們的參數與回傳 會跟著頁面改，<strong>不保證相容</strong>。列出來的目的是「這份文件不漏掉任何端點」， 需要自動化的話請優先用上面那些有明確契約的 API。": "These are <strong>endpoints the admin pages call themselves</strong>, not a stable public interface: their parameters and responses change along with the pages, so <strong>no compatibility is promised</strong>. They are listed so that this document covers every endpoint; for automation, prefer the APIs above, which have a defined contract.",
  "不啟用認證（預設）": "Authentication disabled (default)",
  "不收 PDF": "PDF is not accepted",
  "不是只有名稱：": "is more than just a name:",
  "不透明度 <code>0</code>~<code>1</code>，預設 <code>1</code>": "Opacity <code>0</code>; <code>1</code>, default <code>1</code>",
  "之後所有 <code>/api/*</code> 必須帶以下任一形式：": "From then on every <code>/api/*</code> call must carry one of these:",
  "之後身分證 / 統編 / 信用卡會算出正確的檢查碼（拿去測試系統不會被擋， 但算得出來的號碼有可能剛好是某個真人的）。Email 用": "makes ID numbers, tax IDs and credit cards carry a correct check digit (so a test system will not reject them; but a number that computes correctly could happen to belong to a real person). Email addresses use",
  "乘車證明 PDF，一次最多 200 個": "Travel receipt PDFs, up to 200 at a time",
  "乘車證明整理": "Travel receipts",
  "亂數種子；<code>0</code> = 隨機": "Random seed; <code>0</code> = random",
  "介紹網站": "Introduction site",
  "以下每個端點都附": "Each endpoint below includes",
  "企業 logo 狀態": "Company logo status",
  "位置（mm）": "Position (mm)",
  "作業佇列 —— 優先派送名單（<strong>順序就是優先序</strong>）": "Job queue: priority dispatch list (<strong>the order is the priority</strong>)",
  "作業佇列 —— 取消一件作業": "Job queue: cancel one job",
  "作業佇列 —— 指定優先使用者時的搜尋框": "Job queue: user search box for picking priority users",
  "作業佇列 —— 暫停 / 恢復派送": "Job queue: pause or resume dispatching",
  "作業佇列 —— 最大同時作業數": "Job queue: maximum concurrent jobs",
  "作業佇列 —— 歷史紀錄": "Job queue: history",
  "作業佇列 —— 目前的作業清單": "Job queue: current jobs",
  "作者": "Author",
  "依 8 位統一編號反查公司 / 機關名稱、地址、行業類別。": "Look up a company or agency name, address and industry by 8-digit tax ID.",
  "依 overlay JSON 模型把文字 / 圖片 / 形狀 / 遮罩燒進 PDF（含真刪 redaction）。": "Burn text, images, shapes and masks into a PDF from an overlay JSON model (including genuine redaction).",
  "依字級自動偵測標題，預設 <code>false</code>（只在沒有 <code>bookmarks</code> 且單檔時有作用）": "Detect headings automatically from the font size, default <code>false</code> (only applies with no <code>bookmarks</code> and a single file)",
  "依頁數 / 範圍切分 PDF。": "Split a PDF by page count or ranges.",
  "偵測紙張邊界做透視校正（預設開）。抓不到時自動退回只做拉正": "Detect the sheet edges and correct the perspective (on by default). Falls back to straightening only when the edges cannot be found",
  "僅 <code>pdf2docx-refine</code> 有效：是否套 jtdt-refine 後處理（25 fixer），預設 <code>false</code>": "Only applies to <code>pdf2docx-refine</code>: whether to run the jtdt-refine post-processing (25 fixers), default <code>false</code>",
  "僅 <code>replace</code>：<code>1</code> = 產生可通過檢查碼的假值（見下方說明）": "<code>replace</code> only: <code>1</code> = generate fake values that pass their check digit (see below)",
  "僅 <code>replace</code>：JSON 物件 <code>{&quot;原值&quot;: &quot;指定的新值&quot;}</code>。沒指定的一律自動產生": "<code>replace</code> only: a JSON object <code>{&quot;original&quot;: &quot;the new value&quot;}</code>. Anything not listed is generated automatically",
  "允許列印": "Allow printing",
  "允許複製內容": "Allow copying content",
  "元素 +1），回傳": "element plus 1), returning",
  "內外雙框（獎狀 / 證書風格），預設 <code>false</code>": "Double border (certificate style), default <code>false</code>",
  "兩份以上 PDF": "two or more PDFs",
  "公司主檔 ID（admin 端建立）": "Company profile ID (created in the admin area)",
  "列出 / 抽出 PDF 內嵌附件（embedded files）。": "List or extract files embedded in a PDF.",
  "列出 PDF 所有註解（頁碼、類型、作者、內容、座標、時間）。": "List every annotation in a PDF (page, type, author, content, coordinates, time).",
  "列出所有印章 / 簽名 / Logo / 浮水印資產。": "List every stamp, signature, logo and watermark asset.",
  "列出自己的檔案（<code>?accept=pdf,png</code> 過濾）+ 容量 + 保留時數": "List your own files (filter with <code>?accept=pdf,png</code>) plus the quota and retention period",
  "列出資產": "List assets",
  "刪除 / 抽取 / 重排頁面。": "Delete, extract or reorder pages.",
  "刪除（<code>file_id</code>）": "Delete (<code>file_id</code>)",
  "刻意不列入 Bearer API": "are deliberately left out of the Bearer API",
  "刻意不通過檢查碼": "deliberately fails checksum validation",
  "前後對照縮圖與 pdf-to-office 相同：": "Before-and-after thumbnails work the same as for pdf-to-office:",
  "功能": "Features",
  "加使用者 / 擁有者密碼並設定權限。": "Add user and owner passwords and set permissions.",
  "加密演算法，例 <code>AES-256</code>": "Encryption algorithm, e.g. <code>AES-256</code>",
  "加密的 PDF": "The encrypted PDF",
  "加文字浮水印。": "Add a text watermark.",
  "勾選「Enforce — 沒帶 token 一律拒絕」並儲存": "Tick “Enforce; refuse anything without a token” and save",
  "印章 / 簽名圖（透明 PNG 佳）": "Stamp / signature image (a transparent PNG is best)",
  "印章顏色 hex，預設 <code>#c81414</code>": "Stamp colour as hex, default <code>#c81414</code>",
  "原 PDF": "The original PDF",
  "原始碼庫": "Source repository",
  "原本就是目標尺寸的頁面不動，預設 <code>true</code>": "Leave pages that are already the target size untouched, default <code>true</code>",
  "參數": "Parameter",
  "取檔（<code>?dl=1</code> 下載）": "Fetch a file (<code>?dl=1</code> to download)",
  "取檔）。 需 admin 啟用 LLM 服務（": "to fetch the file). Requires an administrator to enable the LLM service (",
  "取消請用共用的作業端點：<code>POST /api/jobs/{job_id}/cancel</code>。": "To cancel, use the shared job endpoint: <code>POST /api/jobs/{job_id}/cancel</code>.",
  "只回第 N 筆之後的資料 —— 幾萬句時不必每次拉全部。": "returns only the items after the Nth; with tens of thousands of sentences there is no need to fetch them all every time.",
  "只有一顆引擎（jtdt-layout 版面重現），因此<strong>沒有</strong> <code>engine</code> 參數。": "There is only one engine (jtdt-layout layout reproduction), so there is <strong>no</strong> <code>engine</code> parameter.",
  "只清這些作者 CSV，空白 = 全清": "Only these authors, as CSV; blank means all",
  "只清這些類型 CSV（<code>Highlight</code> / <code>Text</code> / <code>FreeText</code> ...），空白 = 全清": "Remove only these types, as CSV (<code>Highlight</code> / <code>Text</code> / <code>FreeText</code> …); blank = remove everything",
  "只跑第一輪，快但漏抓與誤抓都會多": "first pass only: faster, but both misses and false positives go up",
  "各圖旋轉角度 CSV（對應上傳順序），例 <code>0,90,0</code>": "Rotation for each image as CSV, in upload order, e.g. <code>0,90,0</code>",
  "各有兩個目標可選，差別是": "each offer two targets; the difference is the",
  "同一次請求裡的檔案<strong>必須同屬一類</strong>（不能把試算表和文書檔混在一起送）， 而且要與 <code>target</code> 所屬的類一致，否則回 400。": "Files in one request <strong>must all be of the same kind</strong> (a spreadsheet and a word-processing file cannot be mixed) and must match the kind that <code>target</code> belongs to, otherwise the call returns 400.",
  "同一類文件之間互轉格式：文書檔↔文書檔、試算表↔試算表、簡報↔簡報。走 job 模式回 <code>job_id</code>。": "Converts between formats of the same kind: word processing ↔ word processing, spreadsheet ↔ spreadsheet, presentation ↔ presentation. Job mode; returns <code>job_id</code>.",
  "同上，不給就用已存檔的": "Same; omit it and the saved key is used",
  "同上，但用 GET + path 參數。": "The same, but as GET with a path parameter.",
  "同步": "synchronous",
  "含發票 QR Code 的圖片 / PDF": "Image or PDF containing the invoice QR code",
  "呼叫對應工具的提交 endpoint → 拿到 <code>{&quot;job_id&quot;: &quot;...&quot;}</code>": "Call the tool's submit endpoint → receive <code>{&quot;job_id&quot;: &quot;...&quot;}</code>",
  "啟用 API token": "Enabling API tokens",
  "啟用後 <code>/tools/pdf-ocr/*</code> 與內部呼叫 EasyOCR 的工具會自動走遠端 GPU，連線失敗自動退回本機。<code>jt-ocr-server</code> 自身對外端點：": "Once enabled, <code>/tools/pdf-ocr/*</code> and any tool that calls EasyOCR internally use the remote GPU, falling back to the local machine if the connection fails. <code>jt-ocr-server</code>'s own endpoints:",
  "回 <code>image/png</code>（150 dpi）。Office 檔回的是<strong>轉成 PDF 之後</strong>的版面。 檔案過期會回 <code>410</code>；<code>uid</code> / <code>slot</code> / <code>page</code> 不合法一律 <code>404</code>。": "Returns <code>image/png</code> (150 dpi). For office files this is the layout <strong>after conversion to PDF</strong>. An expired file returns <code>410</code>; an invalid <code>uid</code> / <code>slot</code> / <code>page</code> always returns <code>404</code>.",
  "回報問題": "Report an issue",
  "回應 JSON（節錄）：": "JSON response (excerpt):",
  "回應 JSON：": "JSON response:",
  "回應 JSON：<code>{&quot;available&quot;: true/false, ...}</code>。": "Response JSON: <code>{&quot;available&quot;: true/false, ...}</code>.",
  "回應 JSON：<code>{&quot;lines&quot;: [...], &quot;count&quot;: N, &quot;original_count&quot;: M, ...}</code>。": "Response JSON: <code>{&quot;lines&quot;: [...], &quot;count&quot;: N, &quot;original_count&quot;: M, ...}</code>.",
  "回應 JSON：公司名稱 / 地址 / 行業等。找不到回 <code>404</code>。": "Response JSON: company name, address, industry and so on. Not found returns <code>404</code>.",
  "回應 JSON：各類隱藏內容的偵測結果。": "JSON response: what was found in each category.",
  "回應 JSON：同單筆查詢。": "JSON response: the same as the single lookup.",
  "回應 JSON：實體清單 / 操作結果。": "JSON response: the entity list or the result of the operation.",
  "回應 JSON：每段差異的類型（<code>equal</code> / <code>insert</code> / <code>delete</code> / <code>replace</code>）與內容。": "Response JSON: the type of each difference (<code>equal</code> / <code>insert</code> / <code>delete</code> / <code>replace</code>) and its content.",
  "回應 JSON：每筆查詢結果陣列。": "JSON response: an array of results.",
  "回應 JSON：每筆註解的詳細資訊。": "JSON response: full detail for each annotation.",
  "回應 JSON：發票號碼、日期、金額、賣方統編等。": "JSON response: invoice number, date, amount, seller tax ID and so on.",
  "回應 JSON：逐段差異結構。每一頁除了 <code>diff</code>（逐行的文字差異）之外還帶著：": "Response JSON: the per-segment diff structure. Besides <code>diff</code> (the line-level text differences), each page also carries:",
  "回應 JSON：逐頁文字 + 全文。": "JSON response: text per page plus the whole document.",
  "回應 JSON：遮蔽後文字 + 命中的 PII 清單。": "JSON response: the masked text plus the list of PII found.",
  "回應是<strong>翻譯後的檔案本身</strong>（<code>Content-Disposition: attachment</code>），格式與上傳的相同。": "The response is <strong>the translated file itself</strong> (<code>Content-Disposition: attachment</code>), in the same format as the upload.",
  "回應格式": "Response format",
  "回應：": "Response:",
  "回應：<code>{&quot;job_id&quot;: &quot;...&quot;, &quot;download_url&quot;: &quot;...&quot;}</code>。單檔回原格式，多檔回 ZIP。 失敗 4xx + JSON <code>{&quot;detail&quot;: &quot;...&quot;}</code>。": "Response: <code>{&quot;job_id&quot;: &quot;...&quot;, &quot;download_url&quot;: &quot;...&quot;}</code>. One file comes back in the target format, several come back as a ZIP. On failure, 4xx plus JSON <code>{&quot;detail&quot;: &quot;...&quot;}</code>.",
  "回應：<code>{&quot;job_id&quot;: &quot;...&quot;, &quot;download_url&quot;: &quot;...&quot;}</code>；之後用第 10 章的 job API 輪詢 + 取結果。": "Response: <code>{&quot;job_id&quot;: &quot;...&quot;, &quot;download_url&quot;: &quot;...&quot;}</code>; then poll and fetch with the job API in chapter 10.",
  "回應：<code>{&quot;job_id&quot;: &quot;...&quot;}</code>。": "Response: <code>{&quot;job_id&quot;: &quot;...&quot;}</code>.",
  "回應：N-up 後的 PDF。": "Response: the N-up PDF.",
  "回應：PDF 二進位。": "Response: PDF binary.",
  "回應：PDF 二進位（<code>application/pdf</code>）。失敗 4xx + JSON <code>{&quot;detail&quot;: &quot;...&quot;}</code>。": "Response: the PDF itself (<code>application/pdf</code>). On failure, 4xx plus JSON <code>{&quot;detail&quot;: &quot;...&quot;}</code>.",
  "回應：切出的 PDF（多份打包 ZIP）。": "Response: the resulting PDFs (several come back as a ZIP).",
  "回應：加上文字層的 PDF。": "Response: the PDF with a text layer.",
  "回應：加好書籤的 PDF。": "Response: the bookmarked PDF.",
  "回應：加完框線的 PDF。所有數值都會在伺服器端夾在安全範圍內。": "Response: the bordered PDF. Every value is clamped to a safe range on the server.",
  "回應：加完頁碼的 PDF。": "Response: the numbered PDF.",
  "回應：加密後的 PDF。": "Response: the encrypted PDF.",
  "回應：加浮水印後的 PDF。": "Response: the watermarked PDF.",
  "回應：去識別化後的檔案。": "Response: the redacted file.",
  "回應：合併後的 PDF。": "Response: the merged PDF.",
  "回應：單張 A4 白底 PDF 二進位。": "Response: a single white A4 PDF binary.",
  "回應：單頁回 PNG，多頁回 ZIP。": "Response: a PNG for one page, a ZIP for several.",
  "回應：圖片打包 ZIP。": "Response: the images as a ZIP.",
  "回應：填好的 PDF。": "Response: the filled PDF.",
  "回應：壓縮後的 PDF。": "Response: the compressed PDF.",
  "回應：套用 overlay 後的 PDF。模型格式詳見 web UI 的 pdf-editor。": "Response: the PDF with the overlay applied. The model format is documented in the web UI's pdf-editor.",
  "回應：平面化後的 PDF。": "Response: the flattened PDF.",
  "回應：把 job 的 PDF 結果 render 成 PNG（多頁 / 多檔自動打 ZIP）。": "Response: the job's PDF result rendered as PNG (several pages or files come back as a ZIP).",
  "回應：旋轉後的 PDF。": "Response: the rotated PDF.",
  "回應：未帶 clear_* 旗標 → 回 metadata JSON；帶旗標 → 回清除後的 PDF。": "Response: without any clear_* flag it returns the metadata as JSON; with a flag it returns the cleaned PDF.",
  "回應：清除後的 PDF。": "Response: the cleaned PDF.",
  "回應：結果檔（PDF / ZIP）。未完成（<code>status != completed</code>）回 <code>409</code>。": "Response: the result file (PDF / ZIP). If it is not finished (<code>status != completed</code>) the call returns <code>409</code>.",
  "回應：統一尺寸後的 PDF。": "Response: the resized PDF.",
  "回應：蓋好騎縫章的 PDF。": "Response: the PDF with the seam stamp.",
  "回應：蓋章後的 PDF。": "Response: the stamped PDF.",
  "回應：處理後的 PDF。": "Response: the processed PDF.",
  "回應：解密後的 PDF。": "Response: the decrypted PDF.",
  "回應：附件清單 + 內容（ZIP）。": "Response: the attachment list plus contents (ZIP).",
  "圓角半徑（mm），預設 <code>0</code>（直角）": "Corner radius (mm), default <code>0</code> (square corners)",
  "圖片格式 <code>png</code>（預設）/ <code>jpg</code>，僅 <code>include_images=true</code> 時有效": "Image format <code>png</code> (default) or <code>jpg</code>; only applies when <code>include_images=true</code>",
  "圖片轉 PDF": "Images to PDF",
  "在 PDF 上疊加印章 / 簽名圖。": "Overlay a stamp or signature image on a PDF.",
  "在頁面指定位置加頁碼。": "Add page numbers at a chosen position.",
  "基本原則": "Basic principle",
  "填寫字型 ID": "Font ID for the filled text",
  "外側陰影，預設 <code>false</code>": "Outer shadow, default <code>false</code>",
  "外部 GPU OCR Server": "External GPU OCR server",
  "大型 / 耗時操作走 <strong>job 模式</strong>：先回 <code>{&quot;job_id&quot;: &quot;...&quot;}</code>，再用 <code>/api/jobs/{job_id}</code> 輪詢，完成後 <code>/api/jobs/{job_id}/download</code> 取結果": "Large or slow operations use <strong>job mode</strong>: the call first returns <code>{&quot;job_id&quot;: &quot;...&quot;}</code>, you poll <code>/api/jobs/{job_id}</code>, and once it is done you fetch the result from <code>/api/jobs/{job_id}/download</code>",
  "大檔處理": "Large files",
  "大量句子：背景作業版": "Many sentences: the background-job version",
  "套用頁碼，例 <code>1,3-5</code>；空白 = 全部": "Pages to apply it to, e.g. <code>1,3-5</code>; blank = all",
  "如有公開暴露需求，建議加上 <code>limit_req_zone</code> 防濫用": "If it needs to be exposed publicly, add <code>limit_req_zone</code> against abuse",
  "字型，預設 <code>default</code>": "Font, default <code>default</code>",
  "字級（pt）": "Font size (pt)",
  "字級，預設 <code>10</code>": "Font size, default <code>10</code>",
  "存檔：<code>job_id</code>（伺服器端複製 job 結果）或 <code>file</code>（直接上傳 bytes）；僅 PDF / PNG": "Save a file: <code>job_id</code> (the server copies the job result) or <code>file</code> (upload the bytes directly); PDF / PNG only",
  "存檔；回傳 JSON 的端點可接": "to save it; endpoints that return JSON can be piped through",
  "存自己的通知設定": "Save your own notification settings",
  "安裝": "Install",
  "安裝與升級疑難排解": "Install & upgrade troubleshooting",
  "完整變更紀錄見 <a href=\"https://github.com/jasoncheng7115/jt-doc-tools/blob/main/CHANGELOG.md\" target=\"_blank\" rel=\"noopener\">CHANGELOG.md</a>。": "The full change log is in <a href=\"https://github.com/jasoncheng7115/jt-doc-tools/blob/main/CHANGELOG.md\" target=\"_blank\" rel=\"noopener\">CHANGELOG.md</a>.",
  "容量額度、單檔上限、保留時數、啟用 / 停用由 admin 在 <code>/admin/workspace</code> 設定（全站統一，無個人特例）。": "The quota, per-file limit, retention period and the on/off switch are set by an administrator at <code>/admin/workspace</code> (site-wide, with no individual exceptions).",
  "審查輪數，預設讀 admin 設定": "Number of review rounds; defaults to the admin setting",
  "寬度（mm）": "Width (mm)",
  "對 Word / PDF 文件偵測並遮蔽個資（regex / 可選 LLM）。": "Detect and mask personal data in Word and PDF documents (regex, with an optional LLM).",
  "對應的管理頁": "Admin page",
  "對掃描 PDF 做文字辨識，加上可選取的文字層。": "Run OCR on a scanned PDF and add a selectable text layer.",
  "對文字清單做去重、排序、計數、集合運算等管線處理。": "Run a list of text through a pipeline of deduplication, sorting, counting and set operations.",
  "對純文字偵測並遮蔽個資。": "Detect and mask personal data in plain text.",
  "尺寸（mm），預設 30×30": "Size (mm), default 30×30",
  "展開選單": "Open menu",
  "左下角 X 位置（mm）": "X position of the bottom-left corner (mm)",
  "左下角 Y 位置（mm）": "Y position of the bottom-left corner (mm)",
  "已切好的句子陣列（自己切，或先用 <code>/extract-text</code>）": "An array of sentences you have already split (yourself, or with <code>/extract-text</code>)",
  "已有文字層的頁略過，預設 <code>true</code>": "Skip pages that already have a text layer, default <code>true</code>",
  "引用對不上原文而被丟掉的項目數": "the number of entries dropped because their citation did not match the transcript",
  "待填的 PDF 表單": "The PDF form to fill",
  "待翻譯文字": "Text to translate",
  "待處理文字": "The text to process",
  "從 1 開始": "1-based",
  "必填": "Required",
  "或 query string：": "or as a query string:",
  "所以呼叫端可以邊跑邊顯示。": "so the caller can display results as they arrive.",
  "找不到符合的項目": "No matches",
  "把 Markdown 轉成 PDF / Word / OpenDocument。內容可用 <code>file</code> 上傳，也可以直接用 <code>text</code> 帶進來。": "Turns Markdown into PDF / Word / OpenDocument. The content can be uploaded as <code>file</code> or passed directly as <code>text</code>.",
  "把 PDF 反轉成 PowerPoint（.pptx）或 OpenDocument 簡報（.odp），<strong>一頁對一張投影片</strong>， 投影片尺寸沿用原稿（直向 PDF 也照樣還原）。走 job 模式回 <code>job_id</code>。": "Turns a PDF back into PowerPoint (.pptx) or an OpenDocument presentation (.odp), <strong>one page per slide</strong>, keeping the original slide size (portrait PDFs are reproduced too). Job mode; returns <code>job_id</code>.",
  "把 PDF 反轉成 Word（.docx）或 OpenDocument（.odt）。走 job 模式回 <code>job_id</code>。": "Turns a PDF back into Word (.docx) or OpenDocument (.odt). Job mode; returns <code>job_id</code>.",
  "把 PDF 抽成 Markdown。單次呼叫直接回內容，不走 job 模式。": "Extract a PDF as Markdown. A single call returns the content directly, without job mode.",
  "把 PDF 每頁轉成 PNG（多頁自動打包 ZIP）。": "Turn every page of a PDF into a PNG (several pages come back as a ZIP).",
  "把 辦公文件 轉成 PDF（走 OxOffice / LibreOffice 引擎）。": "Convert an office document to PDF (via the OxOffice / LibreOffice engine).",
  "把一張或多張圖片合併成單一 PDF。": "Combine one or more images into a single PDF.",
  "把偵測到的資料換成另一個看起來正常、但不是真的值 —— 適合拿去測試系統、 給外部看的報表、教學範例。原文一樣是<strong>真的刪除</strong>。": "Replaces detected data with something that looks normal but is not real: useful for testing a system, reports that go outside, and teaching examples. The original is still <strong>genuinely deleted</strong>.",
  "把多份 PDF 依上傳順序合併為一份。": "Merge several PDFs into one, in upload order.",
  "把多張掃描（如證件正、反面）中「有內容的區塊」自動偵測出來、保留原彩色，依其在原掃描中的相對位置合成到同一張 A4 白底 PDF。重疊時保留原位置不自動重排（需拖曳微調請改用網頁介面）。": "Detect the areas with content in several scans (both sides of an ID, say), keep their original colour and compose them onto one white A4 PDF at their relative positions. Overlaps keep their original position rather than being rearranged (use the web interface if you need to drag them).",
  "把多頁縮排到單頁（2-up / 4-up 等）。": "Fit several pages onto one sheet (2-up, 4-up …).",
  "把拍歪、掃歪的文件裁掉黑邊、拉正、去除不勻的底色。手機翻拍時會抓出紙張的 四個角做透視校正。<strong>完全不用 AI 也不用 GPU</strong>（傳統影像處理，CPU 約 0.8 秒／頁 @200 dpi）。": "Straightens documents that were photographed or scanned at an angle, trims dark borders and evens out uneven background. For phone photos it finds the four corners of the sheet and corrects the perspective. <strong>No AI and no GPU involved</strong> (classical image processing; about 0.8 s per page on CPU at 200 dpi).",
  "把整份辦公文件翻成另一種語言，<strong>回傳同格式、同版面的檔案</strong> —— 只換文字， 不重排版面。支援 <code>.doc</code> / <code>.docx</code> / <code>.odt</code>、<code>.xls</code> / <code>.xlsx</code> / <code>.ods</code>、 <code>.ppt</code> / <code>.pptx</code> / <code>.odp</code>。": "Translates a whole office document into another language and <strong>returns a file in the same format with the same layout</strong>; only the text changes, nothing is re-laid-out. Supports <code>.doc</code> / <code>.docx</code> / <code>.odt</code>, <code>.xls</code> / <code>.xlsx</code> / <code>.ods</code>, <code>.ppt</code> / <code>.pptx</code> / <code>.odp</code>.",
  "把會議逐字稿整理成摘要、決議、待辦、風險與章節，<strong>每一條都附段號</strong>。": "Turns a meeting transcript into a summary, decisions, action items, risks and chapters, <strong>with a segment number on every entry</strong>.",
  "把混合尺寸的頁面統一成同一種紙張。內容維持向量（文字仍選得到），不是轉成圖片。": "Bring mixed page sizes onto one paper size. The content stays vector (text is still selectable) rather than being turned into images.",
  "把註解燒進頁面內容流（收件方無法移除；表單欄位仍可填）。": "Burn annotations into the page content stream (the recipient cannot remove them; form fields stay fillable).",
  "抓 LLM 模型清單": "Fetch the LLM model list",
  "抽不到文字座標時（掃描件、文字被轉成外框）<code>marks</code> 會是空陣列 —— <strong>那不代表沒有差異</strong>，<code>diff</code> 那邊還是有的。": "When no text coordinates can be read (a scan, or text converted to outlines) <code>marks</code> is an empty array. <strong>That does not mean there are no differences</strong> — they are still in <code>diff</code>.",
  "抽出 PDF 內嵌的所有圖片。": "Extract every image embedded in a PDF.",
  "抽出 PDF 內所有文字（自動處理壞 CMap / OCR 雙層）。": "Extract all text from a PDF (broken CMaps and OCR double layers are handled automatically).",
  "指定頁面，例 <code>1,3,5-8</code>；留空 = 全部": "Specific pages, e.g. <code>1,3,5-8</code>; blank = all",
  "指定頁：JSON 陣列，0 起算頁碼（如 <code>[0,2,4]</code>）。提供時優先於 <code>page_mode</code>；超出範圍的頁碼會被忽略": "Specific pages: a JSON array of 0-based page numbers (such as <code>[0,2,4]</code>). When present it overrides <code>page_mode</code>; page numbers out of range are ignored",
  "掃描 PDF 內可能洩漏的隱藏內容（中繼資料、被遮蓋文字、圖層、附件等）。": "Scan a PDF for hidden content that could leak (metadata, covered text, layers, attachments …).",
  "掃描修正": "Scan cleanup",
  "掃描拼合": "Scan merge",
  "掃描檔，PDF / PNG / JPG / TIFF / WebP，各含一塊內容": "Scans; PDF / PNG / JPG / TIFF / WebP, one block of content each",
  "授權": "Licence",
  "插入目錄頁，預設 <code>false</code>。<strong>網頁介面的預設是勾起來的</strong>，API 維持 <code>false</code> 是為了不讓既有的自動化呼叫突然多出一頁": "Insert a table of contents page, default <code>false</code>. <strong>The web interface has this ticked by default</strong>, while the API keeps <code>false</code> so that existing automated calls do not suddenly gain a page",
  "搜尋 API 端點": "Search API endpoints",
  "搜尋工具 / 端點…": "Search tools / endpoints…",
  "改用某個共用資產的圖（預設用上傳的 <code>stamp_image</code>）": "Use an image from the shared assets instead (the uploaded <code>stamp_image</code> is used by default)",
  "文件去識別化": "Document redaction",
  "文件工具箱": "Document Toolbox",
  "文件差異比對": "Document compare",
  "文件標題": "Document title",
  "文件翻譯": "Document translation",
  "文字去識別化": "Text redaction",
  "文字差異比對": "Text compare",
  "文字顏色 hex": "Text colour as hex",
  "文字顏色 hex，例 <code>#000000</code>": "Text colour as hex, e.g. <code>#000000</code>",
  "新安裝預設不啟用認證，所有 <code>/api/*</code> 直接可用，無需 token。": "A new installation has authentication off, so every <code>/api/*</code> endpoint works straight away without a token.",
  "新版文件": "New document",
  "新版文字": "New text",
  "方法": "Method",
  "旋轉指定頁面。": "Rotate selected pages.",
  "旋轉角度，預設 <code>0</code>": "Rotation, default <code>0</code>",
  "旋轉角度，預設 <code>45</code>": "Rotation, default <code>45</code>",
  "是": "Yes",
  "是否一併輸出內嵌圖片，預設 <code>false</code>": "Whether to output the embedded images as well, default <code>false</code>",
  "是否把淡灰 / 微黃的掃描底色提亮成純白（不影響彩色內容），預設 <code>true</code>": "Whether to lift a pale grey or yellowish scan background to pure white (colour content is unaffected), default <code>true</code>",
  "時的寬高（mm）": "width and height (mm) when",
  "書籤的<strong>層級必須從 1 開始且一次只能加一層</strong>（1→3 會被自動修成 1→2），<strong>頁碼超出總頁數會被夾到最後一頁</strong> —— 兩者都會自動修正，不會失敗。插入目錄頁之後頁碼會自動往後移，不需要自己算 —— <strong><code>toc_at</code> 之前的書籤不會被動到</strong>（指向封面的那一筆仍然是第 1 頁）。": "Bookmark <strong>levels must start at 1 and may only go one level deeper at a time</strong> (1→3 is corrected to 1→2), and a <strong>page number beyond the end of the document is clamped to the last page</strong>; both are corrected automatically rather than failing. Page numbers shift on their own once a contents page is inserted, so there is nothing to work out by hand; <strong>bookmarks before <code>toc_at</code> are left untouched</strong> (the one pointing at the cover is still page 1).",
  "替 PDF 加書籤（閱讀器左側的導覽）與可點的目錄頁。<strong>傳多個檔案會自動串接，並以檔名建立第一層書籤</strong>，子文件原有的書籤降一層保留。收 PDF 與文書檔（文書檔會先自動轉成 PDF）。": "Adds bookmarks to a PDF (the navigation panel on the left of the reader) and a clickable table of contents page. <strong>Several files are joined automatically, with each file name becoming a first-level bookmark</strong>; bookmarks inside each document move one level down and are kept. Accepts PDFs and office documents (office documents are converted to PDF first).",
  "替換模式（<code>mode=replace</code>）": "Replacement mode (<code>mode=replace</code>)",
  "替每一頁加上框線。收 PDF 與文書檔（辦公文件；文書檔會先自動轉成 PDF），輸出一律是 PDF。": "Draw a border on every page. Accepts PDF and word processing files (office documents are converted to PDF first); the output is always PDF.",
  "會議摘要": "Meeting summary",
  "未帶或 token 無效 → <code>401 Unauthorized</code> JSON：": "Missing or invalid token → <code>401 Unauthorized</code> JSON:",
  "本節細項": "In this section",
  "查 QR Code 解碼後端（zbar）是否可用。": "Check whether the QR decoding backend (zbar) is available.",
  "查 job 狀態": "Check job status",
  "查詢的回應：": "The response to a poll:",
  "查進度，完成後到": "to check progress, then",
  "格式樣板，例 <code>第 {n} 頁</code> / <code>{n} / {total}</code>": "Format template, e.g. <code>Page {n}</code> / <code>{n} / {total}</code>",
  "框線顏色 hex，預設 <code>#333333</code>": "Border colour as hex, default <code>#333333</code>",
  "標記已讀（<code>id</code> 或 <code>all=1</code>）": "Mark as read ( <code>id</code> or <code>all=1</code> )",
  "檔案數（側欄徽章用）": "File count (for the sidebar badge)",
  "欄位": "Field",
  "權限密碼": "Permissions password",
  "歸屬與其他作業端點一致：非擁有者一律 <code>404</code>。": "Ownership works as it does for every other job endpoint: anyone who is not the owner gets <code>404</code>.",
  "每組角度亂數（上限 ±8 度），預設 <code>false</code>": "Randomise the angle of each group (up to ±8 degrees), default <code>false</code>",
  "每組高度亂數，預設 <code>false</code>": "Randomise the height of each group, default <code>false</code>",
  "每行一筆的清單文字": "List text, one item per line",
  "每頁列數": "Rows per sheet",
  "每頁欄數": "Columns per sheet",
  "比對兩份文件（PDF / Word）內容差異。": "Compare the contents of two documents (PDF / Word).",
  "比對兩段文字差異。": "Compare two pieces of text.",
  "沒有在 <code>replacements</code> 裡指定的值會<strong>自動產生</strong>，依欄位型別給對的樣子 （身分證、統編、手機、Email、信用卡、人名、地址、日期…），而且<strong>同一個原值 在整份文件裡固定對應同一個假值</strong>。": "Values not listed in <code>replacements</code> are <strong>generated automatically</strong>, shaped correctly for each field type (ID number, tax ID, mobile number, email address, credit card, personal name, address, date …), and <strong>the same original always maps to the same fake value throughout one document</strong>.",
  "浮水印文字": "Watermark text",
  "清單處理": "List tools",
  "清晰化：壓平不勻的底色與陰影（<strong>預設開</strong>）。實測手機翻拍的單邊硬陰影，文字辨識率 0.472 → 0.982；已經很平的掃描件開著也是零變動": "Clean up: flatten uneven background and shadows (<strong>on by default</strong>). Measured on a phone photo with a hard one-sided shadow, text recognition went from 0.472 to 0.982; scans that are already even are left bit-for-bit identical",
  "清除": "Clear",
  "清除 XMP metadata": "Clear XMP metadata",
  "清除文件資訊（作者 / 標題 / 軟體等）": "Clear document info (author / title / producer …)",
  "清除書籤 / 目錄": "Clear bookmarks / contents",
  "清除表單欄位": "Clear form fields",
  "清除註解": "Clear annotations",
  "測試 LLM 連線": "Test the LLM connection",
  "渲染解析度，預設 <code>300</code>": "Render resolution, default <code>300</code>",
  "濃度 <code>0</code>~<code>1</code>，預設 <code>1</code>": "Density <code>0</code>; <code>1</code>, default <code>1</code>",
  "為 1-based 頁碼（對應": "is a 1-based page number (the",
  "版面主題，預設 <code>classic</code>": "Layout theme, default <code>classic</code>",
  "環境需求": "Environment requirement",
  "產出": "Produces",
  "用密碼移除 PDF 加密。": "Remove PDF encryption with the password.",
  "疑難排解": "Troubleshooting",
  "的 Body（JSON）：": "body (JSON):",
  "的：整份翻完才回應，大檔會撞到反向代理的逾時。 網頁介面走的是背景作業版（": ": it responds only when the whole document is done, so a large file will hit the reverse proxy's timeout. The web interface uses the background-job version (",
  "目前<strong>沒有內建</strong> rate limit。建議部署時用反向代理（nginx / Caddy）加：": "There is <strong>no built-in</strong> rate limit at present. Add one at the reverse proxy (nginx / Caddy) when you deploy:",
  "目標 id": "Target id",
  "目標格式 id，取自上面的 <code>formats</code>": "The target format id, taken from <code>formats</code> above",
  "目標語言，預設 <code>zh-TW</code>": "Target language, default <code>zh-TW</code>",
  "目錄列到第幾層（1~3），預設 <code>3</code>": "How many levels the contents lists (1; 3), default <code>3</code>",
  "目錄插在第幾頁之前，預設 <code>1</code>（最前面）。<strong>有封面就填 <code>2</code></strong>": "Insert the contents before this page, default <code>1</code> (right at the front). <strong>With a cover page, use <code>2</code></strong>",
  "目錄頁標題，預設 <code>目錄</code>": "Table of contents title, default <code>Contents</code>",
  "直接回檔案本身（不是 JSON）。<code>format</code> 給了 <code>pdf</code> / <code>docx</code> / <code>odt</code> 以外的值回 <code>400</code>。": "Returns the file itself, not JSON. A <code>format</code> other than <code>pdf</code> / <code>docx</code> / <code>odt</code> returns <code>400</code>.",
  "直接帶 Markdown 內容。與 <code>file</code> 擇一": "The Markdown content itself. Use this or <code>file</code>",
  "相容模式": "Compatibility mode",
  "移除 PDF 註解（可依類型 / 作者篩選）。": "Remove PDF annotations (filtered by type or author).",
  "站內通知列表（未讀優先）": "In-app notification list (unread first)",
  "章的大小（mm），預設 <code>40</code>": "Stamp size (mm), default <code>40</code>",
  "端點": "Endpoint",
  "管理介面自己用的 XHR 端點": "XHR endpoints used by the admin UI itself",
  "管理員到 <code>admin → API Token</code>：": "An administrator goes to <code>admin → API Token</code>:",
  "管理員可在 <code>admin/ocr-langs → 外部 GPU 識別伺服器</code> 部署 <code>jt-ocr-server</code> 到 GPU 主機，jtdt 透過 HTTP 呼叫遠端 EasyOCR，速度比 CPU 快 10× 以上。設定相關 admin endpoints：": "An administrator can deploy <code>jt-ocr-server</code> to a GPU host from <code>admin/ocr-langs → external GPU recognition server</code>; jtdt then calls remote EasyOCR over HTTP, more than 10× faster than CPU. The related admin endpoints:",
  "管理送件前檢核用的自家公司實體（CRUD）。": "Manage your own company entities for the pre-submission check (CRUD).",
  "範例。回傳檔案的端點用": "examples. Endpoints that return a file use",
  "範本 ID（admin 端記住的版型）": "Template ID (the layout remembered in the admin area)",
  "簡介": "Introduction",
  "系統狀態 —— 檢查有沒有新版": "System status: check for a newer release",
  "系統狀態 —— 量反向代理的上傳上限": "System status: measure the reverse proxy's upload limit",
  "系統產生時的章面文字，預設 <code>騎縫章</code>（4 字以內排成田字）": "The text on a generated stamp, default <code>騎縫章</code> (up to 4 characters, laid out in a square)",
  "系統相依套件狀態": "System dependency status",
  "紙張，<code>A4</code>（預設）/ <code>A3</code> / <code>Letter</code> ...": "Paper size, <code>A4</code> (default) / <code>A3</code> / <code>Letter</code> …",
  "統編查詢（path style）": "Company ID lookup (path style)",
  "統編查詢（單筆）": "Company ID lookup (single)",
  "統編查詢（批次）": "Company ID lookup (batch)",
  "統計頁數、字數、詞數、閱讀時間與高頻詞。": "Count pages, characters, words, reading time and frequent terms.",
  "網頁介面走背景作業：<code>POST /tools/doc-straighten/load</code> 上傳 → <code>POST /tools/doc-straighten/submit</code> 送出（回 <code>job_id</code>）→ <code>GET /api/jobs/{job_id}</code> 輪詢 → <code>GET /api/jobs/{job_id}/download</code> 取結果。 逐頁預覽是 <code>POST /tools/doc-straighten/preview</code>（回修正角度與殘留角）。": "The web interface runs it as a background job: <code>POST /tools/doc-straighten/load</code> to upload, <code>POST /tools/doc-straighten/submit</code> to start (returns a <code>job_id</code>), poll <code>GET /api/jobs/{job_id}</code>, then fetch the result from <code>GET /api/jobs/{job_id}/download</code>. Per-page preview is <code>POST /tools/doc-straighten/preview</code>, which returns the correction angle and the residual.",
  "線寬（pt），預設 <code>1.5</code>": "Line width (pt), default <code>1.5</code>",
  "縮圖（PDF 首頁渲染 / PNG 原圖）": "Thumbnail (first PDF page rendered / the PNG itself)",
  "縮小 PDF 檔案大小。": "Make a PDF smaller.",
  "美化。": "to pretty-print it.",
  "而定，以下範例用": "; the examples below use",
  "自動辨識表單欄位並填入公司主檔資料。": "Detect form fields automatically and fill them from a company profile.",
  "自己指定書籤（JSON 陣列，每筆 <code>{&quot;title&quot;,&quot;page&quot;,&quot;level&quot;}</code>）。給了就取代自動產生的": "Supply the bookmarks yourself (a JSON array of <code>{&quot;title&quot;,&quot;page&quot;,&quot;level&quot;}</code>). If given, this replaces the automatically generated ones",
  "自己的印章圖（PNG / JPG，接近純白的底會自動去掉）。不給就由系統產生": "Your own stamp image (PNG / JPG; a near-white background is removed automatically). Without one the system generates a stamp",
  "自訂偵測 regex": "Custom detection regex",
  "舊版文件": "Old document",
  "舊版文字": "Old text",
  "處理模式": "Processing mode",
  "處理管線，每個元素 <code>{&quot;op&quot;: &quot;...&quot;}</code>；op 可為 <code>dedup</code> / <code>sort</code> / <code>count</code> / <code>exclude</code> / <code>lower</code> / <code>upper</code> / <code>title</code> 等": "The processing pipeline; each element is <code>{&quot;op&quot;: &quot;...&quot;}</code>, where op may be <code>dedup</code> / <code>sort</code> / <code>count</code> / <code>exclude</code> / <code>lower</code> / <code>upper</code> / <code>title</code> and others",
  "處理解析度，150 / 200（預設）/ 300": "Processing resolution: 150 / 200 (default) / 300",
  "要偵測的 PII 類型 CSV（身分證 / 電話 / email / 地址 ...），空白 = 預設集": "PII types to detect as CSV (ID number / phone / email / address …); blank means the default set",
  "要偵測的 PII 類型 ID 陣列，省略 = 預設集": "Array of PII type IDs to detect; omit for the default set",
  "要測的位址。<strong>不給就測目前已存檔的設定</strong>": "The address to test. <strong>Omit it and the saved settings are tested instead</strong>",
  "角度，預設 <code>0</code>": "Angle, default <code>0</code>",
  "解析一批台鐵 / 高鐵 / Uber 乘車證明 PDF，回結構化 JSON。<strong>只解析、不寫入使用者的清單</strong>。": "Parses a batch of Taiwan Railway, THSR or Uber ride receipt PDFs and returns structured JSON. <strong>Parsing only — nothing is written to the user's list.</strong>",
  "解析度，預設 <code>150</code>": "Resolution, default <code>150</code>",
  "解析電子發票 QR Code 內容。": "Decode the contents of an e-invoice QR code.",
  "設定匯出清單": "Settings export list",
  "該頁的 pt 尺寸 <code>[寬, 高]</code>": "the page size in points, <code>[width, height]</code>",
  "詳見 <a href=\"https://github.com/jasoncheng7115/jt-doc-tools/blob/main/OPS.md\" target=\"_blank\" rel=\"noopener\">OPS.md</a> 的「反向代理」段與「504 Gateway Timeout 排錯流程」。": "See the \"reverse proxy\" section and the \"504 Gateway Timeout troubleshooting\" section of <a href=\"https://github.com/jasoncheng7115/jt-doc-tools/blob/main/OPS.md\" target=\"_blank\" rel=\"noopener\">OPS.md</a>.",
  "認證標頭": "Authentication header",
  "語言，例 <code>chi_tra+eng</code>（預設）/ <code>eng</code> / <code>chi_sim</code>": "Language, e.g. <code>chi_tra+eng</code> (default) / <code>eng</code> / <code>chi_sim</code>",
  "說明": "Description",
  "讀取 / 更新 LLM 設定": "Read / update LLM settings",
  "讀取 / 清除 PDF metadata、XMP、書籤、註解、表單。": "Read or clear PDF metadata, XMP, bookmarks, annotations and form fields.",
  "讀自己的通知設定（要不要在作業完成時通知、走哪些管道）": "Read your own notification settings (whether to notify on job completion, and through which channels)",
  "走本地端 LLM 逐句翻譯。": "Sentence-by-sentence translation with a local LLM.",
  "起始頁碼，預設 <code>1</code>": "Starting page number, default <code>1</code>",
  "跟 §14b 的工作區同性質：綁登入 session（cookie），<strong>不屬於對外 Bearer API</strong>， 因為內容按登入者隔離。未登入一律 401 / 302。": "Same nature as the workspace in §14b: bound to the login session (cookie), <strong>not part of the public Bearer API</strong>, because the contents are isolated per signed-in user. Requests without a session get 401 / 302.",
  "輪詢 <code>GET /api/jobs/{job_id}</code> 直到 <code>status == &quot;completed&quot;</code>": "Poll <code>GET /api/jobs/{job_id}</code> until <code>status == &quot;completed&quot;</code>",
  "輸出檔名": "Output filename",
  "輸出檔名，預設 <code>scan-merge.pdf</code>": "Output file name, default <code>scan-merge.pdf</code>",
  "轉成黑白。<strong>預設關閉</strong> —— 實測會讓中文的細筆畫消失、文字辨識率明顯下降；它的用途是縮小檔案": "Convert to black and white. <strong>Off by default</strong>: measurements show it erases thin Chinese strokes and clearly lowers OCR accuracy; its purpose is a smaller file, not clearer text",
  "轉換引擎：<code>pdf2docx-refine</code>（預設，穩定）/ <code>jtdt-reform</code>（自家版面重組）/ <code>jtdt-layout</code>（版面重現：LibreOffice/OxOffice Draw 精準匯入 + 自家轉換引擎重組成 docx/odt，版面近 1:1 的頁面錨定文字方塊，含圖片 / 框線；本質非流動文字、無真表格）": "Conversion engine: <code>pdf2docx-refine</code> (default, stable) / <code>jtdt-reform</code> (our own layout rebuild) / <code>jtdt-layout</code> (layout reproduction: precise import through LibreOffice/OxOffice Draw, then rebuilt by our own engine)",
  "辦公文件 文件": "Office document",
  "辦公文件會<strong>先轉成 PDF 再統計</strong> —— 頁數與每頁字數才是「真的印出來會長那樣」的 數字。因此這支端點在收辦公文件時需要 Office 引擎（OxOffice / LibreOffice）。": "Office documents are <strong>converted to PDF before counting</strong>; that is the only way the page count and the words per page match what actually prints. This endpoint therefore needs an Office engine (OxOffice / LibreOffice) when it is given an office document.",
  "辦公文件格式互轉": "Office format conversion",
  "辦公文件轉 PDF": "Office to PDF",
  "辦公文件（上列九種副檔名）": "Office document (the nine extensions above)",
  "辦公文件，可重複帶多份（多份會打包成 ZIP）": "Office document; may be repeated (several files come back as a ZIP)",
  "送件前檢核 — 自家公司主檔": "Pre-submission check; your own company profile",
  "透明度 0–1，預設 <code>0.15</code>": "Opacity 0; 1, default <code>0.15</code>",
  "逐句翻譯": "Sentence translation",
  "逐字稿：<code>.vtt</code> / <code>.srt</code> / <code>.json</code> / <code>.txt</code> / <code>.md</code> / <code>.docx</code> / <code>.odt</code>": "Transcript: <code>.vtt</code> / <code>.srt</code> / <code>.json</code> / <code>.txt</code> / <code>.md</code> / <code>.docx</code> / <code>.odt</code>",
  "這個端點是": "This endpoint is",
  "這四支": "These four",
  "這次比對的識別碼（<strong>回應最上層</strong>），拿去抓頁面圖": "the id of this comparison (<strong>at the top level of the response</strong>), used to fetch the page images",
  "連不上時<strong>回 200</strong>、<code>ok: false</code> 加 <code>error</code> —— 「我們測不到對方」不是 這個請求本身失敗。": "When the connection fails it still <strong>returns 200</strong>, with <code>ok: false</code> and an <code>error</code> message; being unable to reach the server is not a failure of this request itself.",
  "連結": "Links",
  "邊距（mm）": "Margin (mm)",
  "邊距（mm），預設 <code>0</code>": "Margin (mm), default <code>0</code>",
  "邊距（mm），預設 <code>5</code>": "Margin (mm), default <code>5</code>",
  "那一頁的差異框。每筆是 <code>{tag, line, rects}</code>，<code>tag</code> 是 <code>delete</code> / <code>insert</code> / <code>replace</code>": "the difference boxes on that page. Each is <code>{tag, line, rects}</code>, where <code>tag</code> is <code>delete</code> / <code>insert</code> / <code>replace</code>",
  "重新命名（<code>file_id</code>、<code>name</code>）": "Rename (<code>file_id</code>, <code>name</code>)",
  "錯誤格式": "Error format",
  "長時間或批次操作走 job queue。流程：": "Long or batch operations go through the job queue. The flow:",
  "開啟密碼": "Open password",
  "陰影位移（mm），預設 <code>0.6</code>": "Shadow offset (mm), default <code>0.6</code>",
  "陰影擴散（mm），預設 <code>1.2</code>": "Shadow spread (mm), default <code>1.2</code>",
  "陰影濃度 <code>0</code>~<code>1</code>，預設 <code>0.25</code>": "Shadow density <code>0</code>; <code>1</code>, default <code>0.25</code>",
  "陰影顏色 hex，預設 <code>#000000</code>": "Shadow colour as hex, default <code>#000000</code>",
  "雙框兩線間距（mm），預設 <code>1.5</code>": "Gap between the two lines of a double border (mm), default <code>1.5</code>",
  "離頁緣（mm），預設 <code>3</code>": "Distance from the page edge (mm), default <code>3</code>",
  "電子發票後端狀態": "e-Invoice backend status",
  "電子發票掃描": "e-Invoice scan",
  "需 admin 啟用 LLM 服務（": "Requires an administrator to enable the LLM service (",
  "需 admin 登入或 admin role token。": "Requires an administrator sign-in or an admin-role token.",
  "需要先在管理區啟用 LLM；沒啟用回 <strong>503</strong>。逐字稿讀不出東西回 <strong>400</strong> （訊息會說得出支援哪些格式）。": "The LLM must be enabled in the admin area first; if it is not, this returns <strong>503</strong>. A transcript that cannot be read returns <strong>400</strong> (the message says which formats are supported).",
  "頁碼規格，例 <code>1,3,5-8</code>": "Page specification, e.g. <code>1,3,5-8</code>",
  "頁與頁之間插入分隔線，預設 <code>true</code>": "Insert a separator between pages, default <code>true</code>",
  "頁面圖": "Page image",
  "項目": "Item",
  "預設不開，產生的假值": "is off by default, and the fake values it generates",
  "領域提示": "Domain hint",
  "領域提示（提升專業詞彙準確度）": "Domain hint (sharpens specialist vocabulary)",
  "領域提示（法律合約、醫療報告…），提升專業用詞準確度": "Domain hint (legal contract, medical report …) to sharpen the terminology",
  "類型": "Type",
  "顯示用的來源檔名（會出現在「我的作業」）": "Source filename for display (it appears in “My jobs”)",
  "首頁不加框（投影片封面常是滿版設計），預設 <code>false</code>": "No border on the first page (a slide cover is often a full-bleed design), default <code>false</code>",
  "高度（mm）": "Height (mm)",
  "點「核發新 token」→ 輸入用途名稱（例 <code>gitlab-ci</code>）→ 拿到 64 字 hex token（<strong>只顯示一次，存好</strong>）": "Press \"issue a new token\" → give it a purpose (for example <code>gitlab-ci</code>) → receive a 64-character hex token (<strong>shown once only; keep it safe</strong>)",
  "（LLM streaming 友善）": "(friendly to LLM streaming)",
  "（不是畫素）—— 乘上你顯示的頁面圖尺寸就是位置": " (not pixels): multiply by the size of the page image you are showing",
  "（依": "(depends on",
  "（依範圍）/": "(by range) /",
  "（側邊騎縫，預設）/": "(side seam, default) /",
  "（刪除）/": "(delete) /",
  "（只留）/": "(keep only) /",
  "（對開跨頁）": "(across a spread)",
  "（平鋪）/": "(tiled) /",
  "（必設 — LLM 工具單筆推理常 5-15 分鐘，預設 60s 必定 504）": "(required; a single LLM inference often takes 5-15 minutes, and the 60s default is guaranteed to give a 504)",
  "（必設，否則 PDF 大檔會被拒）": "(required, otherwise large PDFs are rejected)",
  "（換成假值）": "(replace with fake values)",
  "（放大填滿）": "(scale up to fill)",
  "（新版）": " (new version)",
  "（最小）": "(smallest)",
  "（未啟用 enforce 時可省略）": "(may be omitted while enforce is off)",
  "（每 N 頁切一份）/": "(one file every N pages) /",
  "（每頁一份）": "(one file per page)",
  "（每頁）/": "(per page) /",
  "（真刪）/": "(genuine deletion) /",
  "（縮放留白，預設，不會掉內容）/": "(scale to fit, default, nothing is lost) /",
  "（置中不縮放，超出會裁掉）/": "(centre without scaling; anything outside is cropped) /",
  "（置中）": "(centred)",
  "（自頁緣內縮，預設）/": "(inset from the page edge, default) /",
  "（舊版）或": " (old version) or",
  "（貼齊該頁內容）": "(flush with that page's content)",
  "（跟著原頁，預設）/": "(follow the original page, default) /",
  "（輕度）/": "(light) /",
  "（遮罩，預設）/": "(mask, default) /",
  "（重排）": "(reorder)",
  "（預設）/": "(default) /",
  "（預設）跑第二輪複審；": "(default) runs the second review pass;",
  "（預設，平衡）/": "(default, balanced) /",
  "）。未啟用回": "). If it is not enabled it returns",
  "）。缺模組時只會看到「轉檔成功但找不到輸出」， 可在「設定 → 相依套件檢查」確認。": "). Without the module you only see \"converted successfully but no output found\"; check it under \"settings → dependency check\".",
  "， 送出後用": ", and after submitting use",
  "，絕不會撞到真人資料。 設成": ", so it can never collide with a real person's data. Set to",
  "，都是文件專用的保留範圍，兩種設定都安全。": ", all of them ranges reserved for documentation, so either setting is safe.",
  "，預設": ", default",
  "：API 預設可不認證（與 web UI 同步開放）；需要鎖時管理員到 admin 「API Token」頁開啟 enforce 模式並核發 bearer token。": ": the API is open by default (matching the web UI); to lock it, an administrator turns on enforce mode on the admin “API Token” page and issues bearer tokens.",
  "：PDF 裡沒有段落，文字是定位好的碎片，換成長度不同的譯文之後 版面一定跑掉。要翻 PDF 請用下面的「逐句翻譯」。": ": a PDF has no paragraphs; its text is positioned fragments, and replacing them with translations of a different length is bound to break the layout. To translate a PDF, use “sentence translation” below.",
  "：它們回傳的是「這個登入者的」資料， 拿 token 呼叫沒有意義（token 綁的是使用者，但這些端點的設計是給瀏覽器用的）。": ": they return data belonging to the signed-in user, so calling them with a token makes little sense (a token is bound to a user, but these endpoints are designed for the browser).",
  "：需要 office 套件的": ": the office suite's",
  "簽章網址取檔（不是給人呼叫的）": "Signed-URL file fetch (not meant to be called by hand)",
  "有些外部服務只收「網址」—— 檔案不是我們上傳給它，是它拿我們給的網址自己來拉。 這種情況我們給的是<strong>短效簽章網址</strong>，而不是 API Token：": "Some external services only accept a URL — the file is not uploaded to them, they fetch it themselves from an address we provide. In that case we hand over a <strong>short-lived signed URL</strong> rather than an API token:",
  "<strong>認證是網址本身</strong>，不帶 <code>Authorization</code>。簽章綁住「檔案 id ＋ 到期時間」，": "<strong>The URL itself is the authentication</strong>, with no <code>Authorization</code> header. The signature binds the file id together with the expiry,",
  "所以改 <code>exp</code> 延期會讓簽章失效。": "so extending <code>exp</code> invalidates the signature.",
  "<strong>驗不過一律回 404</strong> —— 格式不對、簽章錯、過期、檔案不在，四種在外面": "<strong>Anything that fails to verify returns 404</strong> — a malformed id, a wrong signature, an expired URL and a missing file all",
  "看起來一模一樣（回 403 等於告訴對方「這個 id 是存在的」）。": "look identical from outside (a 403 would tell the caller “that id exists”).",
  "網址由伺服器端產生，<strong>沒有公開的端點可以要一組簽章</strong> ——": "The URL is produced server-side, and <strong>there is no public endpoint that hands out a signature</strong> —",
  "這一條列在這裡是為了「這份文件不漏掉任何端點」，不是給你呼叫的。": "it is listed here so that this document misses no endpoint, not for you to call.",
  "拉檔拿到 404 時，最常見的原因是<strong>網址過期</strong>而不是檔案不見了。": "If a fetch returns 404, the usual cause is <strong>an expired URL</strong>, not a missing file.",
  "語音服務（jtlw）—— 存送件位址與金鑰": "Speech service (jtlw): saves the endpoint and API key",
  "語音服務（jtlw）—— 測連線（<code>/health</code> ＋ <code>/capabilities</code> 兩段）": "Speech service (jtlw): connection test (<code>/health</code> plus <code>/capabilities</code>, both halves)",
  "語音服務（jtlw）—— 取對方提供的處理設定清單（下拉用）": "Speech service (jtlw): fetches the processing profiles they offer (for the dropdown)",
  "會議錄音轉逐字稿": "Meeting recording to transcript",
  "把會議錄音或錄影轉成<strong>帶時間與發言者</strong>的逐字稿。辨識在外部語音服務（jtlw） 那側跑 —— <strong>要先在管理區「語音服務（jtlw）」設定好</strong>，沒設定回 <strong>503</strong>。": "Turns a meeting recording or video into a transcript <strong>with timings and speakers</strong>. Recognition runs on the external speech service (jtlw), so <strong>it has to be set up under the admin area's speech service (jtlw) page first</strong>; without that it returns <strong>503</strong>.",
  "音訊 <code>.m4a</code> / <code>.mp3</code> / <code>.wav</code> / <code>.aac</code> / <code>.ogg</code> / <code>.opus</code> / <code>.flac</code>，影片 <code>.mp4</code> / <code>.mov</code> / <code>.mkv</code> / <code>.webm</code>": "Audio <code>.m4a</code> / <code>.mp3</code> / <code>.wav</code> / <code>.aac</code> / <code>.ogg</code> / <code>.opus</code> / <code>.flac</code>, video <code>.mp4</code> / <code>.mov</code> / <code>.mkv</code> / <code>.webm</code>",
  "（預設）或 BCP-47（": "(the default) or BCP-47 (",
  "…）。對方不支援時": "…). When the speech service does not support it,",
  "送件當下": "at submission time",
  "預設 <code>0</code> ＝ 讓它自己判。<strong>建議就留 0</strong>": "Defaults to <code>0</code>, which lets it decide for itself. <strong>Leaving it at 0 is the recommendation</strong>",
  "<strong>這支是同步的</strong> —— 37 分鐘的會議實測 7~8 分鐘（辨識 ＋ 發言者分離 ＋ 校正）， 呼叫端的逾時要放寬。要背景處理請走網頁那條路 （<code>POST /upload</code> → <code>POST /start</code> → 拿作業編號輪詢 <code>/api/jobs/{id}</code>）。": "<strong>This endpoint is synchronous</strong>: a 37-minute meeting measured 7 to 8 minutes (recognition plus speaker separation plus correction), so give the caller a generous timeout. For background processing use the web path instead (<code>POST /upload</code> → <code>POST /start</code> → poll <code>/api/jobs/{id}</code> with the job id).",
  "<strong><code>segments</code> 是三層併起來的</strong>：文字用校正後的、時間來自原始辨識層、 發言者來自 speakers 層，三層以 <code>seq</code> 對應。對不上的段落<strong>不會硬湊</strong> —— 寧可那一段沒有發言者，也不要把別人的名字貼上去。": "<strong><code>segments</code> is three layers merged</strong>: the text comes from the corrected layer, the timings from the raw recognition layer and the speakers from the speakers layer, matched by <code>seq</code>. Segments that do not line up <strong>are not forced together</strong>: better to leave a segment without a speaker than to put somebody else's name on it.",
  "為": "is",
  "代表": "means",
  "校正那一步失敗了": "the correction step failed",
  "，你拿到的是原始辨識結果 （標點與錯字沒有修過）—— 這時候別把它當成校正過的內容去比對。": ", and what you get back is the raw recognition output (punctuation and misrecognitions unrepaired); do not compare it as though it had been corrected.",
  "是代號（": "is a code (",
  "…）。姓名對照是呼叫端自己的事； 網頁那條路可以點代號直接改成人名。": "…). Mapping those to names is up to the caller; on the web path you can click a code and rename it.",
  "<strong><code>num_speakers</code> 問的是「發言量足以辨認的人數」，不是與會人數。</strong> 語音服務在 <strong>20 場中文會議</strong>（AISHELL-4 test，5~7 人）上量過：指定正確人數與 不指定<strong>分不出勝負</strong>（13.24% vs 12.46%，配對 bootstrap 95% 信賴區間 [−2.72, +4.10] 跨過 0）。<strong>所以不要為了「更準」而去填它。</strong>": "<strong><code>num_speakers</code> asks how many people spoke enough to be identifiable, not how many attended.</strong> The speech service measured it over <strong>20 Chinese meetings</strong> (AISHELL-4 test, five to seven people): giving the correct count and leaving it unset are <strong>indistinguishable</strong> (13.24% vs 12.46%; the paired bootstrap 95% confidence interval [−2.72, +4.10] crosses zero). <strong>So do not fill it in hoping for a better result.</strong>",
  "真正的理由是機制：同一批 20 場裡，<strong>最佳的群數從來沒有大於實際人數</strong> （10 場相等、10 場更少、0 場更多）。有 6 個人在場不代表聲學上分得出 6 群 —— 硬要分成 6 群時，系統只能<strong>把講最多的那個人拆開</strong>，而那是最貴的錯。 只講一兩句的人不要算進去；<strong>不確定就留 0</strong>。": "The real reason is mechanical: across those same 20 meetings, <strong>the best number of clusters was never larger than the actual number of people</strong> (equal in 10, smaller in 10, larger in none). Six people in the room does not mean six groups can be told apart acoustically; forced into six, the system can only <strong>split the person who spoke the most</strong>, and that is the most expensive mistake of all. Do not count people who said only a sentence or two; <strong>when in doubt, leave it at 0</strong>.",
  "就回 400。": "it returns 400 right away.",
  "會換成": "are converted to",
  "狀態碼": "Status",
  "意思": "Meaning",
  "要做什麼": "What to do",
  "檔案格式不收、檔案是空的，或語音服務退回你帶的參數（<code>language</code> / <code>num_speakers</code>）": "The file type is not accepted, the file is empty, or the speech service rejected a parameter you sent (<code>language</code> / <code>num_speakers</code>)",
  "改參數；語言不確定就用 <code>auto</code>": "Change the parameter; if you are unsure of the language, use <code>auto</code>",
  "語音服務收下了但處理失敗（辨識失敗、結果是空的、它自己出錯）": "The speech service accepted the job but it failed (recognition failed, the result was empty, or the service itself errored)",
  "訊息裡有原因；一直發生請管理員查語音服務": "The message gives the reason; if it keeps happening, ask an administrator to check the speech service",
  "還沒設定語音服務，或連不上": "The speech service is not set up yet, or cannot be reached",
  "請管理員檢查「語音服務（jtlw）」設定與連線": "Ask an administrator to check the “Speech service (jtlw)” settings and connection",
  "等太久還沒結果（排隊超過 4 小時，或辨識超過上限）": "No result after too long (queued for more than 4 hours, or recognition exceeded the limit)",
  "已經請對方取消；稍後再送": "The job has already been cancelled on the speech service; send it again later",
  "再送出": "before sending.",
  "<strong><code>auto</code> 只看錄音開頭約 30 秒的講話，決定整場用哪一種語言</strong> —— 不是逐段判斷。 開頭若有人先講另一種語言（20 秒就夠），整場都可能辨識錯；開頭的靜音、雜音不影響。 已知整場的語言時請直接指定。 中英<strong>整句</strong>交錯的會議請指定佔多數的語言：另一種語言的整句大多無法正確辨識 （中文句子裡夾英文術語沒有問題）。": "<strong><code>auto</code> only listens to about the first 30 seconds of speech and uses that language for the whole meeting</strong>; it does not decide per segment. If someone speaks another language at the start (20 seconds is enough), the whole meeting may be recognised wrongly; silence or background noise at the start does not matter. When you know the language of the whole meeting, set it. For meetings where <strong>whole sentences</strong> alternate between Chinese and English, set the language used most: whole sentences in the other language are mostly not recognised correctly (English terms inside Chinese sentences are fine)."
}
